# Issue 30782: Add `__eq__` method to class `FDerivativeOperator`

archive/issues_030782.json:
```json
{
    "body": "CC:  @nbruin @slel\n\nKeywords: FDerivativeOperator\n\nDefine a function and its derivative:\n\n```\nsage: U = function('U')\nsage: U(x).diff(x)\ndiff(U(x), x)\nsage: U(x).diff(x).operator()\nD[0](U)\n```\n\nBefore this ticket:\n\n```\nsage: U(x).diff(x).operator() == U(x).diff(x).operator()\nFalse\n```\n\nAfter this ticket:\n\n```\nsage: U(x).diff(x).operator() == U(x).diff(x).operator()\nTrue\n```\n\n\nIssue created by migration from https://trac.sagemath.org/ticket/31019\n\n",
    "created_at": "2020-12-07T02:50:20Z",
    "labels": [
        "symbolics",
        "major",
        "enhancement"
    ],
    "milestone": "https://github.com/sagemath/sagetest/milestones/sage-9.8",
    "title": "Add `__eq__` method to class `FDerivativeOperator`",
    "type": "issue",
    "url": "https://github.com/sagemath/sagetest/issues/30782",
    "user": "@slel"
}
```
CC:  @nbruin @slel

Keywords: FDerivativeOperator

Define a function and its derivative:

```
sage: U = function('U')
sage: U(x).diff(x)
diff(U(x), x)
sage: U(x).diff(x).operator()
D[0](U)
```

Before this ticket:

```
sage: U(x).diff(x).operator() == U(x).diff(x).operator()
False
```

After this ticket:

```
sage: U(x).diff(x).operator() == U(x).diff(x).operator()
True
```


Issue created by migration from https://trac.sagemath.org/ticket/31019





---

archive/issue_comments_440108.json:
```json
{
    "body": "Initial discussion:\n\n- [Ask Sage question 54555: Substitute U' to D[0]U](https://ask.sagemath.org/question/54555)",
    "created_at": "2020-12-07T02:57:00Z",
    "issue": "https://github.com/sagemath/sagetest/issues/30782",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/30782#issuecomment-440108",
    "user": "@slel"
}
```

Initial discussion:

- [Ask Sage question 54555: Substitute U' to D[0]U](https://ask.sagemath.org/question/54555)



---

archive/issue_comments_440109.json:
```json
{
    "body": "Setting new milestone based on a cursory review of ticket status, priority, and last modification date.",
    "created_at": "2021-02-13T20:51:01Z",
    "issue": "https://github.com/sagemath/sagetest/issues/30782",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/30782#issuecomment-440109",
    "user": "@mkoeppe"
}
```

Setting new milestone based on a cursory review of ticket status, priority, and last modification date.
