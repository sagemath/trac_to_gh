# Issue 23204: Remove RingHomomorphism_coercion

archive/issues_022967.json:
```json
{
    "assignees": [],
    "body": "we should try to use `.coerce_map_from()` instead, so we get `is_injective()/is_surjective()` and friends right for `Hom.natural_map()`.\n\nDepends on #23184\nDepends on #23211\n\nCC:  @xcaruso\n\nKeywords: **sd86.5, sd87**\n\nBranch/Commit: **[382e2d1](https://github.com/sagemath/sagetrac-mirror/commit/382e2d180772eb9d355aa7fbf539dcc52607a379)**\n\nReviewer: **Travis Scrimshaw, Aly Deines, William Stein**\n\nAuthor: **David Roe, Julian R\u00fcth**\n\n_Issue created by migration from https://trac.sagemath.org/ticket/23204_\n\n",
    "closed_at": "2017-07-29T15:30:52Z",
    "created_at": "2017-06-09T23:17:44Z",
    "labels": [
        "https://github.com/sagemath/sage/labels/component%3A%20commutative%20algebra",
        "https://github.com/sagemath/sage/labels/p3%20%E2%80%93%20major",
        "https://github.com/sagemath/sage/labels/bug"
    ],
    "milestone": "https://github.com/sagemath/sage/milestones/sage-8.0",
    "reactions": [],
    "repository": "https://github.com/sagemath/sage",
    "title": "Remove RingHomomorphism_coercion",
    "type": "issue",
    "updated_at": "2017-07-29T15:30:52Z",
    "url": "https://github.com/sagemath/sage/issues/23204",
    "user": "https://github.com/saraedum"
}
```
we should try to use `.coerce_map_from()` instead, so we get `is_injective()/is_surjective()` and friends right for `Hom.natural_map()`.

Depends on #23184
Depends on #23211

CC:  @xcaruso

Keywords: **sd86.5, sd87**

Branch/Commit: **[382e2d1](https://github.com/sagemath/sagetrac-mirror/commit/382e2d180772eb9d355aa7fbf539dcc52607a379)**

Reviewer: **Travis Scrimshaw, Aly Deines, William Stein**

Author: **David Roe, Julian Rüth**

_Issue created by migration from https://trac.sagemath.org/ticket/23204_





---

archive/issue_events_305367.json:
```json
{
    "actor": "https://github.com/saraedum",
    "created_at": "2017-06-09T23:17:44Z",
    "event": "milestoned",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "milestone_number": null,
    "milestone_title": "sage-8.0",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/23204#event-305367"
}
```



---

archive/issue_events_305368.json:
```json
{
    "actor": "https://github.com/saraedum",
    "created_at": "2017-06-09T23:17:44Z",
    "event": "labeled",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "label": "https://github.com/sagemath/sage/labels/component%3A%20commutative%20algebra",
    "label_color": "0000ff",
    "label_name": "component: commutative algebra",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/23204#event-305368"
}
```



---

archive/issue_events_305369.json:
```json
{
    "actor": "https://github.com/saraedum",
    "created_at": "2017-06-09T23:17:44Z",
    "event": "labeled",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "label": "https://github.com/sagemath/sage/labels/p3%20%E2%80%93%20major",
    "label_color": "ffbb00",
    "label_name": "p3 \u2013 major",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/23204#event-305369"
}
```



---

archive/issue_events_305370.json:
```json
{
    "actor": "https://github.com/saraedum",
    "created_at": "2017-06-09T23:17:44Z",
    "event": "labeled",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "label": "https://github.com/sagemath/sage/labels/bug",
    "label_color": "d73a4a",
    "label_name": "bug",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/23204#event-305370"
}
```



---

archive/issue_comments_350727.json:
```json
{
    "body": "Changed dependencies from **#23184** to **#23184, #23211**",
    "created_at": "2017-06-11T00:36:29Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350727",
    "user": "https://github.com/roed314"
}
```

Changed dependencies from **#23184** to **#23184, #23211**



---

archive/issue_comments_350728.json:
```json
{
    "body": "Branch: **[u/roed/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/roed/remove_ringhomomorphism_coercion)**",
    "created_at": "2017-06-11T18:39:40Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350728",
    "user": "https://github.com/roed314"
}
```

Branch: **[u/roed/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/roed/remove_ringhomomorphism_coercion)**



---

archive/issue_comments_350729.json:
```json
{
    "body": "Changed branch from **[u/roed/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/roed/remove_ringhomomorphism_coercion)** to **[u/saraedum/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/saraedum/remove_ringhomomorphism_coercion)**",
    "created_at": "2017-06-22T04:41:01Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350729",
    "user": "https://github.com/saraedum"
}
```

Changed branch from **[u/roed/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/roed/remove_ringhomomorphism_coercion)** to **[u/saraedum/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/saraedum/remove_ringhomomorphism_coercion)**



---

archive/issue_comments_350730.json:
```json
{
    "body": "Changed branch from **[u/saraedum/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/saraedum/remove_ringhomomorphism_coercion)** to **[u/roed/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/roed/remove_ringhomomorphism_coercion)**",
    "created_at": "2017-06-27T00:57:17Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350730",
    "user": "https://github.com/roed314"
}
```

Changed branch from **[u/saraedum/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/saraedum/remove_ringhomomorphism_coercion)** to **[u/roed/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/roed/remove_ringhomomorphism_coercion)**



---

archive/issue_comments_350731.json:
```json
{
    "body": "<a id='comment:5'>Comment 5:</a>\nBranch pushed to git repo; I updated commit sha1. New commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/2043ff3a6b723c7497de9561350a3118f0c096dc\">2043ff3</a></td><td><code>Fixing doctest errors</code></td></tr></table>\n",
    "created_at": "2017-06-27T23:57:17Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350731",
    "user": "https://github.com/sagetrac-git"
}
```

<a id='comment:5'>Comment 5:</a>
Branch pushed to git repo; I updated commit sha1. New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/2043ff3a6b723c7497de9561350a3118f0c096dc">2043ff3</a></td><td><code>Fixing doctest errors</code></td></tr></table>




---

archive/issue_comments_350732.json:
```json
{
    "body": "Commit: **[2043ff3](https://github.com/sagemath/sagetrac-mirror/commit/2043ff3a6b723c7497de9561350a3118f0c096dc)**",
    "created_at": "2017-06-27T23:57:17Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350732",
    "user": "https://github.com/sagetrac-git"
}
```

Commit: **[2043ff3](https://github.com/sagemath/sagetrac-mirror/commit/2043ff3a6b723c7497de9561350a3118f0c096dc)**



---

archive/issue_comments_350733.json:
```json
{
    "body": "Changed branch from **[u/roed/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/roed/remove_ringhomomorphism_coercion)** to **[u/saraedum/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/saraedum/remove_ringhomomorphism_coercion)**",
    "created_at": "2017-06-28T00:01:09Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350733",
    "user": "https://github.com/saraedum"
}
```

Changed branch from **[u/roed/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/roed/remove_ringhomomorphism_coercion)** to **[u/saraedum/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/saraedum/remove_ringhomomorphism_coercion)**



---

archive/issue_comments_350734.json:
```json
{
    "body": "Work Issues: **__nonzero__ and is_zero should work like before**",
    "created_at": "2017-06-28T01:58:10Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350734",
    "user": "https://github.com/saraedum"
}
```

Work Issues: **__nonzero__ and is_zero should work like before**



---

archive/issue_comments_350735.json:
```json
{
    "body": "<a id='comment:8'>Comment 8:</a>\nBranch pushed to git repo; I updated commit sha1. New commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/d7eb1797654cc430c60188c8dac6c004e5ea2615\">d7eb179</a></td><td><code>Remove RingHomomorphism_coercion</code></td></tr><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/dbd5b0d1ae75fbc71ee78ffcfa71e99085071e85\">dbd5b0d</a></td><td><code>fix morphism printing in doctests</code></td></tr><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/f606020c2d3d370330bdbb44fdf3f00cb3e64cf7\">f606020</a></td><td><code>Fixed some doctests</code></td></tr></table>\n",
    "created_at": "2017-06-28T02:00:40Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350735",
    "user": "https://github.com/sagetrac-git"
}
```

<a id='comment:8'>Comment 8:</a>
Branch pushed to git repo; I updated commit sha1. New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/d7eb1797654cc430c60188c8dac6c004e5ea2615">d7eb179</a></td><td><code>Remove RingHomomorphism_coercion</code></td></tr><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/dbd5b0d1ae75fbc71ee78ffcfa71e99085071e85">dbd5b0d</a></td><td><code>fix morphism printing in doctests</code></td></tr><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/f606020c2d3d370330bdbb44fdf3f00cb3e64cf7">f606020</a></td><td><code>Fixed some doctests</code></td></tr></table>




---

archive/issue_comments_350736.json:
```json
{
    "body": "Changed commit from **[2043ff3](https://github.com/sagemath/sagetrac-mirror/commit/2043ff3a6b723c7497de9561350a3118f0c096dc)** to **[f606020](https://github.com/sagemath/sagetrac-mirror/commit/f606020c2d3d370330bdbb44fdf3f00cb3e64cf7)**",
    "created_at": "2017-06-28T02:00:40Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350736",
    "user": "https://github.com/sagetrac-git"
}
```

Changed commit from **[2043ff3](https://github.com/sagemath/sagetrac-mirror/commit/2043ff3a6b723c7497de9561350a3118f0c096dc)** to **[f606020](https://github.com/sagemath/sagetrac-mirror/commit/f606020c2d3d370330bdbb44fdf3f00cb3e64cf7)**



---

archive/issue_comments_350737.json:
```json
{
    "body": "Changed commit from **[f606020](https://github.com/sagemath/sagetrac-mirror/commit/f606020c2d3d370330bdbb44fdf3f00cb3e64cf7)** to **[db5cee6](https://github.com/sagemath/sagetrac-mirror/commit/db5cee6d098314aedd80af7f7ac8e18df32c2e20)**",
    "created_at": "2017-07-16T03:15:45Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350737",
    "user": "https://github.com/sagetrac-git"
}
```

Changed commit from **[f606020](https://github.com/sagemath/sagetrac-mirror/commit/f606020c2d3d370330bdbb44fdf3f00cb3e64cf7)** to **[db5cee6](https://github.com/sagemath/sagetrac-mirror/commit/db5cee6d098314aedd80af7f7ac8e18df32c2e20)**



---

archive/issue_comments_350738.json:
```json
{
    "body": "<a id='comment:9'>Comment 9:</a>\nBranch pushed to git repo; I updated commit sha1. New commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/db5cee6d098314aedd80af7f7ac8e18df32c2e20\">db5cee6</a></td><td><code>implement zero() for ring homsets</code></td></tr></table>\n",
    "created_at": "2017-07-16T03:15:45Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350738",
    "user": "https://github.com/sagetrac-git"
}
```

<a id='comment:9'>Comment 9:</a>
Branch pushed to git repo; I updated commit sha1. New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/db5cee6d098314aedd80af7f7ac8e18df32c2e20">db5cee6</a></td><td><code>implement zero() for ring homsets</code></td></tr></table>




---

archive/issue_comments_350739.json:
```json
{
    "body": "Changed commit from **[db5cee6](https://github.com/sagemath/sagetrac-mirror/commit/db5cee6d098314aedd80af7f7ac8e18df32c2e20)** to **[a42ebcf](https://github.com/sagemath/sagetrac-mirror/commit/a42ebcf334ac70c16dfdffe052f6968fa643f858)**",
    "created_at": "2017-07-16T03:21:47Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350739",
    "user": "https://github.com/sagetrac-git"
}
```

Changed commit from **[db5cee6](https://github.com/sagemath/sagetrac-mirror/commit/db5cee6d098314aedd80af7f7ac8e18df32c2e20)** to **[a42ebcf](https://github.com/sagemath/sagetrac-mirror/commit/a42ebcf334ac70c16dfdffe052f6968fa643f858)**



---

archive/issue_comments_350740.json:
```json
{
    "body": "<a id='comment:10'>Comment 10:</a>\nBranch pushed to git repo; I updated commit sha1. New commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/a42ebcf334ac70c16dfdffe052f6968fa643f858\">a42ebcf</a></td><td><code>implement `__nonzero__` when there is no zero element</code></td></tr></table>\n",
    "created_at": "2017-07-16T03:21:47Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350740",
    "user": "https://github.com/sagetrac-git"
}
```

<a id='comment:10'>Comment 10:</a>
Branch pushed to git repo; I updated commit sha1. New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/a42ebcf334ac70c16dfdffe052f6968fa643f858">a42ebcf</a></td><td><code>implement `__nonzero__` when there is no zero element</code></td></tr></table>




---

archive/issue_events_305371.json:
```json
{
    "actor": "https://github.com/saraedum",
    "created_at": "2017-07-16T03:22:06Z",
    "event": "labeled",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "label": "https://github.com/sagemath/sage/labels/needs%20info",
    "label_color": "ffff00",
    "label_name": "needs info",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/23204#event-305371"
}
```



---

archive/issue_comments_350741.json:
```json
{
    "body": "<a id='comment:11'>Comment 11:</a>\nNew commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/a42ebcf334ac70c16dfdffe052f6968fa643f858\">a42ebcf</a></td><td><code>implement `__nonzero__` when there is no zero element</code></td></tr></table>\n\n---\nNew commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/a42ebcf334ac70c16dfdffe052f6968fa643f858\">a42ebcf</a></td><td><code>implement `__nonzero__` when there is no zero element</code></td></tr></table>\n",
    "created_at": "2017-07-16T03:22:06Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350741",
    "user": "https://github.com/saraedum"
}
```

<a id='comment:11'>Comment 11:</a>
New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/a42ebcf334ac70c16dfdffe052f6968fa643f858">a42ebcf</a></td><td><code>implement `__nonzero__` when there is no zero element</code></td></tr></table>

---
New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/a42ebcf334ac70c16dfdffe052f6968fa643f858">a42ebcf</a></td><td><code>implement `__nonzero__` when there is no zero element</code></td></tr></table>




---

archive/issue_comments_350742.json:
```json
{
    "body": "Changed work issues from **__nonzero__ and is_zero should work like before** to **check that __nonzero__ does not cause a major speed regression**",
    "created_at": "2017-07-16T03:22:06Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350742",
    "user": "https://github.com/saraedum"
}
```

Changed work issues from **__nonzero__ and is_zero should work like before** to **check that __nonzero__ does not cause a major speed regression**



---

archive/issue_comments_350743.json:
```json
{
    "body": "Changed keywords from **sd86.5** to **sd86.5, sd87**",
    "created_at": "2017-07-17T17:45:04Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350743",
    "user": "https://github.com/roed314"
}
```

Changed keywords from **sd86.5** to **sd86.5, sd87**



---

archive/issue_events_305372.json:
```json
{
    "actor": "https://github.com/saraedum",
    "created_at": "2017-07-17T20:40:51Z",
    "event": "unlabeled",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "label": "https://github.com/sagemath/sage/labels/needs%20info",
    "label_color": "ffff00",
    "label_name": "needs info",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/23204#event-305372"
}
```



---

archive/issue_events_305373.json:
```json
{
    "actor": "https://github.com/saraedum",
    "created_at": "2017-07-17T20:40:51Z",
    "event": "labeled",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "label": "https://github.com/sagemath/sage/labels/needs%20review",
    "label_color": "7fff00",
    "label_name": "needs review",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/23204#event-305373"
}
```



---

archive/issue_comments_350744.json:
```json
{
    "body": "<a id='comment:13'>Comment 13:</a>\nThe speed regression is minimal. About 235ns vs 220ns in some constructed cases. It should not have much of an effect since most elements overwrite `__nonzero__` anyway.",
    "created_at": "2017-07-17T20:40:51Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350744",
    "user": "https://github.com/saraedum"
}
```

<a id='comment:13'>Comment 13:</a>
The speed regression is minimal. About 235ns vs 220ns in some constructed cases. It should not have much of an effect since most elements overwrite `__nonzero__` anyway.



---

archive/issue_comments_350745.json:
```json
{
    "body": "Changed work issues from **check that __nonzero__ does not cause a major speed regression** to none",
    "created_at": "2017-07-17T20:40:51Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350745",
    "user": "https://github.com/saraedum"
}
```

Changed work issues from **check that __nonzero__ does not cause a major speed regression** to none



---

archive/issue_events_305374.json:
```json
{
    "actor": "https://github.com/sagetrac-xander-faber",
    "created_at": "2017-07-19T11:37:09Z",
    "event": "unlabeled",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "label": "https://github.com/sagemath/sage/labels/needs%20review",
    "label_color": "7fff00",
    "label_name": "needs review",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/23204#event-305374"
}
```



---

archive/issue_events_305375.json:
```json
{
    "actor": "https://github.com/sagetrac-xander-faber",
    "created_at": "2017-07-19T11:37:09Z",
    "event": "labeled",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "label": "https://github.com/sagemath/sage/labels/needs%20work",
    "label_color": "ffff00",
    "label_name": "needs work",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/23204#event-305375"
}
```



---

archive/issue_comments_350746.json:
```json
{
    "body": "<a id='comment:16'>Comment 16:</a>\nDoctests are failing in structure/element.pyx, rings/homset.py, combinat/combinat.py, and rings/fraction_field.FpT.pyx. This has to do with the `Element.__nonzero__()` method producing behavior that disagrees with its docstring. I am addressing this currently.",
    "created_at": "2017-07-19T11:37:09Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350746",
    "user": "https://github.com/sagetrac-xander-faber"
}
```

<a id='comment:16'>Comment 16:</a>
Doctests are failing in structure/element.pyx, rings/homset.py, combinat/combinat.py, and rings/fraction_field.FpT.pyx. This has to do with the `Element.__nonzero__()` method producing behavior that disagrees with its docstring. I am addressing this currently.



---

archive/issue_comments_350747.json:
```json
{
    "body": "<a id='comment:17'>Comment 17:</a>\nA strong -1 on calling the category (first): this is relatively expensive, not likely able to be implemented by the `MorphismMethods` of a category if you have to do formal compositions, and is meant to be overwritten. Really, if you want composite maps, then you should overwrite the `_composition_` hook.\n\nNo bare exceptions.\n\nI don't understand why this needs to be a more generic class:\n\n```diff\ndiff --git a/src/sage/rings/morphism.pxd b/src/sage/rings/morphism.pxd\nindex a751a23..26fbeed 100644\n--- a/src/sage/rings/morphism.pxd\n+++ b/src/sage/rings/morphism.pxd\n@@ -15,10 +15,7 @@ cdef class RingMap_lift(RingMap):\n     cdef CategoryObject S\n \n cdef class RingHomomorphism(RingMap):\n-    cdef RingMap _lift\n-\n-cdef class RingHomomorphism_coercion(RingHomomorphism):\n-    pass\n+    cdef Morphism _lift\n \n cdef class RingHomomorphism_im_gens(RingHomomorphism):\n     cdef __im_gens\n```\n\nThis is almost certainly a speed regression:\n\n```diff\ndiff --git a/src/sage/rings/morphism.pyx b/src/sage/rings/morphism.pyx\nindex 978fe44..054aea3 100644\n--- a/src/sage/rings/morphism.pyx\n+++ b/src/sage/rings/morphism.pyx\n@@ -373,7 +375,10 @@ def is_RingHomomorphism(phi):\n         sage: sage.rings.morphism.is_RingHomomorphism(2/3)\n         False\n     \"\"\"\n-    return isinstance(phi, RingHomomorphism)\n+    # We use the category framework to determine whether something is a ring homomorphism.\n+    from sage.categories.map import Map\n+    from sage.categories.all import Rings\n+    return isinstance(phi, Map) and phi.category_for().is_subcategory(Rings())\n \n cdef class RingMap(Morphism):\n     \"\"\"\n```\nThis may not be so important as it may not really be used in time-critical code, but it shows up all over the schemes stuff (which should be replaced with a direct `isinstance` call as they know the morphisms being construction are a subclass of `RingHomomorphism`). I understand why this is natural, but I don't think this should be done (here) as it is a true behavior change, the `is_*` functions use are discouraged for trivial cases, and the the above.\n\nI am a little wary of removing `RingHomomorphism.__nonzero__` as that specialized code does do a check that could be faster and might be a little better behaved because no equality checking is involved. I just want to make sure you really checked this doesn't introduce a speed regression and works well for inexact rings (which may not be fully doctested).\n\nWhy is this change done:\n\n```diff\ndiff --git a/src/sage/schemes/generic/morphism.py b/src/sage/schemes/generic/morphism.py\nindex 777e3ba..8b7aa8c 100644\n--- a/src/sage/schemes/generic/morphism.py\n+++ b/src/sage/schemes/generic/morphism.py\n@@ -1442,7 +1441,7 @@ class SchemeMorphism_polynomial(SchemeMorphism):\n             S = self.codomain().change_ring(R)\n             H = Hom(T,S)\n \n-        if isinstance(R, Morphism):\n+        if isinstance(R, Map):\n             if R.domain() == self.base_ring():\n                 R = self.domain().ambient_space().coordinate_ring().hom(R, T.ambient_space().coordinate_ring())\n         G = []\n```\nIt does not immediately seem to be related to this ticket.\n\nYou need to properly handle old pickles with the old class.\n\nError messages are not really sentences (according to Python's conventions):\n\n```diff\n-raise TypeError(\"Natural coercion morphism from %s to %s not defined.\"%(self.domain(), self.codomain()))\n+raise TypeError(\"natural coercion morphism from %s to %s not defined\"%(self.domain(), self.codomain()))\n```\n\nThis change really makes me worry:\n\n```\n@@ -737,11 +715,18 @@ cdef class RingHomomorphism(RingMap):\n             sage: f = R.hom([a+b,a-b])\n             sage: g = S.hom(Frac(S))\n             sage: g*f # indirect doctest\n-            Ring morphism:\n+            Composite map:\n               From: Multivariate Polynomial Ring in x, y over Rational Field\n               To:   Fraction Field of Multivariate Polynomial Ring in a, b over Rational Field\n-              Defn: x |--> a + b\n-                    y |--> a - b\n+              Defn:   Ring morphism:\n+                      From: Multivariate Polynomial Ring in x, y over Rational Field\n+                      To:   Multivariate Polynomial Ring in a, b over Rational Field\n+                      Defn: x |--> a + b\n+                            y |--> a - b\n+                    then\n+                      Conversion via FractionFieldElement map:\n+                      From: Multivariate Polynomial Ring in a, b over Rational Field\n+                      To:   Fraction Field of Multivariate Polynomial Ring in a, b over Rational Field\n \n         When ``right`` is defined by the images of generators, the\n         result has the type of a homomorphism between its domain and\n```\nIn particular, it seems like you will now get coercion morphisms that are compositions, which seems to be different behavior than before. This could have subtle effects on code and likely have a performance regression.\n\nI agree with the purpose of this ticket and would like to see the ring morphism code simplified. However, I care a lot about the speed in here because polynomials are a core part of the Sage functionality.",
    "created_at": "2017-07-19T14:25:14Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350747",
    "user": "https://github.com/tscrim"
}
```

<a id='comment:17'>Comment 17:</a>
A strong -1 on calling the category (first): this is relatively expensive, not likely able to be implemented by the `MorphismMethods` of a category if you have to do formal compositions, and is meant to be overwritten. Really, if you want composite maps, then you should overwrite the `_composition_` hook.

No bare exceptions.

I don't understand why this needs to be a more generic class:

```diff
diff --git a/src/sage/rings/morphism.pxd b/src/sage/rings/morphism.pxd
index a751a23..26fbeed 100644
--- a/src/sage/rings/morphism.pxd
+++ b/src/sage/rings/morphism.pxd
@@ -15,10 +15,7 @@ cdef class RingMap_lift(RingMap):
     cdef CategoryObject S
 
 cdef class RingHomomorphism(RingMap):
-    cdef RingMap _lift
-
-cdef class RingHomomorphism_coercion(RingHomomorphism):
-    pass
+    cdef Morphism _lift
 
 cdef class RingHomomorphism_im_gens(RingHomomorphism):
     cdef __im_gens
```

This is almost certainly a speed regression:

```diff
diff --git a/src/sage/rings/morphism.pyx b/src/sage/rings/morphism.pyx
index 978fe44..054aea3 100644
--- a/src/sage/rings/morphism.pyx
+++ b/src/sage/rings/morphism.pyx
@@ -373,7 +375,10 @@ def is_RingHomomorphism(phi):
         sage: sage.rings.morphism.is_RingHomomorphism(2/3)
         False
     """
-    return isinstance(phi, RingHomomorphism)
+    # We use the category framework to determine whether something is a ring homomorphism.
+    from sage.categories.map import Map
+    from sage.categories.all import Rings
+    return isinstance(phi, Map) and phi.category_for().is_subcategory(Rings())
 
 cdef class RingMap(Morphism):
     """
```
This may not be so important as it may not really be used in time-critical code, but it shows up all over the schemes stuff (which should be replaced with a direct `isinstance` call as they know the morphisms being construction are a subclass of `RingHomomorphism`). I understand why this is natural, but I don't think this should be done (here) as it is a true behavior change, the `is_*` functions use are discouraged for trivial cases, and the the above.

I am a little wary of removing `RingHomomorphism.__nonzero__` as that specialized code does do a check that could be faster and might be a little better behaved because no equality checking is involved. I just want to make sure you really checked this doesn't introduce a speed regression and works well for inexact rings (which may not be fully doctested).

Why is this change done:

```diff
diff --git a/src/sage/schemes/generic/morphism.py b/src/sage/schemes/generic/morphism.py
index 777e3ba..8b7aa8c 100644
--- a/src/sage/schemes/generic/morphism.py
+++ b/src/sage/schemes/generic/morphism.py
@@ -1442,7 +1441,7 @@ class SchemeMorphism_polynomial(SchemeMorphism):
             S = self.codomain().change_ring(R)
             H = Hom(T,S)
 
-        if isinstance(R, Morphism):
+        if isinstance(R, Map):
             if R.domain() == self.base_ring():
                 R = self.domain().ambient_space().coordinate_ring().hom(R, T.ambient_space().coordinate_ring())
         G = []
```
It does not immediately seem to be related to this ticket.

You need to properly handle old pickles with the old class.

Error messages are not really sentences (according to Python's conventions):

```diff
-raise TypeError("Natural coercion morphism from %s to %s not defined."%(self.domain(), self.codomain()))
+raise TypeError("natural coercion morphism from %s to %s not defined"%(self.domain(), self.codomain()))
```

This change really makes me worry:

```
@@ -737,11 +715,18 @@ cdef class RingHomomorphism(RingMap):
             sage: f = R.hom([a+b,a-b])
             sage: g = S.hom(Frac(S))
             sage: g*f # indirect doctest
-            Ring morphism:
+            Composite map:
               From: Multivariate Polynomial Ring in x, y over Rational Field
               To:   Fraction Field of Multivariate Polynomial Ring in a, b over Rational Field
-              Defn: x |--> a + b
-                    y |--> a - b
+              Defn:   Ring morphism:
+                      From: Multivariate Polynomial Ring in x, y over Rational Field
+                      To:   Multivariate Polynomial Ring in a, b over Rational Field
+                      Defn: x |--> a + b
+                            y |--> a - b
+                    then
+                      Conversion via FractionFieldElement map:
+                      From: Multivariate Polynomial Ring in a, b over Rational Field
+                      To:   Fraction Field of Multivariate Polynomial Ring in a, b over Rational Field
 
         When ``right`` is defined by the images of generators, the
         result has the type of a homomorphism between its domain and
```
In particular, it seems like you will now get coercion morphisms that are compositions, which seems to be different behavior than before. This could have subtle effects on code and likely have a performance regression.

I agree with the purpose of this ticket and would like to see the ring morphism code simplified. However, I care a lot about the speed in here because polynomials are a core part of the Sage functionality.



---

archive/issue_comments_350748.json:
```json
{
    "body": "Changed branch from **[u/saraedum/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/saraedum/remove_ringhomomorphism_coercion)** to **[u/xander.faber/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/xander.faber/remove_ringhomomorphism_coercion)**",
    "created_at": "2017-07-19T16:40:49Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350748",
    "user": "https://github.com/sagetrac-xander-faber"
}
```

Changed branch from **[u/saraedum/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/saraedum/remove_ringhomomorphism_coercion)** to **[u/xander.faber/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/xander.faber/remove_ringhomomorphism_coercion)**



---

archive/issue_comments_350749.json:
```json
{
    "body": "Changed branch from **[u/xander.faber/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/xander.faber/remove_ringhomomorphism_coercion)** to **[u/saraedum/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/saraedum/remove_ringhomomorphism_coercion)**",
    "created_at": "2017-07-19T16:47:35Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350749",
    "user": "https://github.com/saraedum"
}
```

Changed branch from **[u/xander.faber/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/xander.faber/remove_ringhomomorphism_coercion)** to **[u/saraedum/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/saraedum/remove_ringhomomorphism_coercion)**



---

archive/issue_comments_350750.json:
```json
{
    "body": "Author: **David Roe, Julian R\u00fcth**",
    "created_at": "2017-07-19T17:06:06Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350750",
    "user": "https://github.com/saraedum"
}
```

Author: **David Roe, Julian Rüth**



---

archive/issue_comments_350751.json:
```json
{
    "body": "Changed commit from **[a42ebcf](https://github.com/sagemath/sagetrac-mirror/commit/a42ebcf334ac70c16dfdffe052f6968fa643f858)** to **[6ddff36](https://github.com/sagemath/sagetrac-mirror/commit/6ddff368b759e4c627053921c66d44413390df6f)**",
    "created_at": "2017-07-19T17:06:06Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350751",
    "user": "https://github.com/saraedum"
}
```

Changed commit from **[a42ebcf](https://github.com/sagemath/sagetrac-mirror/commit/a42ebcf334ac70c16dfdffe052f6968fa643f858)** to **[6ddff36](https://github.com/sagemath/sagetrac-mirror/commit/6ddff368b759e4c627053921c66d44413390df6f)**



---

archive/issue_comments_350752.json:
```json
{
    "body": "Reviewer: **Travis Scrimshaw**",
    "created_at": "2017-07-19T17:06:06Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350752",
    "user": "https://github.com/saraedum"
}
```

Reviewer: **Travis Scrimshaw**



---

archive/issue_comments_350753.json:
```json
{
    "body": "<a id='comment:20'>Comment 20:</a>\nDavid Roe and I looked at your comments.\n\nReplying to [@tscrim](#comment%3A17):\n> A strong -1 on calling the category (first): this is relatively expensive, not likely able to be implemented by the `MorphismMethods` of a category if you have to do formal compositions, and is meant to be overwritten. Really, if you want composite maps, then you should overwrite the `_composition_` hook.\n\nWe don't understand what you are trying to say here. Could you rephrase your concern please.\n\n> No bare exceptions.\n\nWhat do you mean?\n\n> I don't understand why this needs to be a more generic class:\n> \n> ```diff\n> diff --git a/src/sage/rings/morphism.pxd b/src/sage/rings/morphism.pxd\n> index a751a23..26fbeed 100644\n> --- a/src/sage/rings/morphism.pxd\n> +++ b/src/sage/rings/morphism.pxd\n> @@ -15,10 +15,7 @@ cdef class RingMap_lift(RingMap):\n>      cdef CategoryObject S\n>  \n>  cdef class RingHomomorphism(RingMap):\n> -    cdef RingMap _lift\n> -\n> -cdef class RingHomomorphism_coercion(RingHomomorphism):\n> -    pass\n> +    cdef Morphism _lift\n>  \n>  cdef class RingHomomorphism_im_gens(RingHomomorphism):\n>      cdef __im_gens\n> ```\n\nBecause some lift map is now not inheriting from `RingMap` anymore.\n\n> This is almost certainly a speed regression:\n> \n> ```diff\n> diff --git a/src/sage/rings/morphism.pyx b/src/sage/rings/morphism.pyx\n> index 978fe44..054aea3 100644\n> --- a/src/sage/rings/morphism.pyx\n> +++ b/src/sage/rings/morphism.pyx\n> @@ -373,7 +375,10 @@ def is_RingHomomorphism(phi):\n>          sage: sage.rings.morphism.is_RingHomomorphism(2/3)\n>          False\n>      \"\"\"\n> -    return isinstance(phi, RingHomomorphism)\n> +    # We use the category framework to determine whether something is a ring homomorphism.\n> +    from sage.categories.map import Map\n> +    from sage.categories.all import Rings\n> +    return isinstance(phi, Map) and phi.category_for().is_subcategory(Rings())\n>  \n>  cdef class RingMap(Morphism):\n>      \"\"\"\n> ```\n> This may not be so important as it may not really be used in time-critical code, but it shows up all over the schemes stuff (which should be replaced with a direct `isinstance` call as they know the morphisms being construction are a subclass of `RingHomomorphism`). I understand why this is natural, but I don't think this should be done (here) as it is a true behavior change, the `is_*` functions use are discouraged for trivial cases, and the the above.\n\nOk. Let us try to take the `is_RingHomomorphism` out completely.\n\n> I am a little wary of removing `RingHomomorphism.__nonzero__` as that specialized code does do a check that could be faster and might be a little better behaved because no equality checking is involved. I just want to make sure you really checked this doesn't introduce a speed regression and works well for inexact rings (which may not be fully doctested).\n\nWe think that you will never get to the `==` check in the new implementation in the case of ring homomorphism (unless the codomain is trivial). We checked a few cases and there was no speed regression.\n\n> Why is this change done:\n> \n> ```diff\n> diff --git a/src/sage/schemes/generic/morphism.py b/src/sage/schemes/generic/morphism.py\n> index 777e3ba..8b7aa8c 100644\n> --- a/src/sage/schemes/generic/morphism.py\n> +++ b/src/sage/schemes/generic/morphism.py\n> @@ -1442,7 +1441,7 @@ class SchemeMorphism_polynomial(SchemeMorphism):\n>              S = self.codomain().change_ring(R)\n>              H = Hom(T,S)\n>  \n> -        if isinstance(R, Morphism):\n> +        if isinstance(R, Map):\n>              if R.domain() == self.base_ring():\n>                  R = self.domain().ambient_space().coordinate_ring().hom(R, T.ambient_space().coordinate_ring())\n>          G = []\n> ```\n> It does not immediately seem to be related to this ticket.\n\nThe reason is that default convert maps do not inherit from Morphism. It is certainly related to this ticket. The type of a map changed, so thi isinstance check has to be more liberal.\n\n> You need to properly handle old pickles with the old class.\n\nSure. The patchbot already complains about this.\n\n> Error messages are not really sentences (according to Python's conventions):\n> \n> ```diff\n> -raise TypeError(\"Natural coercion morphism from %s to %s not defined.\"%(self.domain(), self.codomain()))\n> +raise TypeError(\"natural coercion morphism from %s to %s not defined\"%(self.domain(), self.codomain()))\n> ```\n\nWe do not understand what is the change that you are proposing.\n\n> This change really makes me worry:\n> \n> ```\n> @@ -737,11 +715,18 @@ cdef class RingHomomorphism(RingMap):\n>              sage: f = R.hom([a+b,a-b])\n>              sage: g = S.hom(Frac(S))\n>              sage: g*f # indirect doctest\n> -            Ring morphism:\n> +            Composite map:\n>                From: Multivariate Polynomial Ring in x, y over Rational Field\n>                To:   Fraction Field of Multivariate Polynomial Ring in a, b over Rational Field\n> -              Defn: x |--> a + b\n> -                    y |--> a - b\n> +              Defn:   Ring morphism:\n> +                      From: Multivariate Polynomial Ring in x, y over Rational Field\n> +                      To:   Multivariate Polynomial Ring in a, b over Rational Field\n> +                      Defn: x |--> a + b\n> +                            y |--> a - b\n> +                    then\n> +                      Conversion via FractionFieldElement map:\n> +                      From: Multivariate Polynomial Ring in a, b over Rational Field\n> +                      To:   Fraction Field of Multivariate Polynomial Ring in a, b over Rational Field\n>  \n>          When ``right`` is defined by the images of generators, the\n>          result has the type of a homomorphism between its domain and\n> ```\n> In particular, it seems like you will now get coercion morphisms that are compositions, which seems to be different behavior than before. This could have subtle effects on code and likely have a performance regression.\n\nDavid is going to have another look. There should not be a speed regression. Actually the opposite should be the case. Before the composite map was wrapped in another map, now it is directly accessible. We are going to double check that this is true.\n\n> I agree with the purpose of this ticket and would like to see the ring morphism code simplified. However, I care a lot about the speed in here because polynomials are a core part of the Sage functionality.\n\nSure. Let's be careful about not causing any regressions here.\n  \n---\nNew commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/a810e3c5c88d676ef8df668cbea8340c3e9fb803\">a810e3c</a></td><td><code>Fixed Element.__nonzero__ and doctests</code></td></tr><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/96b2d872a5ba31b85a91e1257da22bc678b83163\">96b2d87</a></td><td><code>Fixed doctest for rings.homset.RingHomset_generic.zero</code></td></tr><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/e11de6965ab0ea856db42a583ca1319194fec5d9\">e11de69</a></td><td><code>Fixed doctests for FpT_Polyring_section and FpT_Fp_section</code></td></tr><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/83a83dd3309b9bc753b3f3393da796183dc6ea0d\">83a83dd</a></td><td><code>Fixed doctests for CombinatorialObject.__bool__</code></td></tr><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/6ddff368b759e4c627053921c66d44413390df6f\">6ddff36</a></td><td><code>Merge branch 'develop' into t/23204/remove_ringhomomorphism_coercion</code></td></tr></table>\n",
    "created_at": "2017-07-19T17:06:06Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350753",
    "user": "https://github.com/saraedum"
}
```

<a id='comment:20'>Comment 20:</a>
David Roe and I looked at your comments.

Replying to [@tscrim](#comment%3A17):
> A strong -1 on calling the category (first): this is relatively expensive, not likely able to be implemented by the `MorphismMethods` of a category if you have to do formal compositions, and is meant to be overwritten. Really, if you want composite maps, then you should overwrite the `_composition_` hook.

We don't understand what you are trying to say here. Could you rephrase your concern please.

> No bare exceptions.

What do you mean?

> I don't understand why this needs to be a more generic class:
> 
> ```diff
> diff --git a/src/sage/rings/morphism.pxd b/src/sage/rings/morphism.pxd
> index a751a23..26fbeed 100644
> --- a/src/sage/rings/morphism.pxd
> +++ b/src/sage/rings/morphism.pxd
> @@ -15,10 +15,7 @@ cdef class RingMap_lift(RingMap):
>      cdef CategoryObject S
>  
>  cdef class RingHomomorphism(RingMap):
> -    cdef RingMap _lift
> -
> -cdef class RingHomomorphism_coercion(RingHomomorphism):
> -    pass
> +    cdef Morphism _lift
>  
>  cdef class RingHomomorphism_im_gens(RingHomomorphism):
>      cdef __im_gens
> ```

Because some lift map is now not inheriting from `RingMap` anymore.

> This is almost certainly a speed regression:
> 
> ```diff
> diff --git a/src/sage/rings/morphism.pyx b/src/sage/rings/morphism.pyx
> index 978fe44..054aea3 100644
> --- a/src/sage/rings/morphism.pyx
> +++ b/src/sage/rings/morphism.pyx
> @@ -373,7 +375,10 @@ def is_RingHomomorphism(phi):
>          sage: sage.rings.morphism.is_RingHomomorphism(2/3)
>          False
>      """
> -    return isinstance(phi, RingHomomorphism)
> +    # We use the category framework to determine whether something is a ring homomorphism.
> +    from sage.categories.map import Map
> +    from sage.categories.all import Rings
> +    return isinstance(phi, Map) and phi.category_for().is_subcategory(Rings())
>  
>  cdef class RingMap(Morphism):
>      """
> ```
> This may not be so important as it may not really be used in time-critical code, but it shows up all over the schemes stuff (which should be replaced with a direct `isinstance` call as they know the morphisms being construction are a subclass of `RingHomomorphism`). I understand why this is natural, but I don't think this should be done (here) as it is a true behavior change, the `is_*` functions use are discouraged for trivial cases, and the the above.

Ok. Let us try to take the `is_RingHomomorphism` out completely.

> I am a little wary of removing `RingHomomorphism.__nonzero__` as that specialized code does do a check that could be faster and might be a little better behaved because no equality checking is involved. I just want to make sure you really checked this doesn't introduce a speed regression and works well for inexact rings (which may not be fully doctested).

We think that you will never get to the `==` check in the new implementation in the case of ring homomorphism (unless the codomain is trivial). We checked a few cases and there was no speed regression.

> Why is this change done:
> 
> ```diff
> diff --git a/src/sage/schemes/generic/morphism.py b/src/sage/schemes/generic/morphism.py
> index 777e3ba..8b7aa8c 100644
> --- a/src/sage/schemes/generic/morphism.py
> +++ b/src/sage/schemes/generic/morphism.py
> @@ -1442,7 +1441,7 @@ class SchemeMorphism_polynomial(SchemeMorphism):
>              S = self.codomain().change_ring(R)
>              H = Hom(T,S)
>  
> -        if isinstance(R, Morphism):
> +        if isinstance(R, Map):
>              if R.domain() == self.base_ring():
>                  R = self.domain().ambient_space().coordinate_ring().hom(R, T.ambient_space().coordinate_ring())
>          G = []
> ```
> It does not immediately seem to be related to this ticket.

The reason is that default convert maps do not inherit from Morphism. It is certainly related to this ticket. The type of a map changed, so thi isinstance check has to be more liberal.

> You need to properly handle old pickles with the old class.

Sure. The patchbot already complains about this.

> Error messages are not really sentences (according to Python's conventions):
> 
> ```diff
> -raise TypeError("Natural coercion morphism from %s to %s not defined."%(self.domain(), self.codomain()))
> +raise TypeError("natural coercion morphism from %s to %s not defined"%(self.domain(), self.codomain()))
> ```

We do not understand what is the change that you are proposing.

> This change really makes me worry:
> 
> ```
> @@ -737,11 +715,18 @@ cdef class RingHomomorphism(RingMap):
>              sage: f = R.hom([a+b,a-b])
>              sage: g = S.hom(Frac(S))
>              sage: g*f # indirect doctest
> -            Ring morphism:
> +            Composite map:
>                From: Multivariate Polynomial Ring in x, y over Rational Field
>                To:   Fraction Field of Multivariate Polynomial Ring in a, b over Rational Field
> -              Defn: x |--> a + b
> -                    y |--> a - b
> +              Defn:   Ring morphism:
> +                      From: Multivariate Polynomial Ring in x, y over Rational Field
> +                      To:   Multivariate Polynomial Ring in a, b over Rational Field
> +                      Defn: x |--> a + b
> +                            y |--> a - b
> +                    then
> +                      Conversion via FractionFieldElement map:
> +                      From: Multivariate Polynomial Ring in a, b over Rational Field
> +                      To:   Fraction Field of Multivariate Polynomial Ring in a, b over Rational Field
>  
>          When ``right`` is defined by the images of generators, the
>          result has the type of a homomorphism between its domain and
> ```
> In particular, it seems like you will now get coercion morphisms that are compositions, which seems to be different behavior than before. This could have subtle effects on code and likely have a performance regression.

David is going to have another look. There should not be a speed regression. Actually the opposite should be the case. Before the composite map was wrapped in another map, now it is directly accessible. We are going to double check that this is true.

> I agree with the purpose of this ticket and would like to see the ring morphism code simplified. However, I care a lot about the speed in here because polynomials are a core part of the Sage functionality.

Sure. Let's be careful about not causing any regressions here.
  
---
New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/a810e3c5c88d676ef8df668cbea8340c3e9fb803">a810e3c</a></td><td><code>Fixed Element.__nonzero__ and doctests</code></td></tr><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/96b2d872a5ba31b85a91e1257da22bc678b83163">96b2d87</a></td><td><code>Fixed doctest for rings.homset.RingHomset_generic.zero</code></td></tr><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/e11de6965ab0ea856db42a583ca1319194fec5d9">e11de69</a></td><td><code>Fixed doctests for FpT_Polyring_section and FpT_Fp_section</code></td></tr><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/83a83dd3309b9bc753b3f3393da796183dc6ea0d">83a83dd</a></td><td><code>Fixed doctests for CombinatorialObject.__bool__</code></td></tr><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/6ddff368b759e4c627053921c66d44413390df6f">6ddff36</a></td><td><code>Merge branch 'develop' into t/23204/remove_ringhomomorphism_coercion</code></td></tr></table>




---

archive/issue_comments_350754.json:
```json
{
    "body": "Work Issues: **drop is_RingHomomorphism, consume old pickles, check that composite maps do not cause a speed regression**",
    "created_at": "2017-07-19T17:06:06Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350754",
    "user": "https://github.com/saraedum"
}
```

Work Issues: **drop is_RingHomomorphism, consume old pickles, check that composite maps do not cause a speed regression**



---

archive/issue_comments_350755.json:
```json
{
    "body": "<a id='comment:21'>Comment 21:</a>\nReplying to [@saraedum](#comment%3A20):\n> David Roe and I looked at your comments.\n> \n> Replying to [@tscrim](#comment%3A17):\n> > A strong -1 on calling the category (first): this is relatively expensive, not likely able to be implemented by the `MorphismMethods` of a category if you have to do formal compositions, and is meant to be overwritten. Really, if you want composite maps, then you should overwrite the `_composition_` hook.\n\n> We don't understand what you are trying to say here. Could you rephrase your concern please.\n\nYou added two blocks for formal composite functions:\n\n```python\n        try:\n            # we try the category first\n            # as of 2017-06, the MRO of this class does not get patched to\n            # include the category's MorphismMethods (because it is a Cython\n            # class); therefore, we can not simply call \"super\" but need to\n            # invoke the category method explicitly\n            return self.getattr_from_category('is_injective')()\n        except (AttributeError, NotImplementedError):\n            pass\n```\nWhat I am saying is this is really (relatively) expensive for something that is almost certainly going to be implemented. If your category has morphisms such that you cannot effectively simplify a composition `f \\circ g` to a single map `h`, then I don't see how you would have enough (generic) information to check that the composition is injective/surjective. So I doubt this would ever get used.\n\nIn contrast, when a category does have enough information to simplify maps, it should implement the `_composition_` hook to return the correct composition map.\n\n> > No bare exceptions.\n\n> What do you mean?\n\nYou indirectly took care of it in a810e3c:\n\n```diff\n@@ -998,12 +998,16 @@ cdef class Element(SageObject):\n             False\n \n         \"\"\"\n+        if hasattr(self,'_parent'):\n+            P = self._parent\n+        elif hasattr(self,'parent'):\n+            P = self.parent()\n+        else:\n+            return True # By convention!\n         try:\n-            zero = self._parent.zero()\n-        except:\n-            return False\n-\n-        return self != zero\n+            return self != P.zero()\n+        except (AttributeError,ValueError):\n+            return True # By convention!\n \n     def is_zero(self):\n         \"\"\"\n```\nActually, I don't see how you could get anything other than the first case as `Element` has a `_parent` attribute this is checked within `Element`), much less into the second block because why would it have a meaningful `.parent()` method in that case? If there is such a class out there, then it is a bug IMO and should be fixed to initialize `Element`. So I would probably just do\n\n```python\ntry:\n    return self != self._parent.zero()\nexcept Exception:\n    return True # By convention\n```\nunless you want something that returns a `TypeError` or `NotImplementedError` to not be caught. For that I am a little more unsure.\n\n> > I don't understand why this needs to be a more generic class:\n> > [snip]\n\n> Because some lift map is now not inheriting from `RingMap` anymore.\n\nThat smells a little like you have encountered a bug and something really should be returning a `RingMap_lift`. Either that or we should also remove `RingMap_lift` with some more generic class for lifts (if that is what we decide to do, we can do that here or on a followup).\n\n> > I am a little wary of removing `RingHomomorphism.__nonzero__` as that specialized code does do a check that could be faster and might be a little better behaved because no equality checking is involved. I just want to make sure you really checked this doesn't introduce a speed regression and works well for inexact rings (which may not be fully doctested).\n\n> We think that you will never get to the `==` check in the new implementation in the case of ring homomorphism (unless the codomain is trivial). We checked a few cases and there was no speed regression.\n\nThat does not seem possible. If the ring homset has no zero, then is relatively expensive because of the throwing and catching of an error and is something done every time. If there is a zero map, then it should only affect the first time it is called, but then it has to do a comparison because of the default `Element.__nonzero__`. What were the cases you tested?\n\n> > Why is this change done:\n> > [snip]\n> > It does not immediately seem to be related to this ticket.\n\n> The reason is that default convert maps do not inherit from Morphism. It is certainly related to this ticket. The type of a map changed, so thi isinstance check has to be more liberal.\n\nI do not see why this needs to be reduced to `Map` but for `RingMap`, it can still be passed in a `Morphism`. If you're sure that this is okay, then I will take your word for it because I don't know that area of the codebase so well.\n\n> > Error messages are not really sentences (according to Python's conventions):\n> > \n> > ```diff\n> > -raise TypeError(\"Natural coercion morphism from %s to %s not defined.\"%(self.domain(), self.codomain()))\n> > +raise TypeError(\"natural coercion morphism from %s to %s not defined\"%(self.domain(), self.codomain()))\n> > ```\n\n> We do not understand what is the change that you are proposing.\n\nYou should use lower case for the initial letters and not have a period (full stop) at the end. Even if this differs with the rest of the file, we are trying to follow Python conventions.\n\n> > This change really makes me worry:\n> > [snip]\n> > In particular, it seems like you will now get coercion morphisms that are compositions, which seems to be different behavior than before. This could have subtle effects on code and likely have a performance regression.\n\n> David is going to have another look. There should not be a speed regression. Actually the opposite should be the case. Before the composite map was wrapped in another map, now it is directly accessible. We are going to double check that this is true.\n\nYou should be correct as no parent implements a custom `coerce`. However, this does mean we have to be more liberal with types (see above).\n\nCould you explain why you needed to change this doctest:\n\n```diff\n@@ -861,7 +808,7 @@ cdef class RingHomomorphism(RingMap):\n \n         This is not implemented in any generality yet::\n \n-            sage: f = ZZ.hom(ZZ)\n+            sage: f = ZZ.hom(Zp)\n             sage: f.inverse_image(ZZ.ideal(2))\n             Traceback (most recent call last):\n             ...\n```",
    "created_at": "2017-07-19T23:27:16Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350755",
    "user": "https://github.com/tscrim"
}
```

<a id='comment:21'>Comment 21:</a>
Replying to [@saraedum](#comment%3A20):
> David Roe and I looked at your comments.
> 
> Replying to [@tscrim](#comment%3A17):
> > A strong -1 on calling the category (first): this is relatively expensive, not likely able to be implemented by the `MorphismMethods` of a category if you have to do formal compositions, and is meant to be overwritten. Really, if you want composite maps, then you should overwrite the `_composition_` hook.

> We don't understand what you are trying to say here. Could you rephrase your concern please.

You added two blocks for formal composite functions:

```python
        try:
            # we try the category first
            # as of 2017-06, the MRO of this class does not get patched to
            # include the category's MorphismMethods (because it is a Cython
            # class); therefore, we can not simply call "super" but need to
            # invoke the category method explicitly
            return self.getattr_from_category('is_injective')()
        except (AttributeError, NotImplementedError):
            pass
```
What I am saying is this is really (relatively) expensive for something that is almost certainly going to be implemented. If your category has morphisms such that you cannot effectively simplify a composition `f \circ g` to a single map `h`, then I don't see how you would have enough (generic) information to check that the composition is injective/surjective. So I doubt this would ever get used.

In contrast, when a category does have enough information to simplify maps, it should implement the `_composition_` hook to return the correct composition map.

> > No bare exceptions.

> What do you mean?

You indirectly took care of it in a810e3c:

```diff
@@ -998,12 +998,16 @@ cdef class Element(SageObject):
             False
 
         """
+        if hasattr(self,'_parent'):
+            P = self._parent
+        elif hasattr(self,'parent'):
+            P = self.parent()
+        else:
+            return True # By convention!
         try:
-            zero = self._parent.zero()
-        except:
-            return False
-
-        return self != zero
+            return self != P.zero()
+        except (AttributeError,ValueError):
+            return True # By convention!
 
     def is_zero(self):
         """
```
Actually, I don't see how you could get anything other than the first case as `Element` has a `_parent` attribute this is checked within `Element`), much less into the second block because why would it have a meaningful `.parent()` method in that case? If there is such a class out there, then it is a bug IMO and should be fixed to initialize `Element`. So I would probably just do

```python
try:
    return self != self._parent.zero()
except Exception:
    return True # By convention
```
unless you want something that returns a `TypeError` or `NotImplementedError` to not be caught. For that I am a little more unsure.

> > I don't understand why this needs to be a more generic class:
> > [snip]

> Because some lift map is now not inheriting from `RingMap` anymore.

That smells a little like you have encountered a bug and something really should be returning a `RingMap_lift`. Either that or we should also remove `RingMap_lift` with some more generic class for lifts (if that is what we decide to do, we can do that here or on a followup).

> > I am a little wary of removing `RingHomomorphism.__nonzero__` as that specialized code does do a check that could be faster and might be a little better behaved because no equality checking is involved. I just want to make sure you really checked this doesn't introduce a speed regression and works well for inexact rings (which may not be fully doctested).

> We think that you will never get to the `==` check in the new implementation in the case of ring homomorphism (unless the codomain is trivial). We checked a few cases and there was no speed regression.

That does not seem possible. If the ring homset has no zero, then is relatively expensive because of the throwing and catching of an error and is something done every time. If there is a zero map, then it should only affect the first time it is called, but then it has to do a comparison because of the default `Element.__nonzero__`. What were the cases you tested?

> > Why is this change done:
> > [snip]
> > It does not immediately seem to be related to this ticket.

> The reason is that default convert maps do not inherit from Morphism. It is certainly related to this ticket. The type of a map changed, so thi isinstance check has to be more liberal.

I do not see why this needs to be reduced to `Map` but for `RingMap`, it can still be passed in a `Morphism`. If you're sure that this is okay, then I will take your word for it because I don't know that area of the codebase so well.

> > Error messages are not really sentences (according to Python's conventions):
> > 
> > ```diff
> > -raise TypeError("Natural coercion morphism from %s to %s not defined."%(self.domain(), self.codomain()))
> > +raise TypeError("natural coercion morphism from %s to %s not defined"%(self.domain(), self.codomain()))
> > ```

> We do not understand what is the change that you are proposing.

You should use lower case for the initial letters and not have a period (full stop) at the end. Even if this differs with the rest of the file, we are trying to follow Python conventions.

> > This change really makes me worry:
> > [snip]
> > In particular, it seems like you will now get coercion morphisms that are compositions, which seems to be different behavior than before. This could have subtle effects on code and likely have a performance regression.

> David is going to have another look. There should not be a speed regression. Actually the opposite should be the case. Before the composite map was wrapped in another map, now it is directly accessible. We are going to double check that this is true.

You should be correct as no parent implements a custom `coerce`. However, this does mean we have to be more liberal with types (see above).

Could you explain why you needed to change this doctest:

```diff
@@ -861,7 +808,7 @@ cdef class RingHomomorphism(RingMap):
 
         This is not implemented in any generality yet::
 
-            sage: f = ZZ.hom(ZZ)
+            sage: f = ZZ.hom(Zp)
             sage: f.inverse_image(ZZ.ideal(2))
             Traceback (most recent call last):
             ...
```



---

archive/issue_comments_350756.json:
```json
{
    "body": "<a id='comment:22'>Comment 22:</a>\nReplying to [@tscrim](#comment%3A21):\n> Replying to [@saraedum](#comment%3A20):\n> > David Roe and I looked at your comments.\n> > \n> > Replying to [@tscrim](#comment%3A17):\n> > > A strong -1 on calling the category (first): this is relatively expensive, not likely able to be implemented by the `MorphismMethods` of a category if you have to do formal compositions, and is meant to be overwritten. Really, if you want composite maps, then you should overwrite the `_composition_` hook.\n\n> > We don't understand what you are trying to say here. Could you rephrase your concern please.\n\n> \n> You added two blocks for formal composite functions:\n> \n> ```python\n>         try:\n>             # we try the category first\n>             # as of 2017-06, the MRO of this class does not get patched to\n>             # include the category's MorphismMethods (because it is a Cython\n>             # class); therefore, we can not simply call \"super\" but need to\n>             # invoke the category method explicitly\n>             return self.getattr_from_category('is_injective')()\n>         except (AttributeError, NotImplementedError):\n>             pass\n> ```\n> What I am saying is this is really (relatively) expensive for something that is almost certainly going to be implemented. If your category has morphisms such that you cannot effectively simplify a composition `f \\circ g` to a single map `h`, then I don't see how you would have enough (generic) information to check that the composition is injective/surjective. So I doubt this would ever get used.\n\nI do not understand how this relates to `_composition_`. The point here is simply that in the category of Rings, a morphism that has a field as its domain, knows that it is injective. However, we can not so easily turn the composition into one morphism.\n\n> > > No bare exceptions.\n\n> > What do you mean?\n\n> You indirectly took care of it in a810e3c:\n\nOk.\n\n> Actually, I don't see how you could get anything other than the first case as `Element` has a `_parent` attribute this is checked within `Element`), much less into the second block because why would it have a meaningful `.parent()` method in that case? If there is such a class out there, then it is a bug IMO and should be fixed to initialize `Element`. So I would probably just do\n> \n> ```python\n> try:\n>     return self != self._parent.zero()\n> except Exception:\n>     return True # By convention\n> ```\n> unless you want something that returns a `TypeError` or `NotImplementedError` to not be caught. For that I am a little more unsure.\n\nI agree. What were the elements without `_parent`?\n\n> > > I don't understand why this needs to be a more generic class:\n> > > [snip]\n\n> > Because some lift map is now not inheriting from `RingMap` anymore.\n\n> That smells a little like you have encountered a bug and something really should be returning a `RingMap_lift`. Either that or we should also remove `RingMap_lift` with some more generic class for lifts (if that is what we decide to do, we can do that here or on a followup).\n\nYes, we should remove `RingMap_lift`. I don't remember the details but changing this type was not a hack to get things to compile.\n\n> > > I am a little wary of removing `RingHomomorphism.__nonzero__` as that specialized code does do a check that could be faster and might be a little better behaved because no equality checking is involved. I just want to make sure you really checked this doesn't introduce a speed regression and works well for inexact rings (which may not be fully doctested).\n\n> > We think that you will never get to the `==` check in the new implementation in the case of ring homomorphism (unless the codomain is trivial). We checked a few cases and there was no speed regression.\n\n> That does not seem possible. If the ring homset has no zero, then is relatively expensive because of the throwing and catching of an error and is something done every time. If there is a zero map, then it should only affect the first time it is called, but then it has to do a comparison because of the default `Element.__nonzero__`. What were the cases you tested?\n\nSorry, I had misread your initial comment. There is probably a speed regression but when would this possibly be relevant (i.e., when do you create lots of rings and check that morphisms between them are nonzero? without knowing that you are talking about rings.)\n\n```\nsage: f = ZZ.hom(Zp(3))\nsage: %timeit bool(f)\n1000000 loops, best of 3: 212 ns per loop # before\n10000 loops, best of 3: 25.5 \u00b5s per loop # after\n```\n\n> > > Why is this change done:\n> > > [snip]\n> > > It does not immediately seem to be related to this ticket.\n\n> > The reason is that default convert maps do not inherit from Morphism. It is certainly related to this ticket. The type of a map changed, so thi isinstance check has to be more liberal.\n\n> I do not see why this needs to be reduced to `Map` but for `RingMap`, it can still be passed in a `Morphism`. If you're sure that this is okay, then I will take your word for it because I don't know that area of the codebase so well.\n\nOk.\n\n> > > Error messages are not really sentences (according to Python's conventions):\n> > > \n> > > ```diff\n> > > -raise TypeError(\"Natural coercion morphism from %s to %s not defined.\"%(self.domain(), self.codomain()))\n> > > +raise TypeError(\"natural coercion morphism from %s to %s not defined\"%(self.domain(), self.codomain()))\n> > > ```\n\nFixed.\n\n> > > This change really makes me worry:\n> > > [snip]\n> > > In particular, it seems like you will now get coercion morphisms that are compositions, which seems to be different behavior than before. This could have subtle effects on code and likely have a performance regression.\n\n> > David is going to have another look. There should not be a speed regression. Actually the opposite should be the case. Before the composite map was wrapped in another map, now it is directly accessible. We are going to double check that this is true.\n\n> You should be correct as no parent implements a custom `coerce`. However, this does mean we have to be more liberal with types (see above).\n\nYes.\n\n> Could you explain why you needed to change this doctest:\n> \n> ```diff\n> @@ -861,7 +808,7 @@ cdef class RingHomomorphism(RingMap):\n>  \n>          This is not implemented in any generality yet::\n>  \n> -            sage: f = ZZ.hom(ZZ)\n> +            sage: f = ZZ.hom(Zp)\n>              sage: f.inverse_image(ZZ.ideal(2))\n>              Traceback (most recent call last):\n>              ...\n> ```\n\n`ZZ.hom(ZZ)` is an `IdentityMorphism` now which does not have an `inverse_image()` method. We went from `NotImplementedError` to `AttributeError` it seems.",
    "created_at": "2017-07-20T00:25:44Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350756",
    "user": "https://github.com/saraedum"
}
```

<a id='comment:22'>Comment 22:</a>
Replying to [@tscrim](#comment%3A21):
> Replying to [@saraedum](#comment%3A20):
> > David Roe and I looked at your comments.
> > 
> > Replying to [@tscrim](#comment%3A17):
> > > A strong -1 on calling the category (first): this is relatively expensive, not likely able to be implemented by the `MorphismMethods` of a category if you have to do formal compositions, and is meant to be overwritten. Really, if you want composite maps, then you should overwrite the `_composition_` hook.

> > We don't understand what you are trying to say here. Could you rephrase your concern please.

> 
> You added two blocks for formal composite functions:
> 
> ```python
>         try:
>             # we try the category first
>             # as of 2017-06, the MRO of this class does not get patched to
>             # include the category's MorphismMethods (because it is a Cython
>             # class); therefore, we can not simply call "super" but need to
>             # invoke the category method explicitly
>             return self.getattr_from_category('is_injective')()
>         except (AttributeError, NotImplementedError):
>             pass
> ```
> What I am saying is this is really (relatively) expensive for something that is almost certainly going to be implemented. If your category has morphisms such that you cannot effectively simplify a composition `f \circ g` to a single map `h`, then I don't see how you would have enough (generic) information to check that the composition is injective/surjective. So I doubt this would ever get used.

I do not understand how this relates to `_composition_`. The point here is simply that in the category of Rings, a morphism that has a field as its domain, knows that it is injective. However, we can not so easily turn the composition into one morphism.

> > > No bare exceptions.

> > What do you mean?

> You indirectly took care of it in a810e3c:

Ok.

> Actually, I don't see how you could get anything other than the first case as `Element` has a `_parent` attribute this is checked within `Element`), much less into the second block because why would it have a meaningful `.parent()` method in that case? If there is such a class out there, then it is a bug IMO and should be fixed to initialize `Element`. So I would probably just do
> 
> ```python
> try:
>     return self != self._parent.zero()
> except Exception:
>     return True # By convention
> ```
> unless you want something that returns a `TypeError` or `NotImplementedError` to not be caught. For that I am a little more unsure.

I agree. What were the elements without `_parent`?

> > > I don't understand why this needs to be a more generic class:
> > > [snip]

> > Because some lift map is now not inheriting from `RingMap` anymore.

> That smells a little like you have encountered a bug and something really should be returning a `RingMap_lift`. Either that or we should also remove `RingMap_lift` with some more generic class for lifts (if that is what we decide to do, we can do that here or on a followup).

Yes, we should remove `RingMap_lift`. I don't remember the details but changing this type was not a hack to get things to compile.

> > > I am a little wary of removing `RingHomomorphism.__nonzero__` as that specialized code does do a check that could be faster and might be a little better behaved because no equality checking is involved. I just want to make sure you really checked this doesn't introduce a speed regression and works well for inexact rings (which may not be fully doctested).

> > We think that you will never get to the `==` check in the new implementation in the case of ring homomorphism (unless the codomain is trivial). We checked a few cases and there was no speed regression.

> That does not seem possible. If the ring homset has no zero, then is relatively expensive because of the throwing and catching of an error and is something done every time. If there is a zero map, then it should only affect the first time it is called, but then it has to do a comparison because of the default `Element.__nonzero__`. What were the cases you tested?

Sorry, I had misread your initial comment. There is probably a speed regression but when would this possibly be relevant (i.e., when do you create lots of rings and check that morphisms between them are nonzero? without knowing that you are talking about rings.)

```
sage: f = ZZ.hom(Zp(3))
sage: %timeit bool(f)
1000000 loops, best of 3: 212 ns per loop # before
10000 loops, best of 3: 25.5 µs per loop # after
```

> > > Why is this change done:
> > > [snip]
> > > It does not immediately seem to be related to this ticket.

> > The reason is that default convert maps do not inherit from Morphism. It is certainly related to this ticket. The type of a map changed, so thi isinstance check has to be more liberal.

> I do not see why this needs to be reduced to `Map` but for `RingMap`, it can still be passed in a `Morphism`. If you're sure that this is okay, then I will take your word for it because I don't know that area of the codebase so well.

Ok.

> > > Error messages are not really sentences (according to Python's conventions):
> > > 
> > > ```diff
> > > -raise TypeError("Natural coercion morphism from %s to %s not defined."%(self.domain(), self.codomain()))
> > > +raise TypeError("natural coercion morphism from %s to %s not defined"%(self.domain(), self.codomain()))
> > > ```

Fixed.

> > > This change really makes me worry:
> > > [snip]
> > > In particular, it seems like you will now get coercion morphisms that are compositions, which seems to be different behavior than before. This could have subtle effects on code and likely have a performance regression.

> > David is going to have another look. There should not be a speed regression. Actually the opposite should be the case. Before the composite map was wrapped in another map, now it is directly accessible. We are going to double check that this is true.

> You should be correct as no parent implements a custom `coerce`. However, this does mean we have to be more liberal with types (see above).

Yes.

> Could you explain why you needed to change this doctest:
> 
> ```diff
> @@ -861,7 +808,7 @@ cdef class RingHomomorphism(RingMap):
>  
>          This is not implemented in any generality yet::
>  
> -            sage: f = ZZ.hom(ZZ)
> +            sage: f = ZZ.hom(Zp)
>              sage: f.inverse_image(ZZ.ideal(2))
>              Traceback (most recent call last):
>              ...
> ```

`ZZ.hom(ZZ)` is an `IdentityMorphism` now which does not have an `inverse_image()` method. We went from `NotImplementedError` to `AttributeError` it seems.



---

archive/issue_comments_350757.json:
```json
{
    "body": "Changed commit from **[6ddff36](https://github.com/sagemath/sagetrac-mirror/commit/6ddff368b759e4c627053921c66d44413390df6f)** to **[29bad40](https://github.com/sagemath/sagetrac-mirror/commit/29bad40adeff929af5ec0697f6f1a65949bf497f)**",
    "created_at": "2017-07-20T00:26:07Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350757",
    "user": "https://github.com/sagetrac-git"
}
```

Changed commit from **[6ddff36](https://github.com/sagemath/sagetrac-mirror/commit/6ddff368b759e4c627053921c66d44413390df6f)** to **[29bad40](https://github.com/sagemath/sagetrac-mirror/commit/29bad40adeff929af5ec0697f6f1a65949bf497f)**



---

archive/issue_comments_350758.json:
```json
{
    "body": "<a id='comment:23'>Comment 23:</a>\nBranch pushed to git repo; I updated commit sha1. New commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/cd600d8e8855f9d907e72c19a8e807c2cbb2eac0\">cd600d8</a></td><td><code>fix typo</code></td></tr><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/b2f5e24a85385045f1d6de7976602fdc2e6c303a\">b2f5e24</a></td><td><code>fix typo</code></td></tr><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/7eccce66097a684ad95a28eefe44ef6709445b01\">7eccce6</a></td><td><code>Merge branch 'develop' into t/23204/remove_ringhomomorphism_coercion</code></td></tr><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/24173a59333dd115107b7eeba7a57cf9edd42d54\">24173a5</a></td><td><code>fix wording</code></td></tr><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/29bad40adeff929af5ec0697f6f1a65949bf497f\">29bad40</a></td><td><code>Merge branch 'u/saraedum/remove_ringhomomorphism_coercion' of git://trac.sagemath.org/sage into t/23204/remove_ringhomomorphism_coercion</code></td></tr></table>\n",
    "created_at": "2017-07-20T00:26:07Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350758",
    "user": "https://github.com/sagetrac-git"
}
```

<a id='comment:23'>Comment 23:</a>
Branch pushed to git repo; I updated commit sha1. New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/cd600d8e8855f9d907e72c19a8e807c2cbb2eac0">cd600d8</a></td><td><code>fix typo</code></td></tr><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/b2f5e24a85385045f1d6de7976602fdc2e6c303a">b2f5e24</a></td><td><code>fix typo</code></td></tr><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/7eccce66097a684ad95a28eefe44ef6709445b01">7eccce6</a></td><td><code>Merge branch 'develop' into t/23204/remove_ringhomomorphism_coercion</code></td></tr><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/24173a59333dd115107b7eeba7a57cf9edd42d54">24173a5</a></td><td><code>fix wording</code></td></tr><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/29bad40adeff929af5ec0697f6f1a65949bf497f">29bad40</a></td><td><code>Merge branch 'u/saraedum/remove_ringhomomorphism_coercion' of git://trac.sagemath.org/sage into t/23204/remove_ringhomomorphism_coercion</code></td></tr></table>




---

archive/issue_comments_350759.json:
```json
{
    "body": "<a id='comment:24'>Comment 24:</a>\nReplying to [@saraedum](#comment%3A22):\n> Replying to [@tscrim](#comment%3A21):\n> > Replying to [@saraedum](#comment%3A20):\n> > > David Roe and I looked at your comments.\n> > > \n> > > Replying to [@tscrim](#comment%3A17):\n> > > > A strong -1 on calling the category (first): this is relatively expensive, not likely able to be implemented by the `MorphismMethods` of a category if you have to do formal compositions, and is meant to be overwritten. Really, if you want composite maps, then you should overwrite the `_composition_` hook.\n\n> > > We don't understand what you are trying to say here. Could you rephrase your concern please.\n\n> > \n> > You added two blocks for formal composite functions:\n> > [snip]\n> > What I am saying is this is really (relatively) expensive for something that is almost certainly going to be implemented. If your category has morphisms such that you cannot effectively simplify a composition `f \\circ g` to a single map `h`, then I don't see how you would have enough (generic) information to check that the composition is injective/surjective. So I doubt this would ever get used.\n\n> I do not understand how this relates to `_composition_`. The point here is simply that in the category of Rings, a morphism that has a field as its domain, knows that it is injective. However, we can not so easily turn the composition into one morphism.\n\nSo you're doing implementing that at the category level? I feel like for something like this you should have a generic field morphism class that knows outright that it is injective and implements a custom `_composition_` that returns the same class because it is completely determined by the image of `1`. So the composition would amount to chasing through each map what happens to `1`. If you are using some more specialized morphisms and need the generic composition, then let the generic code of the composition do its thing. I don't see the point of slowing everything else down for a special case that is more effectively dealt with by a concrete class.\n\n> I agree. What were the elements without `_parent`?\n\nMy point is that never happens. All elements must inherit from `Element` (or you are basically reimplementing `Element` with less features).\n\n> > > > I don't understand why this needs to be a more generic class:\n> > > > [snip]\n\n> > > Because some lift map is now not inheriting from `RingMap` anymore.\n\n> > That smells a little like you have encountered a bug and something really should be returning a `RingMap_lift`. Either that or we should also remove `RingMap_lift` with some more generic class for lifts (if that is what we decide to do, we can do that here or on a followup).\n\n> Yes, we should remove `RingMap_lift`. I don't remember the details but changing this type was not a hack to get things to compile.\n\nI would call it a temporary fix until we remove `RingMap_list`. However, will that be a followup or done here? There is good reason to do it here as we are already touching a lot of places and is similar in spirit, but it would be a definite expansion of scope. I leave it up to you.\n\n> > > > I am a little wary of removing `RingHomomorphism.__nonzero__` as that specialized code does do a check that could be faster and might be a little better behaved because no equality checking is involved. I just want to make sure you really checked this doesn't introduce a speed regression and works well for inexact rings (which may not be fully doctested).\n\n> > > We think that you will never get to the `==` check in the new implementation in the case of ring homomorphism (unless the codomain is trivial). We checked a few cases and there was no speed regression.\n\n> > That does not seem possible. If the ring homset has no zero, then is relatively expensive because of the throwing and catching of an error and is something done every time. If there is a zero map, then it should only affect the first time it is called, but then it has to do a comparison because of the default `Element.__nonzero__`. What were the cases you tested?\n\n> Sorry, I had misread your initial comment. There is probably a speed regression but when would this possibly be relevant (i.e., when do you create lots of rings and check that morphisms between them are nonzero? without knowing that you are talking about rings.)\n> \n> ```\n> sage: f = ZZ.hom(Zp(3))\n> sage: %timeit bool(f)\n> 1000000 loops, best of 3: 212 ns per loop # before\n> 10000 loops, best of 3: 25.5 \u00b5s per loop # after\n> ```\n\nI don't have a use case for it, but I do not see the point in deliberately introducing a regression by removing code that is (was?) working perfectly well. Someone might have a use case for it; I know people create lots of polynomial rings over finite fields as the field size varies, and they might be doing a check for a nonzero map in that code.\n\n> > Could you explain why you needed to change this doctest:\n> > \n> > ```diff\n> > @@ -861,7 +808,7 @@ cdef class RingHomomorphism(RingMap):\n> >  \n> >          This is not implemented in any generality yet::\n> >  \n> > -            sage: f = ZZ.hom(ZZ)\n> > +            sage: f = ZZ.hom(Zp)\n> >              sage: f.inverse_image(ZZ.ideal(2))\n> >              Traceback (most recent call last):\n> >              ...\n> > ```\n\n> `ZZ.hom(ZZ)` is an `IdentityMorphism` now which does not have an `inverse_image()` method. We went from `NotImplementedError` to `AttributeError` it seems.\n\nSo this really is a subtle backwards incompatible change. Hopefully nobody was just catching the `NotImplementedError`. I don't think there is any (reasonable) way to put a deprecation and/or get around that.\n\nThis test is also not valid because `Zp` is a class, not a parent.\n\nActually, that brings up another question, not just that you need to handle old pickles, but do we want to have a small deprecation class for `RingHomomorphism_coercion`? This is an implementation detail, but it is something publicly documented. So someone might have code that uses this class and that code would just break upon upgrade.",
    "created_at": "2017-07-20T01:00:43Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350759",
    "user": "https://github.com/tscrim"
}
```

<a id='comment:24'>Comment 24:</a>
Replying to [@saraedum](#comment%3A22):
> Replying to [@tscrim](#comment%3A21):
> > Replying to [@saraedum](#comment%3A20):
> > > David Roe and I looked at your comments.
> > > 
> > > Replying to [@tscrim](#comment%3A17):
> > > > A strong -1 on calling the category (first): this is relatively expensive, not likely able to be implemented by the `MorphismMethods` of a category if you have to do formal compositions, and is meant to be overwritten. Really, if you want composite maps, then you should overwrite the `_composition_` hook.

> > > We don't understand what you are trying to say here. Could you rephrase your concern please.

> > 
> > You added two blocks for formal composite functions:
> > [snip]
> > What I am saying is this is really (relatively) expensive for something that is almost certainly going to be implemented. If your category has morphisms such that you cannot effectively simplify a composition `f \circ g` to a single map `h`, then I don't see how you would have enough (generic) information to check that the composition is injective/surjective. So I doubt this would ever get used.

> I do not understand how this relates to `_composition_`. The point here is simply that in the category of Rings, a morphism that has a field as its domain, knows that it is injective. However, we can not so easily turn the composition into one morphism.

So you're doing implementing that at the category level? I feel like for something like this you should have a generic field morphism class that knows outright that it is injective and implements a custom `_composition_` that returns the same class because it is completely determined by the image of `1`. So the composition would amount to chasing through each map what happens to `1`. If you are using some more specialized morphisms and need the generic composition, then let the generic code of the composition do its thing. I don't see the point of slowing everything else down for a special case that is more effectively dealt with by a concrete class.

> I agree. What were the elements without `_parent`?

My point is that never happens. All elements must inherit from `Element` (or you are basically reimplementing `Element` with less features).

> > > > I don't understand why this needs to be a more generic class:
> > > > [snip]

> > > Because some lift map is now not inheriting from `RingMap` anymore.

> > That smells a little like you have encountered a bug and something really should be returning a `RingMap_lift`. Either that or we should also remove `RingMap_lift` with some more generic class for lifts (if that is what we decide to do, we can do that here or on a followup).

> Yes, we should remove `RingMap_lift`. I don't remember the details but changing this type was not a hack to get things to compile.

I would call it a temporary fix until we remove `RingMap_list`. However, will that be a followup or done here? There is good reason to do it here as we are already touching a lot of places and is similar in spirit, but it would be a definite expansion of scope. I leave it up to you.

> > > > I am a little wary of removing `RingHomomorphism.__nonzero__` as that specialized code does do a check that could be faster and might be a little better behaved because no equality checking is involved. I just want to make sure you really checked this doesn't introduce a speed regression and works well for inexact rings (which may not be fully doctested).

> > > We think that you will never get to the `==` check in the new implementation in the case of ring homomorphism (unless the codomain is trivial). We checked a few cases and there was no speed regression.

> > That does not seem possible. If the ring homset has no zero, then is relatively expensive because of the throwing and catching of an error and is something done every time. If there is a zero map, then it should only affect the first time it is called, but then it has to do a comparison because of the default `Element.__nonzero__`. What were the cases you tested?

> Sorry, I had misread your initial comment. There is probably a speed regression but when would this possibly be relevant (i.e., when do you create lots of rings and check that morphisms between them are nonzero? without knowing that you are talking about rings.)
> 
> ```
> sage: f = ZZ.hom(Zp(3))
> sage: %timeit bool(f)
> 1000000 loops, best of 3: 212 ns per loop # before
> 10000 loops, best of 3: 25.5 µs per loop # after
> ```

I don't have a use case for it, but I do not see the point in deliberately introducing a regression by removing code that is (was?) working perfectly well. Someone might have a use case for it; I know people create lots of polynomial rings over finite fields as the field size varies, and they might be doing a check for a nonzero map in that code.

> > Could you explain why you needed to change this doctest:
> > 
> > ```diff
> > @@ -861,7 +808,7 @@ cdef class RingHomomorphism(RingMap):
> >  
> >          This is not implemented in any generality yet::
> >  
> > -            sage: f = ZZ.hom(ZZ)
> > +            sage: f = ZZ.hom(Zp)
> >              sage: f.inverse_image(ZZ.ideal(2))
> >              Traceback (most recent call last):
> >              ...
> > ```

> `ZZ.hom(ZZ)` is an `IdentityMorphism` now which does not have an `inverse_image()` method. We went from `NotImplementedError` to `AttributeError` it seems.

So this really is a subtle backwards incompatible change. Hopefully nobody was just catching the `NotImplementedError`. I don't think there is any (reasonable) way to put a deprecation and/or get around that.

This test is also not valid because `Zp` is a class, not a parent.

Actually, that brings up another question, not just that you need to handle old pickles, but do we want to have a small deprecation class for `RingHomomorphism_coercion`? This is an implementation detail, but it is something publicly documented. So someone might have code that uses this class and that code would just break upon upgrade.



---

archive/issue_comments_350760.json:
```json
{
    "body": "Changed work issues from **drop is_RingHomomorphism, consume old pickles, check that composite maps do not cause a speed regression** to **drop is_RingHomomorphism, consume old pickles, check that composite maps do not cause a speed regression, remove _parent hasattr, fix inverse image doctest**",
    "created_at": "2017-07-20T01:41:24Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350760",
    "user": "https://github.com/saraedum"
}
```

Changed work issues from **drop is_RingHomomorphism, consume old pickles, check that composite maps do not cause a speed regression** to **drop is_RingHomomorphism, consume old pickles, check that composite maps do not cause a speed regression, remove _parent hasattr, fix inverse image doctest**



---

archive/issue_comments_350761.json:
```json
{
    "body": "<a id='comment:26'>Comment 26:</a>\nApropos of the `_parent` question, there was an example of such behavior in the doctest of the method I was fixing:\n\n```\nsage: Hom(ZZ,Zmod(1)).an_element().parent()\nSet of Homomorphisms from Integer Ring to Ring of integers modulo 1\nsage: Hom(ZZ,Zmod(1)).an_element()._parent\n...\nAttributeError: 'sage.rings.morphism.RingHomomorphism_im_gens' object has no attribute '_parent'\n```",
    "created_at": "2017-07-20T01:59:50Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350761",
    "user": "https://github.com/sagetrac-xander-faber"
}
```

<a id='comment:26'>Comment 26:</a>
Apropos of the `_parent` question, there was an example of such behavior in the doctest of the method I was fixing:

```
sage: Hom(ZZ,Zmod(1)).an_element().parent()
Set of Homomorphisms from Integer Ring to Ring of integers modulo 1
sage: Hom(ZZ,Zmod(1)).an_element()._parent
...
AttributeError: 'sage.rings.morphism.RingHomomorphism_im_gens' object has no attribute '_parent'
```



---

archive/issue_comments_350762.json:
```json
{
    "body": "<a id='comment:27'>Comment 27:</a>\nNo, you cannot get `_parent` in a Sage session as it is a C(ython) variable. However, within Cython code this should not be a problem.",
    "created_at": "2017-07-20T02:08:28Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350762",
    "user": "https://github.com/tscrim"
}
```

<a id='comment:27'>Comment 27:</a>
No, you cannot get `_parent` in a Sage session as it is a C(ython) variable. However, within Cython code this should not be a problem.



---

archive/issue_comments_350763.json:
```json
{
    "body": "<a id='comment:28'>Comment 28:</a>\nReplying to [@tscrim](#comment%3A24):\n> Replying to [@saraedum](#comment%3A22):\n> > Replying to [@tscrim](#comment%3A21):\n> > > Replying to [@saraedum](#comment%3A20):\n> > > > David Roe and I looked at your comments.\n> > > > \n> > > > Replying to [@tscrim](#comment%3A17):\n> > > > > A strong -1 on calling the category (first): this is relatively expensive, not likely able to be implemented by the `MorphismMethods` of a category if you have to do formal compositions, and is meant to be overwritten. Really, if you want composite maps, then you should overwrite the `_composition_` hook.\n\n> > > > We don't understand what you are trying to say here. Could you rephrase your concern please.\n\n> > > \n> > > You added two blocks for formal composite functions:\n> > > [snip]\n> > > What I am saying is this is really (relatively) expensive for something that is almost certainly going to be implemented. If your category has morphisms such that you cannot effectively simplify a composition `f \\circ g` to a single map `h`, then I don't see how you would have enough (generic) information to check that the composition is injective/surjective. So I doubt this would ever get used.\n\n> > I do not understand how this relates to `_composition_`. The point here is simply that in the category of Rings, a morphism that has a field as its domain, knows that it is injective. However, we can not so easily turn the composition into one morphism.\n\n> \n> So you're doing implementing that at the category level? I feel like for something like this you should have a generic field morphism class that knows outright that it is injective and implements a custom `_composition_` that returns the same class because it is completely determined by the image of `1`. So the composition would amount to chasing through each map what happens to `1`. If you are using some more specialized morphisms and need the generic composition, then let the generic code of the composition do its thing. I don't see the point of slowing everything else down for a special case that is more effectively dealt with by a concrete class.\n\nMorphisms from fields aren't completely determined by where they send 1 (they have to send 1 to 1).  For example, complex conjugation is a morphism of fields.  Moreover, fields can embed into lots of kinds of rings that aren't fields, and you don't want to require a concrete class for each of these situations.  One of the reasons for the existence of the category framework is to enable writing generic code like this.\n \n> > I agree. What were the elements without `_parent`?\n\n> \n> My point is that never happens. All elements must inherit from `Element` (or you are basically reimplementing `Element` with less features).\n\nYep, this should get fixed.\n\n> > > > > I don't understand why this needs to be a more generic class:\n> > > > > [snip]\n\n> > > > Because some lift map is now not inheriting from `RingMap` anymore.\n\n> > > That smells a little like you have encountered a bug and something really should be returning a `RingMap_lift`. Either that or we should also remove `RingMap_lift` with some more generic class for lifts (if that is what we decide to do, we can do that here or on a followup).\n\n> > Yes, we should remove `RingMap_lift`. I don't remember the details but changing this type was not a hack to get things to compile.\n\n> \n> I would call it a temporary fix until we remove `RingMap_list`. However, will that be a followup or done here? There is good reason to do it here as we are already touching a lot of places and is similar in spirit, but it would be a definite expansion of scope. I leave it up to you.\n\nI think we should create a followup ticket to remove `RingMap_lift`, since I think it will take a nontrivial amount of work.\n\n> > > > > I am a little wary of removing `RingHomomorphism.__nonzero__` as that specialized code does do a check that could be faster and might be a little better behaved because no equality checking is involved. I just want to make sure you really checked this doesn't introduce a speed regression and works well for inexact rings (which may not be fully doctested).\n\n> > > > We think that you will never get to the `==` check in the new implementation in the case of ring homomorphism (unless the codomain is trivial). We checked a few cases and there was no speed regression.\n\n> > > That does not seem possible. If the ring homset has no zero, then is relatively expensive because of the throwing and catching of an error and is something done every time. If there is a zero map, then it should only affect the first time it is called, but then it has to do a comparison because of the default `Element.__nonzero__`. What were the cases you tested?\n\n> > Sorry, I had misread your initial comment. There is probably a speed regression but when would this possibly be relevant (i.e., when do you create lots of rings and check that morphisms between them are nonzero? without knowing that you are talking about rings.)\n> > \n> > ```\n> > sage: f = ZZ.hom(Zp(3))\n> > sage: %timeit bool(f)\n> > 1000000 loops, best of 3: 212 ns per loop # before\n> > 10000 loops, best of 3: 25.5 \u00b5s per loop # after\n> > ```\n\n> \n> I don't have a use case for it, but I do not see the point in deliberately introducing a regression by removing code that is (was?) working perfectly well. Someone might have a use case for it; I know people create lots of polynomial rings over finite fields as the field size varies, and they might be doing a check for a nonzero map in that code.\n\nIf you're creating polynomial rings over finite fields, then the morphism can never be zero so you won't be calling this code.  The only time you can have a zero morphism is if the codomain is the zero ring.  It seems unlikely that you're going to be testing this enough that the difference between `200ns` and `20\u00b5s`\n\nThe benefit is that we are now able to use a general framework for morphisms between rings that don't inherit from `RingHomomorphism`, for example the coercion map from `ZZ` to `QQ`.  In my opinion, this is valuable enough to merit the speed regression in this case.\n\nFinally, Julian and I talked about it and we don't see an easy way to add the custom code back in.  Since `__nonzero__` is a double underscore method, we can't use `cached_method` there, and you can't productively use `cached_method` on `zero` since it won't cache the fact that it raised an error.  Moreover, if we try to implement this in the category, then in order to  hook code into `__nonzero__` we have to make it slower for all other elements, which doesn't seem reasonable.\n\n> > > Could you explain why you needed to change this doctest:\n> > > \n> > > ```diff\n> > > @@ -861,7 +808,7 @@ cdef class RingHomomorphism(RingMap):\n> > >  \n> > >          This is not implemented in any generality yet::\n> > >  \n> > > -            sage: f = ZZ.hom(ZZ)\n> > > +            sage: f = ZZ.hom(Zp)\n> > >              sage: f.inverse_image(ZZ.ideal(2))\n> > >              Traceback (most recent call last):\n> > >              ...\n> > > ```\n\n> > `ZZ.hom(ZZ)` is an `IdentityMorphism` now which does not have an `inverse_image()` method. We went from `NotImplementedError` to `AttributeError` it seems.\n\n> \n> So this really is a subtle backwards incompatible change. Hopefully nobody was just catching the `NotImplementedError`. I don't think there is any (reasonable) way to put a deprecation and/or get around that.\n\nI don't see a way to deprecate it either, but I'm not too worried about it.\n\n> This test is also not valid because `Zp` is a class, not a parent.\n\nAgreed; I think Julian is fixing this now.\n\n> Actually, that brings up another question, not just that you need to handle old pickles, but do we want to have a small deprecation class for `RingHomomorphism_coercion`? This is an implementation detail, but it is something publicly documented. So someone might have code that uses this class and that code would just break upon upgrade.\n\nYeah, it seems reasonable to add something like\n\n```\nfrom sage.misc.superseded import deprecation\nclass RingHomomorphism_coercion(RingHomomorphism):\n    def __init__(self, *args, **kwds):\n        deprecation(23204, \"Don't use this\")\n        RingHomomorphism.__init__(self, *args, **kwds)\n```",
    "created_at": "2017-07-20T02:29:41Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350763",
    "user": "https://github.com/roed314"
}
```

<a id='comment:28'>Comment 28:</a>
Replying to [@tscrim](#comment%3A24):
> Replying to [@saraedum](#comment%3A22):
> > Replying to [@tscrim](#comment%3A21):
> > > Replying to [@saraedum](#comment%3A20):
> > > > David Roe and I looked at your comments.
> > > > 
> > > > Replying to [@tscrim](#comment%3A17):
> > > > > A strong -1 on calling the category (first): this is relatively expensive, not likely able to be implemented by the `MorphismMethods` of a category if you have to do formal compositions, and is meant to be overwritten. Really, if you want composite maps, then you should overwrite the `_composition_` hook.

> > > > We don't understand what you are trying to say here. Could you rephrase your concern please.

> > > 
> > > You added two blocks for formal composite functions:
> > > [snip]
> > > What I am saying is this is really (relatively) expensive for something that is almost certainly going to be implemented. If your category has morphisms such that you cannot effectively simplify a composition `f \circ g` to a single map `h`, then I don't see how you would have enough (generic) information to check that the composition is injective/surjective. So I doubt this would ever get used.

> > I do not understand how this relates to `_composition_`. The point here is simply that in the category of Rings, a morphism that has a field as its domain, knows that it is injective. However, we can not so easily turn the composition into one morphism.

> 
> So you're doing implementing that at the category level? I feel like for something like this you should have a generic field morphism class that knows outright that it is injective and implements a custom `_composition_` that returns the same class because it is completely determined by the image of `1`. So the composition would amount to chasing through each map what happens to `1`. If you are using some more specialized morphisms and need the generic composition, then let the generic code of the composition do its thing. I don't see the point of slowing everything else down for a special case that is more effectively dealt with by a concrete class.

Morphisms from fields aren't completely determined by where they send 1 (they have to send 1 to 1).  For example, complex conjugation is a morphism of fields.  Moreover, fields can embed into lots of kinds of rings that aren't fields, and you don't want to require a concrete class for each of these situations.  One of the reasons for the existence of the category framework is to enable writing generic code like this.
 
> > I agree. What were the elements without `_parent`?

> 
> My point is that never happens. All elements must inherit from `Element` (or you are basically reimplementing `Element` with less features).

Yep, this should get fixed.

> > > > > I don't understand why this needs to be a more generic class:
> > > > > [snip]

> > > > Because some lift map is now not inheriting from `RingMap` anymore.

> > > That smells a little like you have encountered a bug and something really should be returning a `RingMap_lift`. Either that or we should also remove `RingMap_lift` with some more generic class for lifts (if that is what we decide to do, we can do that here or on a followup).

> > Yes, we should remove `RingMap_lift`. I don't remember the details but changing this type was not a hack to get things to compile.

> 
> I would call it a temporary fix until we remove `RingMap_list`. However, will that be a followup or done here? There is good reason to do it here as we are already touching a lot of places and is similar in spirit, but it would be a definite expansion of scope. I leave it up to you.

I think we should create a followup ticket to remove `RingMap_lift`, since I think it will take a nontrivial amount of work.

> > > > > I am a little wary of removing `RingHomomorphism.__nonzero__` as that specialized code does do a check that could be faster and might be a little better behaved because no equality checking is involved. I just want to make sure you really checked this doesn't introduce a speed regression and works well for inexact rings (which may not be fully doctested).

> > > > We think that you will never get to the `==` check in the new implementation in the case of ring homomorphism (unless the codomain is trivial). We checked a few cases and there was no speed regression.

> > > That does not seem possible. If the ring homset has no zero, then is relatively expensive because of the throwing and catching of an error and is something done every time. If there is a zero map, then it should only affect the first time it is called, but then it has to do a comparison because of the default `Element.__nonzero__`. What were the cases you tested?

> > Sorry, I had misread your initial comment. There is probably a speed regression but when would this possibly be relevant (i.e., when do you create lots of rings and check that morphisms between them are nonzero? without knowing that you are talking about rings.)
> > 
> > ```
> > sage: f = ZZ.hom(Zp(3))
> > sage: %timeit bool(f)
> > 1000000 loops, best of 3: 212 ns per loop # before
> > 10000 loops, best of 3: 25.5 µs per loop # after
> > ```

> 
> I don't have a use case for it, but I do not see the point in deliberately introducing a regression by removing code that is (was?) working perfectly well. Someone might have a use case for it; I know people create lots of polynomial rings over finite fields as the field size varies, and they might be doing a check for a nonzero map in that code.

If you're creating polynomial rings over finite fields, then the morphism can never be zero so you won't be calling this code.  The only time you can have a zero morphism is if the codomain is the zero ring.  It seems unlikely that you're going to be testing this enough that the difference between `200ns` and `20µs`

The benefit is that we are now able to use a general framework for morphisms between rings that don't inherit from `RingHomomorphism`, for example the coercion map from `ZZ` to `QQ`.  In my opinion, this is valuable enough to merit the speed regression in this case.

Finally, Julian and I talked about it and we don't see an easy way to add the custom code back in.  Since `__nonzero__` is a double underscore method, we can't use `cached_method` there, and you can't productively use `cached_method` on `zero` since it won't cache the fact that it raised an error.  Moreover, if we try to implement this in the category, then in order to  hook code into `__nonzero__` we have to make it slower for all other elements, which doesn't seem reasonable.

> > > Could you explain why you needed to change this doctest:
> > > 
> > > ```diff
> > > @@ -861,7 +808,7 @@ cdef class RingHomomorphism(RingMap):
> > >  
> > >          This is not implemented in any generality yet::
> > >  
> > > -            sage: f = ZZ.hom(ZZ)
> > > +            sage: f = ZZ.hom(Zp)
> > >              sage: f.inverse_image(ZZ.ideal(2))
> > >              Traceback (most recent call last):
> > >              ...
> > > ```

> > `ZZ.hom(ZZ)` is an `IdentityMorphism` now which does not have an `inverse_image()` method. We went from `NotImplementedError` to `AttributeError` it seems.

> 
> So this really is a subtle backwards incompatible change. Hopefully nobody was just catching the `NotImplementedError`. I don't think there is any (reasonable) way to put a deprecation and/or get around that.

I don't see a way to deprecate it either, but I'm not too worried about it.

> This test is also not valid because `Zp` is a class, not a parent.

Agreed; I think Julian is fixing this now.

> Actually, that brings up another question, not just that you need to handle old pickles, but do we want to have a small deprecation class for `RingHomomorphism_coercion`? This is an implementation detail, but it is something publicly documented. So someone might have code that uses this class and that code would just break upon upgrade.

Yeah, it seems reasonable to add something like

```
from sage.misc.superseded import deprecation
class RingHomomorphism_coercion(RingHomomorphism):
    def __init__(self, *args, **kwds):
        deprecation(23204, "Don't use this")
        RingHomomorphism.__init__(self, *args, **kwds)
```



---

archive/issue_comments_350764.json:
```json
{
    "body": "<a id='comment:29'>Comment 29:</a>\n> > > > > > I am a little wary of removing `RingHomomorphism.__nonzero__` as that specialized code does do a check that could be faster and might be a little better behaved because no equality checking is involved. I just want to make sure you really checked this doesn't introduce a speed regression and works well for inexact rings (which may not be fully doctested).\n\n> > > > > We think that you will never get to the `==` check in the new implementation in the case of ring homomorphism (unless the codomain is trivial). We checked a few cases and there was no speed regression.\n\n> > > > That does not seem possible. If the ring homset has no zero, then is relatively expensive because of the throwing and catching of an error and is something done every time. If there is a zero map, then it should only affect the first time it is called, but then it has to do a comparison because of the default `Element.__nonzero__`. What were the cases you tested?\n\n> > > Sorry, I had misread your initial comment. There is probably a speed regression but when would this possibly be relevant (i.e., when do you create lots of rings and check that morphisms between them are nonzero? without knowing that you are talking about rings.)\n> > > \n> > > ```\n> > > sage: f = ZZ.hom(Zp(3))\n> > > sage: %timeit bool(f)\n> > > 1000000 loops, best of 3: 212 ns per loop # before\n> > > 10000 loops, best of 3: 25.5 \u00b5s per loop # after\n> > > ```\n\n> > \n> > I don't have a use case for it, but I do not see the point in deliberately introducing a regression by removing code that is (was?) working perfectly well. Someone might have a use case for it; I know people create lots of polynomial rings over finite fields as the field size varies, and they might be doing a check for a nonzero map in that code.\n\n> \n> If you're creating polynomial rings over finite fields, then the morphism can never be zero so you won't be calling this code.  The only time you can have a zero morphism is if the codomain is the zero ring.  It seems unlikely that you're going to be testing this enough that the difference between `200ns` and `20\u00b5s`\n> \n> The benefit is that we are now able to use a general framework for morphisms between rings that don't inherit from `RingHomomorphism`, for example the coercion map from `ZZ` to `QQ`.  In my opinion, this is valuable enough to merit the speed regression in this case.\n> \n> Finally, Julian and I talked about it and we don't see an easy way to add the custom code back in.  Since `__nonzero__` is a double underscore method, we can't use `cached_method` there, and you can't productively use `cached_method` on `zero` since it won't cache the fact that it raised an error.  Moreover, if we try to implement this in the category, then in order to  hook code into `__nonzero__` we have to make it slower for all other elements, which doesn't seem reasonable.\n\nOkay, we came up with some situations where the speed might matter.  For example, if you call `coerce_map_from` it returns either a morphism or `None`.  It's possible that people then use the result in a boolean context to determine whether it's `None`.\n\nWe'll try to improve the speed.",
    "created_at": "2017-07-20T03:00:05Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350764",
    "user": "https://github.com/roed314"
}
```

<a id='comment:29'>Comment 29:</a>
> > > > > > I am a little wary of removing `RingHomomorphism.__nonzero__` as that specialized code does do a check that could be faster and might be a little better behaved because no equality checking is involved. I just want to make sure you really checked this doesn't introduce a speed regression and works well for inexact rings (which may not be fully doctested).

> > > > > We think that you will never get to the `==` check in the new implementation in the case of ring homomorphism (unless the codomain is trivial). We checked a few cases and there was no speed regression.

> > > > That does not seem possible. If the ring homset has no zero, then is relatively expensive because of the throwing and catching of an error and is something done every time. If there is a zero map, then it should only affect the first time it is called, but then it has to do a comparison because of the default `Element.__nonzero__`. What were the cases you tested?

> > > Sorry, I had misread your initial comment. There is probably a speed regression but when would this possibly be relevant (i.e., when do you create lots of rings and check that morphisms between them are nonzero? without knowing that you are talking about rings.)
> > > 
> > > ```
> > > sage: f = ZZ.hom(Zp(3))
> > > sage: %timeit bool(f)
> > > 1000000 loops, best of 3: 212 ns per loop # before
> > > 10000 loops, best of 3: 25.5 µs per loop # after
> > > ```

> > 
> > I don't have a use case for it, but I do not see the point in deliberately introducing a regression by removing code that is (was?) working perfectly well. Someone might have a use case for it; I know people create lots of polynomial rings over finite fields as the field size varies, and they might be doing a check for a nonzero map in that code.

> 
> If you're creating polynomial rings over finite fields, then the morphism can never be zero so you won't be calling this code.  The only time you can have a zero morphism is if the codomain is the zero ring.  It seems unlikely that you're going to be testing this enough that the difference between `200ns` and `20µs`
> 
> The benefit is that we are now able to use a general framework for morphisms between rings that don't inherit from `RingHomomorphism`, for example the coercion map from `ZZ` to `QQ`.  In my opinion, this is valuable enough to merit the speed regression in this case.
> 
> Finally, Julian and I talked about it and we don't see an easy way to add the custom code back in.  Since `__nonzero__` is a double underscore method, we can't use `cached_method` there, and you can't productively use `cached_method` on `zero` since it won't cache the fact that it raised an error.  Moreover, if we try to implement this in the category, then in order to  hook code into `__nonzero__` we have to make it slower for all other elements, which doesn't seem reasonable.

Okay, we came up with some situations where the speed might matter.  For example, if you call `coerce_map_from` it returns either a morphism or `None`.  It's possible that people then use the result in a boolean context to determine whether it's `None`.

We'll try to improve the speed.



---

archive/issue_comments_350765.json:
```json
{
    "body": "<a id='comment:30'>Comment 30:</a>\nReplying to [@roed314](#comment%3A28):\n> Replying to [@tscrim](#comment%3A24):\n> > Replying to [@saraedum](#comment%3A22):\n> > > Replying to [@tscrim](#comment%3A21):\n> > > > Replying to [@saraedum](#comment%3A20):\n> > > > > David Roe and I looked at your comments.\n> > > > > \n> > > > > Replying to [@tscrim](#comment%3A17):\n> > > > > > A strong -1 on calling the category (first): this is relatively expensive, not likely able to be implemented by the `MorphismMethods` of a category if you have to do formal compositions, and is meant to be overwritten. Really, if you want composite maps, then you should overwrite the `_composition_` hook.\n\n> > > > > We don't understand what you are trying to say here. Could you rephrase your concern please.\n\n> > > > \n> > > > You added two blocks for formal composite functions:\n> > > > [snip]\n> > > > What I am saying is this is really (relatively) expensive for something that is almost certainly going to be implemented. If your category has morphisms such that you cannot effectively simplify a composition `f \\circ g` to a single map `h`, then I don't see how you would have enough (generic) information to check that the composition is injective/surjective. So I doubt this would ever get used.\n\n> > > I do not understand how this relates to `_composition_`. The point here is simply that in the category of Rings, a morphism that has a field as its domain, knows that it is injective. However, we can not so easily turn the composition into one morphism.\n\n> > \n> > So you're doing implementing that at the category level? I feel like for something like this you should have a generic field morphism class that knows outright that it is injective and implements a custom `_composition_` that returns the same class because it is completely determined by the image of `1`. So the composition would amount to chasing through each map what happens to `1`. If you are using some more specialized morphisms and need the generic composition, then let the generic code of the composition do its thing. I don't see the point of slowing everything else down for a special case that is more effectively dealt with by a concrete class.\n\n> \n> Morphisms from fields aren't completely determined by where they send 1 (they have to send 1 to 1).  For example, complex conjugation is a morphism of fields.  Moreover, fields can embed into lots of kinds of rings that aren't fields, and you don't want to require a concrete class for each of these situations.  One of the reasons for the existence of the category framework is to enable writing generic code like this.\n\nAh, right. However, you are not doing general code, you are doing a special case, which IMO is a relatively small set of the cases, in a concrete class. Because of that, it should be a separate class if you really need that. To me, the category framework is primarily for general code that is *not* implemented in a concrete class, but can be used as a fallback, and often times, it is slow. I strongly believe that there is no practical advantage in any situation to having to do a Python call up to through the category framework instead of just letting the composite map do its thing. Moreover, I believe there will be a penalty for this extra (useless) call up through the category in general; there is typically not enough structure to say anything.\n\n> I think we should create a followup ticket to remove `RingMap_lift`, since I think it will take a nontrivial amount of work.\n\nPlease create it and cc me on it; I will be happy to review it.\n\n> > > > > > I am a little wary of removing `RingHomomorphism.__nonzero__` as that specialized code does do a check that could be faster and might be a little better behaved because no equality checking is involved. I just want to make sure you really checked this doesn't introduce a speed regression and works well for inexact rings (which may not be fully doctested).\n\n> > > > > We think that you will never get to the `==` check in the new implementation in the case of ring homomorphism (unless the codomain is trivial). We checked a few cases and there was no speed regression.\n\n> > > > That does not seem possible. If the ring homset has no zero, then is relatively expensive because of the throwing and catching of an error and is something done every time. If there is a zero map, then it should only affect the first time it is called, but then it has to do a comparison because of the default `Element.__nonzero__`. What were the cases you tested?\n\n> > > Sorry, I had misread your initial comment. There is probably a speed regression but when would this possibly be relevant (i.e., when do you create lots of rings and check that morphisms between them are nonzero? without knowing that you are talking about rings.)\n> > > \n> > > ```\n> > > sage: f = ZZ.hom(Zp(3))\n> > > sage: %timeit bool(f)\n> > > 1000000 loops, best of 3: 212 ns per loop # before\n> > > 10000 loops, best of 3: 25.5 \u00b5s per loop # after\n> > > ```\n\n> > \n> > I don't have a use case for it, but I do not see the point in deliberately introducing a regression by removing code that is (was?) working perfectly well. Someone might have a use case for it; I know people create lots of polynomial rings over finite fields as the field size varies, and they might be doing a check for a nonzero map in that code.\n\n> \n> If you're creating polynomial rings over finite fields, then the morphism can never be zero so you won't be calling this code.  The only time you can have a zero morphism is if the codomain is the zero ring.  It seems unlikely that you're going to be testing this enough that the difference between `200ns` and `20\u00b5s`\n> \n> The benefit is that we are now able to use a general framework for morphisms between rings that don't inherit from `RingHomomorphism`, for example the coercion map from `ZZ` to `QQ`.  In my opinion, this is valuable enough to merit the speed regression in this case.\n\nThis is not an argument for removing `__nonzero__`; only for having the coercion maps not be a subclass of `RingHomomorphism`. If they don't inherit from `RingHomomorphism`, then fine, they do not benefit. However, what about all of those classes that do? You are penalizing them for no reason. Also, the category framework is in some sense a (sequence of) abstract (Python) base classes; if you need to have your own hierarchy of (C/Python) classes, then there is no need to try to force things up to the category level.\n\n> Finally, Julian and I talked about it and we don't see an easy way to add the custom code back in.  Since `__nonzero__` is a double underscore method, we can't use `cached_method` there, and you can't productively use `cached_method` on `zero` since it won't cache the fact that it raised an error.  Moreover, if we try to implement this in the category, then in order to  hook code into `__nonzero__` we have to make it slower for all other elements, which doesn't seem reasonable.\n\nHuh? You cannot use a `@cached_method` only because it is a Cython class, which needs to handle the special double underscore methods differently. For Python classes, you can use `@cached_method` on, e.g., `__nonzero__`. However, this is unrelated because there is/was no caching going on.\n\nLastly, just as a word of caution, moving things up into a category forces them to be Python code, which is inherently slower than Cython code. Also, what is your end-game with the ring morphism code? Do you have a plan for what you want it to be like when all the changes are done?",
    "created_at": "2017-07-20T13:05:17Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350765",
    "user": "https://github.com/tscrim"
}
```

<a id='comment:30'>Comment 30:</a>
Replying to [@roed314](#comment%3A28):
> Replying to [@tscrim](#comment%3A24):
> > Replying to [@saraedum](#comment%3A22):
> > > Replying to [@tscrim](#comment%3A21):
> > > > Replying to [@saraedum](#comment%3A20):
> > > > > David Roe and I looked at your comments.
> > > > > 
> > > > > Replying to [@tscrim](#comment%3A17):
> > > > > > A strong -1 on calling the category (first): this is relatively expensive, not likely able to be implemented by the `MorphismMethods` of a category if you have to do formal compositions, and is meant to be overwritten. Really, if you want composite maps, then you should overwrite the `_composition_` hook.

> > > > > We don't understand what you are trying to say here. Could you rephrase your concern please.

> > > > 
> > > > You added two blocks for formal composite functions:
> > > > [snip]
> > > > What I am saying is this is really (relatively) expensive for something that is almost certainly going to be implemented. If your category has morphisms such that you cannot effectively simplify a composition `f \circ g` to a single map `h`, then I don't see how you would have enough (generic) information to check that the composition is injective/surjective. So I doubt this would ever get used.

> > > I do not understand how this relates to `_composition_`. The point here is simply that in the category of Rings, a morphism that has a field as its domain, knows that it is injective. However, we can not so easily turn the composition into one morphism.

> > 
> > So you're doing implementing that at the category level? I feel like for something like this you should have a generic field morphism class that knows outright that it is injective and implements a custom `_composition_` that returns the same class because it is completely determined by the image of `1`. So the composition would amount to chasing through each map what happens to `1`. If you are using some more specialized morphisms and need the generic composition, then let the generic code of the composition do its thing. I don't see the point of slowing everything else down for a special case that is more effectively dealt with by a concrete class.

> 
> Morphisms from fields aren't completely determined by where they send 1 (they have to send 1 to 1).  For example, complex conjugation is a morphism of fields.  Moreover, fields can embed into lots of kinds of rings that aren't fields, and you don't want to require a concrete class for each of these situations.  One of the reasons for the existence of the category framework is to enable writing generic code like this.

Ah, right. However, you are not doing general code, you are doing a special case, which IMO is a relatively small set of the cases, in a concrete class. Because of that, it should be a separate class if you really need that. To me, the category framework is primarily for general code that is *not* implemented in a concrete class, but can be used as a fallback, and often times, it is slow. I strongly believe that there is no practical advantage in any situation to having to do a Python call up to through the category framework instead of just letting the composite map do its thing. Moreover, I believe there will be a penalty for this extra (useless) call up through the category in general; there is typically not enough structure to say anything.

> I think we should create a followup ticket to remove `RingMap_lift`, since I think it will take a nontrivial amount of work.

Please create it and cc me on it; I will be happy to review it.

> > > > > > I am a little wary of removing `RingHomomorphism.__nonzero__` as that specialized code does do a check that could be faster and might be a little better behaved because no equality checking is involved. I just want to make sure you really checked this doesn't introduce a speed regression and works well for inexact rings (which may not be fully doctested).

> > > > > We think that you will never get to the `==` check in the new implementation in the case of ring homomorphism (unless the codomain is trivial). We checked a few cases and there was no speed regression.

> > > > That does not seem possible. If the ring homset has no zero, then is relatively expensive because of the throwing and catching of an error and is something done every time. If there is a zero map, then it should only affect the first time it is called, but then it has to do a comparison because of the default `Element.__nonzero__`. What were the cases you tested?

> > > Sorry, I had misread your initial comment. There is probably a speed regression but when would this possibly be relevant (i.e., when do you create lots of rings and check that morphisms between them are nonzero? without knowing that you are talking about rings.)
> > > 
> > > ```
> > > sage: f = ZZ.hom(Zp(3))
> > > sage: %timeit bool(f)
> > > 1000000 loops, best of 3: 212 ns per loop # before
> > > 10000 loops, best of 3: 25.5 µs per loop # after
> > > ```

> > 
> > I don't have a use case for it, but I do not see the point in deliberately introducing a regression by removing code that is (was?) working perfectly well. Someone might have a use case for it; I know people create lots of polynomial rings over finite fields as the field size varies, and they might be doing a check for a nonzero map in that code.

> 
> If you're creating polynomial rings over finite fields, then the morphism can never be zero so you won't be calling this code.  The only time you can have a zero morphism is if the codomain is the zero ring.  It seems unlikely that you're going to be testing this enough that the difference between `200ns` and `20µs`
> 
> The benefit is that we are now able to use a general framework for morphisms between rings that don't inherit from `RingHomomorphism`, for example the coercion map from `ZZ` to `QQ`.  In my opinion, this is valuable enough to merit the speed regression in this case.

This is not an argument for removing `__nonzero__`; only for having the coercion maps not be a subclass of `RingHomomorphism`. If they don't inherit from `RingHomomorphism`, then fine, they do not benefit. However, what about all of those classes that do? You are penalizing them for no reason. Also, the category framework is in some sense a (sequence of) abstract (Python) base classes; if you need to have your own hierarchy of (C/Python) classes, then there is no need to try to force things up to the category level.

> Finally, Julian and I talked about it and we don't see an easy way to add the custom code back in.  Since `__nonzero__` is a double underscore method, we can't use `cached_method` there, and you can't productively use `cached_method` on `zero` since it won't cache the fact that it raised an error.  Moreover, if we try to implement this in the category, then in order to  hook code into `__nonzero__` we have to make it slower for all other elements, which doesn't seem reasonable.

Huh? You cannot use a `@cached_method` only because it is a Cython class, which needs to handle the special double underscore methods differently. For Python classes, you can use `@cached_method` on, e.g., `__nonzero__`. However, this is unrelated because there is/was no caching going on.

Lastly, just as a word of caution, moving things up into a category forces them to be Python code, which is inherently slower than Cython code. Also, what is your end-game with the ring morphism code? Do you have a plan for what you want it to be like when all the changes are done?



---

archive/issue_comments_350766.json:
```json
{
    "body": "<a id='comment:31'>Comment 31:</a>\nBranch pushed to git repo; I updated commit sha1. New commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/7ba221d6471bef3a74570ba8b8d5059bde3cd8dc\">7ba221d</a></td><td><code>Deprecate is_RingHomomorphism</code></td></tr></table>\n",
    "created_at": "2017-07-20T19:55:52Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350766",
    "user": "https://github.com/sagetrac-git"
}
```

<a id='comment:31'>Comment 31:</a>
Branch pushed to git repo; I updated commit sha1. New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/7ba221d6471bef3a74570ba8b8d5059bde3cd8dc">7ba221d</a></td><td><code>Deprecate is_RingHomomorphism</code></td></tr></table>




---

archive/issue_comments_350767.json:
```json
{
    "body": "Changed commit from **[29bad40](https://github.com/sagemath/sagetrac-mirror/commit/29bad40adeff929af5ec0697f6f1a65949bf497f)** to **[7ba221d](https://github.com/sagemath/sagetrac-mirror/commit/7ba221d6471bef3a74570ba8b8d5059bde3cd8dc)**",
    "created_at": "2017-07-20T19:55:52Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350767",
    "user": "https://github.com/sagetrac-git"
}
```

Changed commit from **[29bad40](https://github.com/sagemath/sagetrac-mirror/commit/29bad40adeff929af5ec0697f6f1a65949bf497f)** to **[7ba221d](https://github.com/sagemath/sagetrac-mirror/commit/7ba221d6471bef3a74570ba8b8d5059bde3cd8dc)**



---

archive/issue_comments_350768.json:
```json
{
    "body": "Changed work issues from **drop is_RingHomomorphism, consume old pickles, check that composite maps do not cause a speed regression, remove _parent hasattr, fix inverse image doctest** to **consume old pickles, check that composite maps do not cause a speed regression, remove _parent hasattr, fix inverse image doctest**",
    "created_at": "2017-07-20T19:57:02Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350768",
    "user": "https://github.com/saraedum"
}
```

Changed work issues from **drop is_RingHomomorphism, consume old pickles, check that composite maps do not cause a speed regression, remove _parent hasattr, fix inverse image doctest** to **consume old pickles, check that composite maps do not cause a speed regression, remove _parent hasattr, fix inverse image doctest**



---

archive/issue_comments_350769.json:
```json
{
    "body": "<a id='comment:32'>Comment 32:</a>\nNew commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/7ba221d6471bef3a74570ba8b8d5059bde3cd8dc\">7ba221d</a></td><td><code>Deprecate is_RingHomomorphism</code></td></tr></table>\n\n---\nNew commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/7ba221d6471bef3a74570ba8b8d5059bde3cd8dc\">7ba221d</a></td><td><code>Deprecate is_RingHomomorphism</code></td></tr></table>\n",
    "created_at": "2017-07-20T19:57:02Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350769",
    "user": "https://github.com/saraedum"
}
```

<a id='comment:32'>Comment 32:</a>
New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/7ba221d6471bef3a74570ba8b8d5059bde3cd8dc">7ba221d</a></td><td><code>Deprecate is_RingHomomorphism</code></td></tr></table>

---
New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/7ba221d6471bef3a74570ba8b8d5059bde3cd8dc">7ba221d</a></td><td><code>Deprecate is_RingHomomorphism</code></td></tr></table>




---

archive/issue_comments_350770.json:
```json
{
    "body": "<a id='comment:33'>Comment 33:</a>\nBranch pushed to git repo; I updated commit sha1. New commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/71db2646349fc5a893404ffa89bd62b59100b8ea\">71db264</a></td><td><code>Remove _parent,parent() distinction</code></td></tr></table>\n",
    "created_at": "2017-07-20T19:59:36Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350770",
    "user": "https://github.com/sagetrac-git"
}
```

<a id='comment:33'>Comment 33:</a>
Branch pushed to git repo; I updated commit sha1. New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/71db2646349fc5a893404ffa89bd62b59100b8ea">71db264</a></td><td><code>Remove _parent,parent() distinction</code></td></tr></table>




---

archive/issue_comments_350771.json:
```json
{
    "body": "Changed commit from **[7ba221d](https://github.com/sagemath/sagetrac-mirror/commit/7ba221d6471bef3a74570ba8b8d5059bde3cd8dc)** to **[71db264](https://github.com/sagemath/sagetrac-mirror/commit/71db2646349fc5a893404ffa89bd62b59100b8ea)**",
    "created_at": "2017-07-20T19:59:36Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350771",
    "user": "https://github.com/sagetrac-git"
}
```

Changed commit from **[7ba221d](https://github.com/sagemath/sagetrac-mirror/commit/7ba221d6471bef3a74570ba8b8d5059bde3cd8dc)** to **[71db264](https://github.com/sagemath/sagetrac-mirror/commit/71db2646349fc5a893404ffa89bd62b59100b8ea)**



---

archive/issue_comments_350772.json:
```json
{
    "body": "<a id='comment:34'>Comment 34:</a>\nNew commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/71db2646349fc5a893404ffa89bd62b59100b8ea\">71db264</a></td><td><code>Remove _parent,parent() distinction</code></td></tr></table>\n\n---\nNew commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/71db2646349fc5a893404ffa89bd62b59100b8ea\">71db264</a></td><td><code>Remove _parent,parent() distinction</code></td></tr></table>\n",
    "created_at": "2017-07-20T19:59:51Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350772",
    "user": "https://github.com/saraedum"
}
```

<a id='comment:34'>Comment 34:</a>
New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/71db2646349fc5a893404ffa89bd62b59100b8ea">71db264</a></td><td><code>Remove _parent,parent() distinction</code></td></tr></table>

---
New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/71db2646349fc5a893404ffa89bd62b59100b8ea">71db264</a></td><td><code>Remove _parent,parent() distinction</code></td></tr></table>




---

archive/issue_comments_350773.json:
```json
{
    "body": "Changed work issues from **consume old pickles, check that composite maps do not cause a speed regression, remove _parent hasattr, fix inverse image doctest** to **consume old pickles, check that composite maps do not cause a speed regression, fix inverse image doctest**",
    "created_at": "2017-07-20T19:59:51Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350773",
    "user": "https://github.com/saraedum"
}
```

Changed work issues from **consume old pickles, check that composite maps do not cause a speed regression, remove _parent hasattr, fix inverse image doctest** to **consume old pickles, check that composite maps do not cause a speed regression, fix inverse image doctest**



---

archive/issue_comments_350774.json:
```json
{
    "body": "Changed work issues from **consume old pickles, check that composite maps do not cause a speed regression, fix inverse image doctest** to **check that composite maps do not cause a speed regression, fix inverse image doctest**",
    "created_at": "2017-07-20T20:33:28Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350774",
    "user": "https://github.com/saraedum"
}
```

Changed work issues from **consume old pickles, check that composite maps do not cause a speed regression, fix inverse image doctest** to **check that composite maps do not cause a speed regression, fix inverse image doctest**



---

archive/issue_comments_350775.json:
```json
{
    "body": "<a id='comment:36'>Comment 36:</a>\nBranch pushed to git repo; I updated commit sha1. New commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/dbed916f8366217c3dd7735004e3aef7d0255968\">dbed916</a></td><td><code>Put RingHomomorphism_coercion back in</code></td></tr><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/1b5e6bb08288950a6f3cfa56f7c9760d7cdb5dc4\">1b5e6bb</a></td><td><code>fix typo</code></td></tr></table>\n",
    "created_at": "2017-07-20T20:35:46Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350775",
    "user": "https://github.com/sagetrac-git"
}
```

<a id='comment:36'>Comment 36:</a>
Branch pushed to git repo; I updated commit sha1. New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/dbed916f8366217c3dd7735004e3aef7d0255968">dbed916</a></td><td><code>Put RingHomomorphism_coercion back in</code></td></tr><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/1b5e6bb08288950a6f3cfa56f7c9760d7cdb5dc4">1b5e6bb</a></td><td><code>fix typo</code></td></tr></table>




---

archive/issue_comments_350776.json:
```json
{
    "body": "Changed commit from **[71db264](https://github.com/sagemath/sagetrac-mirror/commit/71db2646349fc5a893404ffa89bd62b59100b8ea)** to **[1b5e6bb](https://github.com/sagemath/sagetrac-mirror/commit/1b5e6bb08288950a6f3cfa56f7c9760d7cdb5dc4)**",
    "created_at": "2017-07-20T20:35:46Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350776",
    "user": "https://github.com/sagetrac-git"
}
```

Changed commit from **[71db264](https://github.com/sagemath/sagetrac-mirror/commit/71db2646349fc5a893404ffa89bd62b59100b8ea)** to **[1b5e6bb](https://github.com/sagemath/sagetrac-mirror/commit/1b5e6bb08288950a6f3cfa56f7c9760d7cdb5dc4)**



---

archive/issue_comments_350777.json:
```json
{
    "body": "Changed work issues from **check that composite maps do not cause a speed regression, fix inverse image doctest** to **check that composite maps do not cause a speed regression**",
    "created_at": "2017-07-20T20:35:57Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350777",
    "user": "https://github.com/saraedum"
}
```

Changed work issues from **check that composite maps do not cause a speed regression, fix inverse image doctest** to **check that composite maps do not cause a speed regression**



---

archive/issue_comments_350778.json:
```json
{
    "body": "<a id='comment:37'>Comment 37:</a>\nNew commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/dbed916f8366217c3dd7735004e3aef7d0255968\">dbed916</a></td><td><code>Put RingHomomorphism_coercion back in</code></td></tr><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/1b5e6bb08288950a6f3cfa56f7c9760d7cdb5dc4\">1b5e6bb</a></td><td><code>fix typo</code></td></tr></table>\n\n---\nNew commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/dbed916f8366217c3dd7735004e3aef7d0255968\">dbed916</a></td><td><code>Put RingHomomorphism_coercion back in</code></td></tr><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/1b5e6bb08288950a6f3cfa56f7c9760d7cdb5dc4\">1b5e6bb</a></td><td><code>fix typo</code></td></tr></table>\n",
    "created_at": "2017-07-20T20:35:57Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350778",
    "user": "https://github.com/saraedum"
}
```

<a id='comment:37'>Comment 37:</a>
New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/dbed916f8366217c3dd7735004e3aef7d0255968">dbed916</a></td><td><code>Put RingHomomorphism_coercion back in</code></td></tr><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/1b5e6bb08288950a6f3cfa56f7c9760d7cdb5dc4">1b5e6bb</a></td><td><code>fix typo</code></td></tr></table>

---
New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/dbed916f8366217c3dd7735004e3aef7d0255968">dbed916</a></td><td><code>Put RingHomomorphism_coercion back in</code></td></tr><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/1b5e6bb08288950a6f3cfa56f7c9760d7cdb5dc4">1b5e6bb</a></td><td><code>fix typo</code></td></tr></table>




---

archive/issue_comments_350779.json:
```json
{
    "body": "<a id='comment:38'>Comment 38:</a>\nBranch pushed to git repo; I updated commit sha1. New commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/20ec906aa59cfd9fadeac72399f224dbc77877c7\">20ec906</a></td><td><code>Speed up __nonzero__</code></td></tr></table>\n",
    "created_at": "2017-07-20T20:57:07Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350779",
    "user": "https://github.com/sagetrac-git"
}
```

<a id='comment:38'>Comment 38:</a>
Branch pushed to git repo; I updated commit sha1. New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/20ec906aa59cfd9fadeac72399f224dbc77877c7">20ec906</a></td><td><code>Speed up __nonzero__</code></td></tr></table>




---

archive/issue_comments_350780.json:
```json
{
    "body": "Changed commit from **[1b5e6bb](https://github.com/sagemath/sagetrac-mirror/commit/1b5e6bb08288950a6f3cfa56f7c9760d7cdb5dc4)** to **[20ec906](https://github.com/sagemath/sagetrac-mirror/commit/20ec906aa59cfd9fadeac72399f224dbc77877c7)**",
    "created_at": "2017-07-20T20:57:07Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350780",
    "user": "https://github.com/sagetrac-git"
}
```

Changed commit from **[1b5e6bb](https://github.com/sagemath/sagetrac-mirror/commit/1b5e6bb08288950a6f3cfa56f7c9760d7cdb5dc4)** to **[20ec906](https://github.com/sagemath/sagetrac-mirror/commit/20ec906aa59cfd9fadeac72399f224dbc77877c7)**



---

archive/issue_events_305376.json:
```json
{
    "actor": "https://github.com/saraedum",
    "created_at": "2017-07-20T20:57:18Z",
    "event": "unlabeled",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "label": "https://github.com/sagemath/sage/labels/needs%20work",
    "label_color": "ffff00",
    "label_name": "needs work",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/23204#event-305376"
}
```



---

archive/issue_events_305377.json:
```json
{
    "actor": "https://github.com/saraedum",
    "created_at": "2017-07-20T20:57:18Z",
    "event": "labeled",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "label": "https://github.com/sagemath/sage/labels/needs%20review",
    "label_color": "7fff00",
    "label_name": "needs review",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/23204#event-305377"
}
```



---

archive/issue_comments_350781.json:
```json
{
    "body": "<a id='comment:39'>Comment 39:</a>\nNew commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/20ec906aa59cfd9fadeac72399f224dbc77877c7\">20ec906</a></td><td><code>Speed up __nonzero__</code></td></tr></table>\n\n---\nNew commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/20ec906aa59cfd9fadeac72399f224dbc77877c7\">20ec906</a></td><td><code>Speed up __nonzero__</code></td></tr></table>\n",
    "created_at": "2017-07-20T20:57:18Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350781",
    "user": "https://github.com/saraedum"
}
```

<a id='comment:39'>Comment 39:</a>
New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/20ec906aa59cfd9fadeac72399f224dbc77877c7">20ec906</a></td><td><code>Speed up __nonzero__</code></td></tr></table>

---
New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/20ec906aa59cfd9fadeac72399f224dbc77877c7">20ec906</a></td><td><code>Speed up __nonzero__</code></td></tr></table>




---

archive/issue_comments_350782.json:
```json
{
    "body": "Changed work issues from **check that composite maps do not cause a speed regression** to none",
    "created_at": "2017-07-20T20:57:18Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350782",
    "user": "https://github.com/saraedum"
}
```

Changed work issues from **check that composite maps do not cause a speed regression** to none



---

archive/issue_comments_350783.json:
```json
{
    "body": "<a id='comment:40'>Comment 40:</a>\nReplying to [@tscrim](#comment%3A30):\n> Replying to [@roed314](#comment%3A28):\n> > Replying to [@tscrim](#comment%3A24):\n> > > Replying to [@saraedum](#comment%3A22):\n> > > > Replying to [@tscrim](#comment%3A21):\n> > > > > Replying to [@saraedum](#comment%3A20):\n> > > > > > David Roe and I looked at your comments.\n> > > > > > \n> > > > > > Replying to [@tscrim](#comment%3A17):\n> > > > > > > A strong -1 on calling the category (first): this is relatively expensive, not likely able to be implemented by the `MorphismMethods` of a category if you have to do formal compositions, and is meant to be overwritten. Really, if you want composite maps, then you should overwrite the `_composition_` hook.\n\n> > > > > > We don't understand what you are trying to say here. Could you rephrase your concern please.\n\n> > > > > \n> > > > > You added two blocks for formal composite functions:\n> > > > > [snip]\n> > > > > What I am saying is this is really (relatively) expensive for something that is almost certainly going to be implemented. If your category has morphisms such that you cannot effectively simplify a composition `f \\circ g` to a single map `h`, then I don't see how you would have enough (generic) information to check that the composition is injective/surjective. So I doubt this would ever get used.\n\n> > > > I do not understand how this relates to `_composition_`. The point here is simply that in the category of Rings, a morphism that has a field as its domain, knows that it is injective. However, we can not so easily turn the composition into one morphism.\n\n> > > \n> > > So you're doing implementing that at the category level? I feel like for something like this you should have a generic field morphism class that knows outright that it is injective and implements a custom `_composition_` that returns the same class because it is completely determined by the image of `1`. So the composition would amount to chasing through each map what happens to `1`. If you are using some more specialized morphisms and need the generic composition, then let the generic code of the composition do its thing. I don't see the point of slowing everything else down for a special case that is more effectively dealt with by a concrete class.\n\n> > \n> > Morphisms from fields aren't completely determined by where they send 1 (they have to send 1 to 1).  For example, complex conjugation is a morphism of fields.  Moreover, fields can embed into lots of kinds of rings that aren't fields, and you don't want to require a concrete class for each of these situations.  One of the reasons for the existence of the category framework is to enable writing generic code like this.\n\n> \n> Ah, right. However, you are not doing general code, you are doing a special case, which IMO is a relatively small set of the cases, in a concrete class. Because of that, it should be a separate class if you really need that. To me, the category framework is primarily for general code that is *not* implemented in a concrete class, but can be used as a fallback, and often times, it is slow. I strongly believe that there is no practical advantage in any situation to having to do a Python call up to through the category framework instead of just letting the composite map do its thing. Moreover, I believe there will be a penalty for this extra (useless) call up through the category in general; there is typically not enough structure to say anything.\n\nWe are removing a wrapper class. This should make things like `__call__` faster which is probably much more important. Also, in this case there is enough structure in the category of rings to say interesting things.\n\n> > I think we should create a followup ticket to remove `RingMap_lift`, since I think it will take a nontrivial amount of work.\n\n> Please create it and cc me on it; I will be happy to review it.\n\n#23500.\n\n> > > > > > > I am a little wary of removing `RingHomomorphism.__nonzero__` as that specialized code does do a check that could be faster and might be a little better behaved because no equality checking is involved. I just want to make sure you really checked this doesn't introduce a speed regression and works well for inexact rings (which may not be fully doctested).\n\n> > > > > > We think that you will never get to the `==` check in the new implementation in the case of ring homomorphism (unless the codomain is trivial). We checked a few cases and there was no speed regression.\n\n> > > > > That does not seem possible. If the ring homset has no zero, then is relatively expensive because of the throwing and catching of an error and is something done every time. If there is a zero map, then it should only affect the first time it is called, but then it has to do a comparison because of the default `Element.__nonzero__`. What were the cases you tested?\n\n> > > > Sorry, I had misread your initial comment. There is probably a speed regression but when would this possibly be relevant (i.e., when do you create lots of rings and check that morphisms between them are nonzero? without knowing that you are talking about rings.)\n> > > > \n> > > > ```\n> > > > sage: f = ZZ.hom(Zp(3))\n> > > > sage: %timeit bool(f)\n> > > > 1000000 loops, best of 3: 212 ns per loop # before\n> > > > 10000 loops, best of 3: 25.5 \u00b5s per loop # after\n> > > > ```\n\n> > > \n> > > I don't have a use case for it, but I do not see the point in deliberately introducing a regression by removing code that is (was?) working perfectly well. Someone might have a use case for it; I know people create lots of polynomial rings over finite fields as the field size varies, and they might be doing a check for a nonzero map in that code.\n\n> > \n> > If you're creating polynomial rings over finite fields, then the morphism can never be zero so you won't be calling this code.  The only time you can have a zero morphism is if the codomain is the zero ring.  It seems unlikely that you're going to be testing this enough that the difference between `200ns` and `20\u00b5s`\n> > \n> > The benefit is that we are now able to use a general framework for morphisms between rings that don't inherit from `RingHomomorphism`, for example the coercion map from `ZZ` to `QQ`.  In my opinion, this is valuable enough to merit the speed regression in this case.\n\n> \n> This is not an argument for removing `__nonzero__`; only for having the coercion maps not be a subclass of `RingHomomorphism`. If they don't inherit from `RingHomomorphism`, then fine, they do not benefit. However, what about all of those classes that do? You are penalizing them for no reason. Also, the category framework is in some sense a (sequence of) abstract (Python) base classes; if you need to have your own hierarchy of (C/Python) classes, then there is no need to try to force things up to the category level.\n\nWe have put it back in.\n\n> > Finally, Julian and I talked about it and we don't see an easy way to add the custom code back in.  Since `__nonzero__` is a double underscore method, we can't use `cached_method` there, and you can't productively use `cached_method` on `zero` since it won't cache the fact that it raised an error.  Moreover, if we try to implement this in the category, then in order to  hook code into `__nonzero__` we have to make it slower for all other elements, which doesn't seem reasonable.\n\n> \n> Huh? You cannot use a `@cached_method` only because it is a Cython class, which needs to handle the special double underscore methods differently. For Python classes, you can use `@cached_method` on, e.g., `__nonzero__`. However, this is unrelated because there is/was no caching going on.\n\nOk. We were just discussing ways of making it faster. We have implemented one.\n\n> Lastly, just as a word of caution, moving things up into a category forces them to be Python code, which is inherently slower than Cython code. Also, what is your end-game with the ring morphism code? Do you have a plan for what you want it to be like when all the changes are done?\n\nThe idea is not to require morphisms of rings to inherit form `RingHomomorphism`. They should mostly inherit from whatever makes their implementation easiest. So, we certainly do not want to get rid of classes such as `RingHomomorphism_im_gens`.",
    "created_at": "2017-07-20T21:09:44Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350783",
    "user": "https://github.com/saraedum"
}
```

<a id='comment:40'>Comment 40:</a>
Replying to [@tscrim](#comment%3A30):
> Replying to [@roed314](#comment%3A28):
> > Replying to [@tscrim](#comment%3A24):
> > > Replying to [@saraedum](#comment%3A22):
> > > > Replying to [@tscrim](#comment%3A21):
> > > > > Replying to [@saraedum](#comment%3A20):
> > > > > > David Roe and I looked at your comments.
> > > > > > 
> > > > > > Replying to [@tscrim](#comment%3A17):
> > > > > > > A strong -1 on calling the category (first): this is relatively expensive, not likely able to be implemented by the `MorphismMethods` of a category if you have to do formal compositions, and is meant to be overwritten. Really, if you want composite maps, then you should overwrite the `_composition_` hook.

> > > > > > We don't understand what you are trying to say here. Could you rephrase your concern please.

> > > > > 
> > > > > You added two blocks for formal composite functions:
> > > > > [snip]
> > > > > What I am saying is this is really (relatively) expensive for something that is almost certainly going to be implemented. If your category has morphisms such that you cannot effectively simplify a composition `f \circ g` to a single map `h`, then I don't see how you would have enough (generic) information to check that the composition is injective/surjective. So I doubt this would ever get used.

> > > > I do not understand how this relates to `_composition_`. The point here is simply that in the category of Rings, a morphism that has a field as its domain, knows that it is injective. However, we can not so easily turn the composition into one morphism.

> > > 
> > > So you're doing implementing that at the category level? I feel like for something like this you should have a generic field morphism class that knows outright that it is injective and implements a custom `_composition_` that returns the same class because it is completely determined by the image of `1`. So the composition would amount to chasing through each map what happens to `1`. If you are using some more specialized morphisms and need the generic composition, then let the generic code of the composition do its thing. I don't see the point of slowing everything else down for a special case that is more effectively dealt with by a concrete class.

> > 
> > Morphisms from fields aren't completely determined by where they send 1 (they have to send 1 to 1).  For example, complex conjugation is a morphism of fields.  Moreover, fields can embed into lots of kinds of rings that aren't fields, and you don't want to require a concrete class for each of these situations.  One of the reasons for the existence of the category framework is to enable writing generic code like this.

> 
> Ah, right. However, you are not doing general code, you are doing a special case, which IMO is a relatively small set of the cases, in a concrete class. Because of that, it should be a separate class if you really need that. To me, the category framework is primarily for general code that is *not* implemented in a concrete class, but can be used as a fallback, and often times, it is slow. I strongly believe that there is no practical advantage in any situation to having to do a Python call up to through the category framework instead of just letting the composite map do its thing. Moreover, I believe there will be a penalty for this extra (useless) call up through the category in general; there is typically not enough structure to say anything.

We are removing a wrapper class. This should make things like `__call__` faster which is probably much more important. Also, in this case there is enough structure in the category of rings to say interesting things.

> > I think we should create a followup ticket to remove `RingMap_lift`, since I think it will take a nontrivial amount of work.

> Please create it and cc me on it; I will be happy to review it.

#23500.

> > > > > > > I am a little wary of removing `RingHomomorphism.__nonzero__` as that specialized code does do a check that could be faster and might be a little better behaved because no equality checking is involved. I just want to make sure you really checked this doesn't introduce a speed regression and works well for inexact rings (which may not be fully doctested).

> > > > > > We think that you will never get to the `==` check in the new implementation in the case of ring homomorphism (unless the codomain is trivial). We checked a few cases and there was no speed regression.

> > > > > That does not seem possible. If the ring homset has no zero, then is relatively expensive because of the throwing and catching of an error and is something done every time. If there is a zero map, then it should only affect the first time it is called, but then it has to do a comparison because of the default `Element.__nonzero__`. What were the cases you tested?

> > > > Sorry, I had misread your initial comment. There is probably a speed regression but when would this possibly be relevant (i.e., when do you create lots of rings and check that morphisms between them are nonzero? without knowing that you are talking about rings.)
> > > > 
> > > > ```
> > > > sage: f = ZZ.hom(Zp(3))
> > > > sage: %timeit bool(f)
> > > > 1000000 loops, best of 3: 212 ns per loop # before
> > > > 10000 loops, best of 3: 25.5 µs per loop # after
> > > > ```

> > > 
> > > I don't have a use case for it, but I do not see the point in deliberately introducing a regression by removing code that is (was?) working perfectly well. Someone might have a use case for it; I know people create lots of polynomial rings over finite fields as the field size varies, and they might be doing a check for a nonzero map in that code.

> > 
> > If you're creating polynomial rings over finite fields, then the morphism can never be zero so you won't be calling this code.  The only time you can have a zero morphism is if the codomain is the zero ring.  It seems unlikely that you're going to be testing this enough that the difference between `200ns` and `20µs`
> > 
> > The benefit is that we are now able to use a general framework for morphisms between rings that don't inherit from `RingHomomorphism`, for example the coercion map from `ZZ` to `QQ`.  In my opinion, this is valuable enough to merit the speed regression in this case.

> 
> This is not an argument for removing `__nonzero__`; only for having the coercion maps not be a subclass of `RingHomomorphism`. If they don't inherit from `RingHomomorphism`, then fine, they do not benefit. However, what about all of those classes that do? You are penalizing them for no reason. Also, the category framework is in some sense a (sequence of) abstract (Python) base classes; if you need to have your own hierarchy of (C/Python) classes, then there is no need to try to force things up to the category level.

We have put it back in.

> > Finally, Julian and I talked about it and we don't see an easy way to add the custom code back in.  Since `__nonzero__` is a double underscore method, we can't use `cached_method` there, and you can't productively use `cached_method` on `zero` since it won't cache the fact that it raised an error.  Moreover, if we try to implement this in the category, then in order to  hook code into `__nonzero__` we have to make it slower for all other elements, which doesn't seem reasonable.

> 
> Huh? You cannot use a `@cached_method` only because it is a Cython class, which needs to handle the special double underscore methods differently. For Python classes, you can use `@cached_method` on, e.g., `__nonzero__`. However, this is unrelated because there is/was no caching going on.

Ok. We were just discussing ways of making it faster. We have implemented one.

> Lastly, just as a word of caution, moving things up into a category forces them to be Python code, which is inherently slower than Cython code. Also, what is your end-game with the ring morphism code? Do you have a plan for what you want it to be like when all the changes are done?

The idea is not to require morphisms of rings to inherit form `RingHomomorphism`. They should mostly inherit from whatever makes their implementation easiest. So, we certainly do not want to get rid of classes such as `RingHomomorphism_im_gens`.



---

archive/issue_comments_350784.json:
```json
{
    "body": "Changed commit from **[20ec906](https://github.com/sagemath/sagetrac-mirror/commit/20ec906aa59cfd9fadeac72399f224dbc77877c7)** to **[4eb236d](https://github.com/sagemath/sagetrac-mirror/commit/4eb236d12dad79e37093b80283ab05b8c5945245)**",
    "created_at": "2017-07-20T21:20:03Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350784",
    "user": "https://github.com/sagetrac-git"
}
```

Changed commit from **[20ec906](https://github.com/sagemath/sagetrac-mirror/commit/20ec906aa59cfd9fadeac72399f224dbc77877c7)** to **[4eb236d](https://github.com/sagemath/sagetrac-mirror/commit/4eb236d12dad79e37093b80283ab05b8c5945245)**



---

archive/issue_comments_350785.json:
```json
{
    "body": "<a id='comment:41'>Comment 41:</a>\nBranch pushed to git repo; I updated commit sha1. New commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/99669ca11422db92be367d1ecdc3acdb98c3c8f9\">99669ca</a></td><td><code>remove tabs</code></td></tr><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/4eb236d12dad79e37093b80283ab05b8c5945245\">4eb236d</a></td><td><code>fix doctests</code></td></tr></table>\n",
    "created_at": "2017-07-20T21:20:03Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350785",
    "user": "https://github.com/sagetrac-git"
}
```

<a id='comment:41'>Comment 41:</a>
Branch pushed to git repo; I updated commit sha1. New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/99669ca11422db92be367d1ecdc3acdb98c3c8f9">99669ca</a></td><td><code>remove tabs</code></td></tr><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/4eb236d12dad79e37093b80283ab05b8c5945245">4eb236d</a></td><td><code>fix doctests</code></td></tr></table>




---

archive/issue_events_305378.json:
```json
{
    "actor": "https://github.com/saraedum",
    "created_at": "2017-07-20T22:35:45Z",
    "event": "unlabeled",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "label": "https://github.com/sagemath/sage/labels/needs%20review",
    "label_color": "7fff00",
    "label_name": "needs review",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/23204#event-305378"
}
```



---

archive/issue_events_305379.json:
```json
{
    "actor": "https://github.com/saraedum",
    "created_at": "2017-07-20T22:35:45Z",
    "event": "labeled",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "label": "https://github.com/sagemath/sage/labels/needs%20work",
    "label_color": "ffff00",
    "label_name": "needs work",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/23204#event-305379"
}
```



---

archive/issue_comments_350786.json:
```json
{
    "body": "Work Issues: **failing doctests**",
    "created_at": "2017-07-20T22:35:45Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350786",
    "user": "https://github.com/saraedum"
}
```

Work Issues: **failing doctests**



---

archive/issue_comments_350787.json:
```json
{
    "body": "<a id='comment:43'>Comment 43:</a>\nReplying to [@saraedum](#comment%3A40):\n> Replying to [@tscrim](#comment%3A30):\n> > Replying to [@roed314](#comment%3A28):\n> > > Morphisms from fields aren't completely determined by where they send 1 (they have to send 1 to 1).  For example, complex conjugation is a morphism of fields.  Moreover, fields can embed into lots of kinds of rings that aren't fields, and you don't want to require a concrete class for each of these situations.  One of the reasons for the existence of the category framework is to enable writing generic code like this.\n\n> > \n> > Ah, right. However, you are not doing general code, you are doing a special case, which IMO is a relatively small set of the cases, in a concrete class. Because of that, it should be a separate class if you really need that. To me, the category framework is primarily for general code that is *not* implemented in a concrete class, but can be used as a fallback, and often times, it is slow. I strongly believe that there is no practical advantage in any situation to having to do a Python call up to through the category framework instead of just letting the composite map do its thing. Moreover, I believe there will be a penalty for this extra (useless) call up through the category in general; there is typically not enough structure to say anything.\n\n> We are removing a wrapper class. This should make things like `__call__` faster which is probably much more important. Also, in this case there is enough structure in the category of rings to say interesting things.\n\nFields, yes; rings, I am not so sure. I work in categories with even less structure (Lie algebras), but still could see formal composite morphisms. Also, the wrapper class only did two *cython* function calls, which should be faster that the extra Python call(s) + MRO search, especially when there is no such method. I still strongly believe very few categories will ever be able to implement a generic `is_injective`/`is_surjective` to make this worthwhile.\n\n> > > Finally, Julian and I talked about it and we don't see an easy way to add the custom code back in.  Since `__nonzero__` is a double underscore method, we can't use `cached_method` there, and you can't productively use `cached_method` on `zero` since it won't cache the fact that it raised an error.  Moreover, if we try to implement this in the category, then in order to  hook code into `__nonzero__` we have to make it slower for all other elements, which doesn't seem reasonable.\n\n> > \n> > Huh? You cannot use a `@cached_method` only because it is a Cython class, which needs to handle the special double underscore methods differently. For Python classes, you can use `@cached_method` on, e.g., `__nonzero__`. However, this is unrelated because there is/was no caching going on.\n\n> Ok. We were just discussing ways of making it faster. We have implemented one.\n\nDid you try just implementing `__nonzero__` for the category? I forget if this works with extension classes and special methods. There doesn't seem to be an implementation of `__nonzero__` otherwise in the MRO.\n\n> > Lastly, just as a word of caution, moving things up into a category forces them to be Python code, which is inherently slower than Cython code. Also, what is your end-game with the ring morphism code? Do you have a plan for what you want it to be like when all the changes are done?\n\n> The idea is not to require morphisms of rings to inherit form `RingHomomorphism`. They should mostly inherit from whatever makes their implementation easiest. So, we certainly do not want to get rid of classes such as `RingHomomorphism_im_gens`.\n\nThere are some good reasons to have these concrete classes, with some stemming from a general lack of structure of ring morphisms. In some ways, by removing `RingHomomorphism_coercion`, we have made a problem for ourselves because coercion maps become the only maps not to inherit from `RingHomomorphism`. We can easily make it a requirement that morphisms inherit from `RingHomomorphism` as really nothing is (currently) implemented across multiple categories. I guess my question is do you see some larger hierarchy of classes that will run parallel to the category hierarchy?",
    "created_at": "2017-07-20T23:50:33Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350787",
    "user": "https://github.com/tscrim"
}
```

<a id='comment:43'>Comment 43:</a>
Replying to [@saraedum](#comment%3A40):
> Replying to [@tscrim](#comment%3A30):
> > Replying to [@roed314](#comment%3A28):
> > > Morphisms from fields aren't completely determined by where they send 1 (they have to send 1 to 1).  For example, complex conjugation is a morphism of fields.  Moreover, fields can embed into lots of kinds of rings that aren't fields, and you don't want to require a concrete class for each of these situations.  One of the reasons for the existence of the category framework is to enable writing generic code like this.

> > 
> > Ah, right. However, you are not doing general code, you are doing a special case, which IMO is a relatively small set of the cases, in a concrete class. Because of that, it should be a separate class if you really need that. To me, the category framework is primarily for general code that is *not* implemented in a concrete class, but can be used as a fallback, and often times, it is slow. I strongly believe that there is no practical advantage in any situation to having to do a Python call up to through the category framework instead of just letting the composite map do its thing. Moreover, I believe there will be a penalty for this extra (useless) call up through the category in general; there is typically not enough structure to say anything.

> We are removing a wrapper class. This should make things like `__call__` faster which is probably much more important. Also, in this case there is enough structure in the category of rings to say interesting things.

Fields, yes; rings, I am not so sure. I work in categories with even less structure (Lie algebras), but still could see formal composite morphisms. Also, the wrapper class only did two *cython* function calls, which should be faster that the extra Python call(s) + MRO search, especially when there is no such method. I still strongly believe very few categories will ever be able to implement a generic `is_injective`/`is_surjective` to make this worthwhile.

> > > Finally, Julian and I talked about it and we don't see an easy way to add the custom code back in.  Since `__nonzero__` is a double underscore method, we can't use `cached_method` there, and you can't productively use `cached_method` on `zero` since it won't cache the fact that it raised an error.  Moreover, if we try to implement this in the category, then in order to  hook code into `__nonzero__` we have to make it slower for all other elements, which doesn't seem reasonable.

> > 
> > Huh? You cannot use a `@cached_method` only because it is a Cython class, which needs to handle the special double underscore methods differently. For Python classes, you can use `@cached_method` on, e.g., `__nonzero__`. However, this is unrelated because there is/was no caching going on.

> Ok. We were just discussing ways of making it faster. We have implemented one.

Did you try just implementing `__nonzero__` for the category? I forget if this works with extension classes and special methods. There doesn't seem to be an implementation of `__nonzero__` otherwise in the MRO.

> > Lastly, just as a word of caution, moving things up into a category forces them to be Python code, which is inherently slower than Cython code. Also, what is your end-game with the ring morphism code? Do you have a plan for what you want it to be like when all the changes are done?

> The idea is not to require morphisms of rings to inherit form `RingHomomorphism`. They should mostly inherit from whatever makes their implementation easiest. So, we certainly do not want to get rid of classes such as `RingHomomorphism_im_gens`.

There are some good reasons to have these concrete classes, with some stemming from a general lack of structure of ring morphisms. In some ways, by removing `RingHomomorphism_coercion`, we have made a problem for ourselves because coercion maps become the only maps not to inherit from `RingHomomorphism`. We can easily make it a requirement that morphisms inherit from `RingHomomorphism` as really nothing is (currently) implemented across multiple categories. I guess my question is do you see some larger hierarchy of classes that will run parallel to the category hierarchy?



---

archive/issue_comments_350788.json:
```json
{
    "body": "<a id='comment:44'>Comment 44:</a>\nReplying to [@tscrim](#comment%3A43):\n> Replying to [@saraedum](#comment%3A40):\n> > Replying to [@tscrim](#comment%3A30):\n> > > Replying to [@roed314](#comment%3A28):\n> > > > Morphisms from fields aren't completely determined by where they send 1 (they have to send 1 to 1).  For example, complex conjugation is a morphism of fields.  Moreover, fields can embed into lots of kinds of rings that aren't fields, and you don't want to require a concrete class for each of these situations.  One of the reasons for the existence of the category framework is to enable writing generic code like this.\n\n> > > \n> > > Ah, right. However, you are not doing general code, you are doing a special case, which IMO is a relatively small set of the cases, in a concrete class. Because of that, it should be a separate class if you really need that. To me, the category framework is primarily for general code that is *not* implemented in a concrete class, but can be used as a fallback, and often times, it is slow. I strongly believe that there is no practical advantage in any situation to having to do a Python call up to through the category framework instead of just letting the composite map do its thing. Moreover, I believe there will be a penalty for this extra (useless) call up through the category in general; there is typically not enough structure to say anything.\n\n> > We are removing a wrapper class. This should make things like `__call__` faster which is probably much more important. Also, in this case there is enough structure in the category of rings to say interesting things.\n\n> \n> Fields, yes; rings, I am not so sure. I work in categories with even less structure (Lie algebras), but still could see formal composite morphisms. Also, the wrapper class only did two *cython* function calls, which should be faster that the extra Python call(s) + MRO search, especially when there is no such method. \n\nOf course we're not going to beat two cython function calls, but I don't think that that should be the standard.  There are times when speed regressions are acceptable.\n\n> I still strongly believe very few categories will ever be able to implement a generic `is_injective`/`is_surjective` to make this worthwhile.\n\nI don't see how it matters whether there are other categories that can do so.  It's possible in `Rings()`, and there are multiple tickets waiting on this functionality.  How does removing `RingHomomorphism_coercion` affect other categories anyway?\n\n> > > Lastly, just as a word of caution, moving things up into a category forces them to be Python code, which is inherently slower than Cython code. Also, what is your end-game with the ring morphism code? Do you have a plan for what you want it to be like when all the changes are done?\n\n> > The idea is not to require morphisms of rings to inherit form `RingHomomorphism`. They should mostly inherit from whatever makes their implementation easiest. So, we certainly do not want to get rid of classes such as `RingHomomorphism_im_gens`.\n\n> \n> There are some good reasons to have these concrete classes, with some stemming from a general lack of structure of ring morphisms. In some ways, by removing `RingHomomorphism_coercion`, we have made a problem for ourselves because coercion maps become the only maps not to inherit from `RingHomomorphism`. We can easily make it a requirement that morphisms inherit from `RingHomomorphism` as really nothing is (currently) implemented across multiple categories. I guess my question is do you see some larger hierarchy of classes that will run parallel to the category hierarchy?\n\n`DefaultConvertMap` is implemented across multiple categories.  The coercion between `ZZ` and `QQ` does not inherit from `RingHomomorphism`.  I suspect that there are others across Sage.",
    "created_at": "2017-07-21T00:53:56Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350788",
    "user": "https://github.com/roed314"
}
```

<a id='comment:44'>Comment 44:</a>
Replying to [@tscrim](#comment%3A43):
> Replying to [@saraedum](#comment%3A40):
> > Replying to [@tscrim](#comment%3A30):
> > > Replying to [@roed314](#comment%3A28):
> > > > Morphisms from fields aren't completely determined by where they send 1 (they have to send 1 to 1).  For example, complex conjugation is a morphism of fields.  Moreover, fields can embed into lots of kinds of rings that aren't fields, and you don't want to require a concrete class for each of these situations.  One of the reasons for the existence of the category framework is to enable writing generic code like this.

> > > 
> > > Ah, right. However, you are not doing general code, you are doing a special case, which IMO is a relatively small set of the cases, in a concrete class. Because of that, it should be a separate class if you really need that. To me, the category framework is primarily for general code that is *not* implemented in a concrete class, but can be used as a fallback, and often times, it is slow. I strongly believe that there is no practical advantage in any situation to having to do a Python call up to through the category framework instead of just letting the composite map do its thing. Moreover, I believe there will be a penalty for this extra (useless) call up through the category in general; there is typically not enough structure to say anything.

> > We are removing a wrapper class. This should make things like `__call__` faster which is probably much more important. Also, in this case there is enough structure in the category of rings to say interesting things.

> 
> Fields, yes; rings, I am not so sure. I work in categories with even less structure (Lie algebras), but still could see formal composite morphisms. Also, the wrapper class only did two *cython* function calls, which should be faster that the extra Python call(s) + MRO search, especially when there is no such method. 

Of course we're not going to beat two cython function calls, but I don't think that that should be the standard.  There are times when speed regressions are acceptable.

> I still strongly believe very few categories will ever be able to implement a generic `is_injective`/`is_surjective` to make this worthwhile.

I don't see how it matters whether there are other categories that can do so.  It's possible in `Rings()`, and there are multiple tickets waiting on this functionality.  How does removing `RingHomomorphism_coercion` affect other categories anyway?

> > > Lastly, just as a word of caution, moving things up into a category forces them to be Python code, which is inherently slower than Cython code. Also, what is your end-game with the ring morphism code? Do you have a plan for what you want it to be like when all the changes are done?

> > The idea is not to require morphisms of rings to inherit form `RingHomomorphism`. They should mostly inherit from whatever makes their implementation easiest. So, we certainly do not want to get rid of classes such as `RingHomomorphism_im_gens`.

> 
> There are some good reasons to have these concrete classes, with some stemming from a general lack of structure of ring morphisms. In some ways, by removing `RingHomomorphism_coercion`, we have made a problem for ourselves because coercion maps become the only maps not to inherit from `RingHomomorphism`. We can easily make it a requirement that morphisms inherit from `RingHomomorphism` as really nothing is (currently) implemented across multiple categories. I guess my question is do you see some larger hierarchy of classes that will run parallel to the category hierarchy?

`DefaultConvertMap` is implemented across multiple categories.  The coercion between `ZZ` and `QQ` does not inherit from `RingHomomorphism`.  I suspect that there are others across Sage.



---

archive/issue_comments_350789.json:
```json
{
    "body": "Changed commit from **[4eb236d](https://github.com/sagemath/sagetrac-mirror/commit/4eb236d12dad79e37093b80283ab05b8c5945245)** to **[e891a88](https://github.com/sagemath/sagetrac-mirror/commit/e891a884f8eca9e1e7d226306cb79b9cd827ee8b)**",
    "created_at": "2017-07-21T02:16:25Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350789",
    "user": "https://github.com/sagetrac-git"
}
```

Changed commit from **[4eb236d](https://github.com/sagemath/sagetrac-mirror/commit/4eb236d12dad79e37093b80283ab05b8c5945245)** to **[e891a88](https://github.com/sagemath/sagetrac-mirror/commit/e891a884f8eca9e1e7d226306cb79b9cd827ee8b)**



---

archive/issue_comments_350790.json:
```json
{
    "body": "<a id='comment:45'>Comment 45:</a>\nBranch pushed to git repo; I updated commit sha1. New commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/626e7931dd126b3b070b5681e624ae942a291abf\">626e793</a></td><td><code>Fix doctest</code></td></tr><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/e891a884f8eca9e1e7d226306cb79b9cd827ee8b\">e891a88</a></td><td><code>Replace isinstance(RingHomomorphism)</code></td></tr></table>\n",
    "created_at": "2017-07-21T02:16:25Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350790",
    "user": "https://github.com/sagetrac-git"
}
```

<a id='comment:45'>Comment 45:</a>
Branch pushed to git repo; I updated commit sha1. New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/626e7931dd126b3b070b5681e624ae942a291abf">626e793</a></td><td><code>Fix doctest</code></td></tr><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/e891a884f8eca9e1e7d226306cb79b9cd827ee8b">e891a88</a></td><td><code>Replace isinstance(RingHomomorphism)</code></td></tr></table>




---

archive/issue_comments_350791.json:
```json
{
    "body": "Changed work issues from **failing doctests** to none",
    "created_at": "2017-07-21T02:22:24Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350791",
    "user": "https://github.com/saraedum"
}
```

Changed work issues from **failing doctests** to none



---

archive/issue_events_305380.json:
```json
{
    "actor": "https://github.com/saraedum",
    "created_at": "2017-07-21T02:22:24Z",
    "event": "unlabeled",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "label": "https://github.com/sagemath/sage/labels/needs%20work",
    "label_color": "ffff00",
    "label_name": "needs work",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/23204#event-305380"
}
```



---

archive/issue_events_305381.json:
```json
{
    "actor": "https://github.com/saraedum",
    "created_at": "2017-07-21T02:22:24Z",
    "event": "labeled",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "label": "https://github.com/sagemath/sage/labels/needs%20review",
    "label_color": "7fff00",
    "label_name": "needs review",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/23204#event-305381"
}
```



---

archive/issue_comments_350792.json:
```json
{
    "body": "<a id='comment:47'>Comment 47:</a>\nReplying to [@roed314](#comment%3A44):\n> Replying to [@tscrim](#comment%3A43):\n> > Replying to [@saraedum](#comment%3A40):\n> > > Replying to [@tscrim](#comment%3A30):\n> > > > Replying to [@roed314](#comment%3A28):\n> > > > > Morphisms from fields aren't completely determined by where they send 1 (they have to send 1 to 1).  For example, complex conjugation is a morphism of fields.  Moreover, fields can embed into lots of kinds of rings that aren't fields, and you don't want to require a concrete class for each of these situations.  One of the reasons for the existence of the category framework is to enable writing generic code like this.\n\n> > > > \n> > > > Ah, right. However, you are not doing general code, you are doing a special case, which IMO is a relatively small set of the cases, in a concrete class. Because of that, it should be a separate class if you really need that. To me, the category framework is primarily for general code that is *not* implemented in a concrete class, but can be used as a fallback, and often times, it is slow. I strongly believe that there is no practical advantage in any situation to having to do a Python call up to through the category framework instead of just letting the composite map do its thing. Moreover, I believe there will be a penalty for this extra (useless) call up through the category in general; there is typically not enough structure to say anything.\n\n> > > We are removing a wrapper class. This should make things like `__call__` faster which is probably much more important. Also, in this case there is enough structure in the category of rings to say interesting things.\n\n> > \n> > Fields, yes; rings, I am not so sure. I work in categories with even less structure (Lie algebras), but still could see formal composite morphisms. Also, the wrapper class only did two *cython* function calls, which should be faster that the extra Python call(s) + MRO search, especially when there is no such method. \n\n> \n> Of course we're not going to beat two cython function calls, but I don't think that that should be the standard.  There are times when speed regressions are acceptable.\n\nYes, but this is not one of those times IMO.\n\n> > I still strongly believe very few categories will ever be able to implement a generic `is_injective`/`is_surjective` to make this worthwhile.\n\n> \n> I don't see how it matters whether there are other categories that can do so.  It's possible in `Rings()`, and there are multiple tickets waiting on this functionality.  How does removing `RingHomomorphism_coercion` affect other categories anyway?\n\nRemoving that does not. Yet, the addition of the extra code to go up through the category framework in the `FormalCompositeMap` does because that is a very general construction. It is also not even possible in `Rings()` to implement a general `is_injective` and `is_surjective`; only the first one for `Fields`. I also see `FormalCompositeMap` as a concrete implementation of a specific type of morphism, and so it is under no obligation to do things from the category.\n\n> > > > Lastly, just as a word of caution, moving things up into a category forces them to be Python code, which is inherently slower than Cython code. Also, what is your end-game with the ring morphism code? Do you have a plan for what you want it to be like when all the changes are done?\n\n> > > The idea is not to require morphisms of rings to inherit form `RingHomomorphism`. They should mostly inherit from whatever makes their implementation easiest. So, we certainly do not want to get rid of classes such as `RingHomomorphism_im_gens`.\n\n> > \n> > There are some good reasons to have these concrete classes, with some stemming from a general lack of structure of ring morphisms. In some ways, by removing `RingHomomorphism_coercion`, we have made a problem for ourselves because coercion maps become the only maps not to inherit from `RingHomomorphism`. We can easily make it a requirement that morphisms inherit from `RingHomomorphism` as really nothing is (currently) implemented across multiple categories. I guess my question is do you see some larger hierarchy of classes that will run parallel to the category hierarchy?\n\n> \n> `DefaultConvertMap` is implemented across multiple categories.\n\nDo you mean used in multiple classes in multiple categories? However, there are no subclasses or other parallel implementations AFAIK.\n\n>  The coercion between `ZZ` and `QQ` does not inherit from `RingHomomorphism`.  I suspect that there are others across Sage.\n\nThe `ZZ -> QQ` probably should inherit from `RingHomomorphism`. I would say any others that are ring morphisms should inherit for both uniformity and to take advantage of that code. There is no cost to this unless the plan is to remove `RingHomomorphism` altogether as an ABC (I am not sure this is the best thing going forward, but I haven't really thought about it yet).\n\nI do support adding extra default implementations in the category, but I do not support always forcing concrete classes to make any sort of super calls, more so as the first attempted action.\n\nFrom the patchbot, there are still failing doctests and the documentation does not build.",
    "created_at": "2017-07-21T15:32:56Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350792",
    "user": "https://github.com/tscrim"
}
```

<a id='comment:47'>Comment 47:</a>
Replying to [@roed314](#comment%3A44):
> Replying to [@tscrim](#comment%3A43):
> > Replying to [@saraedum](#comment%3A40):
> > > Replying to [@tscrim](#comment%3A30):
> > > > Replying to [@roed314](#comment%3A28):
> > > > > Morphisms from fields aren't completely determined by where they send 1 (they have to send 1 to 1).  For example, complex conjugation is a morphism of fields.  Moreover, fields can embed into lots of kinds of rings that aren't fields, and you don't want to require a concrete class for each of these situations.  One of the reasons for the existence of the category framework is to enable writing generic code like this.

> > > > 
> > > > Ah, right. However, you are not doing general code, you are doing a special case, which IMO is a relatively small set of the cases, in a concrete class. Because of that, it should be a separate class if you really need that. To me, the category framework is primarily for general code that is *not* implemented in a concrete class, but can be used as a fallback, and often times, it is slow. I strongly believe that there is no practical advantage in any situation to having to do a Python call up to through the category framework instead of just letting the composite map do its thing. Moreover, I believe there will be a penalty for this extra (useless) call up through the category in general; there is typically not enough structure to say anything.

> > > We are removing a wrapper class. This should make things like `__call__` faster which is probably much more important. Also, in this case there is enough structure in the category of rings to say interesting things.

> > 
> > Fields, yes; rings, I am not so sure. I work in categories with even less structure (Lie algebras), but still could see formal composite morphisms. Also, the wrapper class only did two *cython* function calls, which should be faster that the extra Python call(s) + MRO search, especially when there is no such method. 

> 
> Of course we're not going to beat two cython function calls, but I don't think that that should be the standard.  There are times when speed regressions are acceptable.

Yes, but this is not one of those times IMO.

> > I still strongly believe very few categories will ever be able to implement a generic `is_injective`/`is_surjective` to make this worthwhile.

> 
> I don't see how it matters whether there are other categories that can do so.  It's possible in `Rings()`, and there are multiple tickets waiting on this functionality.  How does removing `RingHomomorphism_coercion` affect other categories anyway?

Removing that does not. Yet, the addition of the extra code to go up through the category framework in the `FormalCompositeMap` does because that is a very general construction. It is also not even possible in `Rings()` to implement a general `is_injective` and `is_surjective`; only the first one for `Fields`. I also see `FormalCompositeMap` as a concrete implementation of a specific type of morphism, and so it is under no obligation to do things from the category.

> > > > Lastly, just as a word of caution, moving things up into a category forces them to be Python code, which is inherently slower than Cython code. Also, what is your end-game with the ring morphism code? Do you have a plan for what you want it to be like when all the changes are done?

> > > The idea is not to require morphisms of rings to inherit form `RingHomomorphism`. They should mostly inherit from whatever makes their implementation easiest. So, we certainly do not want to get rid of classes such as `RingHomomorphism_im_gens`.

> > 
> > There are some good reasons to have these concrete classes, with some stemming from a general lack of structure of ring morphisms. In some ways, by removing `RingHomomorphism_coercion`, we have made a problem for ourselves because coercion maps become the only maps not to inherit from `RingHomomorphism`. We can easily make it a requirement that morphisms inherit from `RingHomomorphism` as really nothing is (currently) implemented across multiple categories. I guess my question is do you see some larger hierarchy of classes that will run parallel to the category hierarchy?

> 
> `DefaultConvertMap` is implemented across multiple categories.

Do you mean used in multiple classes in multiple categories? However, there are no subclasses or other parallel implementations AFAIK.

>  The coercion between `ZZ` and `QQ` does not inherit from `RingHomomorphism`.  I suspect that there are others across Sage.

The `ZZ -> QQ` probably should inherit from `RingHomomorphism`. I would say any others that are ring morphisms should inherit for both uniformity and to take advantage of that code. There is no cost to this unless the plan is to remove `RingHomomorphism` altogether as an ABC (I am not sure this is the best thing going forward, but I haven't really thought about it yet).

I do support adding extra default implementations in the category, but I do not support always forcing concrete classes to make any sort of super calls, more so as the first attempted action.

From the patchbot, there are still failing doctests and the documentation does not build.



---

archive/issue_events_305382.json:
```json
{
    "actor": "https://github.com/tscrim",
    "created_at": "2017-07-21T15:32:56Z",
    "event": "unlabeled",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "label": "https://github.com/sagemath/sage/labels/needs%20review",
    "label_color": "7fff00",
    "label_name": "needs review",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/23204#event-305382"
}
```



---

archive/issue_events_305383.json:
```json
{
    "actor": "https://github.com/tscrim",
    "created_at": "2017-07-21T15:32:56Z",
    "event": "labeled",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "label": "https://github.com/sagemath/sage/labels/needs%20work",
    "label_color": "ffff00",
    "label_name": "needs work",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/23204#event-305383"
}
```



---

archive/issue_comments_350793.json:
```json
{
    "body": "Changed branch from **[u/saraedum/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/saraedum/remove_ringhomomorphism_coercion)** to **[u/aly.deines/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/aly.deines/remove_ringhomomorphism_coercion)**",
    "created_at": "2017-07-21T18:47:20Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350793",
    "user": "https://github.com/adeines"
}
```

Changed branch from **[u/saraedum/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/saraedum/remove_ringhomomorphism_coercion)** to **[u/aly.deines/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/aly.deines/remove_ringhomomorphism_coercion)**



---

archive/issue_events_305384.json:
```json
{
    "actor": "https://github.com/adeines",
    "created_at": "2017-07-21T18:48:57Z",
    "event": "unlabeled",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "label": "https://github.com/sagemath/sage/labels/needs%20work",
    "label_color": "ffff00",
    "label_name": "needs work",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/23204#event-305384"
}
```



---

archive/issue_events_305385.json:
```json
{
    "actor": "https://github.com/adeines",
    "created_at": "2017-07-21T18:48:57Z",
    "event": "labeled",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "label": "https://github.com/sagemath/sage/labels/positive%20review",
    "label_color": "dfffc0",
    "label_name": "positive review",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/23204#event-305385"
}
```



---

archive/issue_comments_350794.json:
```json
{
    "body": "Changed commit from **[e891a88](https://github.com/sagemath/sagetrac-mirror/commit/e891a884f8eca9e1e7d226306cb79b9cd827ee8b)** to **[b15febb](https://github.com/sagemath/sagetrac-mirror/commit/b15febbb9c7ef9be0163d572343bef5297e3611d)**",
    "created_at": "2017-07-21T18:48:57Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350794",
    "user": "https://github.com/adeines"
}
```

Changed commit from **[e891a88](https://github.com/sagemath/sagetrac-mirror/commit/e891a884f8eca9e1e7d226306cb79b9cd827ee8b)** to **[b15febb](https://github.com/sagemath/sagetrac-mirror/commit/b15febbb9c7ef9be0163d572343bef5297e3611d)**



---

archive/issue_comments_350795.json:
```json
{
    "body": "Changed reviewer from **Travis Scrimshaw** to **Travis Scrimshaw, Aly Deines**",
    "created_at": "2017-07-21T18:48:57Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350795",
    "user": "https://github.com/adeines"
}
```

Changed reviewer from **Travis Scrimshaw** to **Travis Scrimshaw, Aly Deines**



---

archive/issue_comments_350796.json:
```json
{
    "body": "<a id='comment:49'>Comment 49:</a>\nFixed two typos in documentation.  Looks good and all tests pass.\n\n---\nNew commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/b15febbb9c7ef9be0163d572343bef5297e3611d\">b15febb</a></td><td><code>Fixed two doctests typos.</code></td></tr></table>\n",
    "created_at": "2017-07-21T18:48:57Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350796",
    "user": "https://github.com/adeines"
}
```

<a id='comment:49'>Comment 49:</a>
Fixed two typos in documentation.  Looks good and all tests pass.

---
New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/b15febbb9c7ef9be0163d572343bef5297e3611d">b15febb</a></td><td><code>Fixed two doctests typos.</code></td></tr></table>




---

archive/issue_events_305386.json:
```json
{
    "actor": "https://github.com/tscrim",
    "created_at": "2017-07-21T22:14:32Z",
    "event": "unlabeled",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "label": "https://github.com/sagemath/sage/labels/positive%20review",
    "label_color": "dfffc0",
    "label_name": "positive review",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/23204#event-305386"
}
```



---

archive/issue_events_305387.json:
```json
{
    "actor": "https://github.com/tscrim",
    "created_at": "2017-07-21T22:14:32Z",
    "event": "labeled",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "label": "https://github.com/sagemath/sage/labels/needs%20work",
    "label_color": "ffff00",
    "label_name": "needs work",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/23204#event-305387"
}
```



---

archive/issue_comments_350797.json:
```json
{
    "body": "<a id='comment:50'>Comment 50:</a>\nYou do not get to set a positive review just because you fixed some trivial doctest failures. Code needs to be peer-reviewed, not self-reviewed. Furthermore, that was not the only problem according to the patchbot:\n\n```\nsage -t --long src/sage/schemes/curves/projective_curve.py  # 3 doctests failed\n```\nand again the documentation does not build according to the patchbot.\n\nMost importantly, there is still ongoing discussion about the code in this ticket, and you do not get to unilaterally say the code is good. You cannot set a positive review until the debate is resolved.",
    "created_at": "2017-07-21T22:14:32Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350797",
    "user": "https://github.com/tscrim"
}
```

<a id='comment:50'>Comment 50:</a>
You do not get to set a positive review just because you fixed some trivial doctest failures. Code needs to be peer-reviewed, not self-reviewed. Furthermore, that was not the only problem according to the patchbot:

```
sage -t --long src/sage/schemes/curves/projective_curve.py  # 3 doctests failed
```
and again the documentation does not build according to the patchbot.

Most importantly, there is still ongoing discussion about the code in this ticket, and you do not get to unilaterally say the code is good. You cannot set a positive review until the debate is resolved.



---

archive/issue_comments_350798.json:
```json
{
    "body": "Changed branch from **[u/aly.deines/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/aly.deines/remove_ringhomomorphism_coercion)** to **[u/saraedum/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/saraedum/remove_ringhomomorphism_coercion)**",
    "created_at": "2017-07-21T22:21:12Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350798",
    "user": "https://github.com/saraedum"
}
```

Changed branch from **[u/aly.deines/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/aly.deines/remove_ringhomomorphism_coercion)** to **[u/saraedum/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/saraedum/remove_ringhomomorphism_coercion)**



---

archive/issue_comments_350799.json:
```json
{
    "body": "<a id='comment:52'>Comment 52:</a>\nReplying to [@tscrim](#comment%3A50):\n> You do not get to set a positive review just because you fixed some trivial doctest failures. Code needs to be peer-reviewed, not self-reviewed. Furthermore, that was not the only problem according to the patchbot:\n> \n> ```\n> sage -t --long src/sage/schemes/curves/projective_curve.py  # 3 doctests failed\n> ```\n> and again the documentation does not build according to the patchbot.\n> \n> Most importantly, there is still ongoing discussion about the code in this ticket, and you do not get to unilaterally say the code is good. You cannot set a positive review until the debate is resolved.\n\nYour tone is condescending and you\u2019re making incorrect assumptions about the person who set the positive review having not read the code, etc.\n\n---\nNew commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/0c3d77228a92891b5c381442e7edbb3102eec888\">0c3d772</a></td><td><code>fix doctests</code></td></tr><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/ea35cc4827370a4d133c1a115ff1d85954320598\">ea35cc4</a></td><td><code>Merge branch 'u/aly.deines/remove_ringhomomorphism_coercion' of git://trac.sagemath.org/sage into t/23204/remove_ringhomomorphism_coercion</code></td></tr><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/961b6152a6187b88d9a536aa8929762f850b8d1c\">961b615</a></td><td><code>Merge remote-tracking branch 'origin/develop' into t/23204/remove_ringhomomorphism_coercion</code></td></tr></table>\n",
    "created_at": "2017-07-21T22:23:23Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350799",
    "user": "https://github.com/williamstein"
}
```

<a id='comment:52'>Comment 52:</a>
Replying to [@tscrim](#comment%3A50):
> You do not get to set a positive review just because you fixed some trivial doctest failures. Code needs to be peer-reviewed, not self-reviewed. Furthermore, that was not the only problem according to the patchbot:
> 
> ```
> sage -t --long src/sage/schemes/curves/projective_curve.py  # 3 doctests failed
> ```
> and again the documentation does not build according to the patchbot.
> 
> Most importantly, there is still ongoing discussion about the code in this ticket, and you do not get to unilaterally say the code is good. You cannot set a positive review until the debate is resolved.

Your tone is condescending and you’re making incorrect assumptions about the person who set the positive review having not read the code, etc.

---
New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/0c3d77228a92891b5c381442e7edbb3102eec888">0c3d772</a></td><td><code>fix doctests</code></td></tr><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/ea35cc4827370a4d133c1a115ff1d85954320598">ea35cc4</a></td><td><code>Merge branch 'u/aly.deines/remove_ringhomomorphism_coercion' of git://trac.sagemath.org/sage into t/23204/remove_ringhomomorphism_coercion</code></td></tr><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/961b6152a6187b88d9a536aa8929762f850b8d1c">961b615</a></td><td><code>Merge remote-tracking branch 'origin/develop' into t/23204/remove_ringhomomorphism_coercion</code></td></tr></table>




---

archive/issue_comments_350800.json:
```json
{
    "body": "Changed commit from **[b15febb](https://github.com/sagemath/sagetrac-mirror/commit/b15febbb9c7ef9be0163d572343bef5297e3611d)** to **[961b615](https://github.com/sagemath/sagetrac-mirror/commit/961b6152a6187b88d9a536aa8929762f850b8d1c)**",
    "created_at": "2017-07-21T22:23:23Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350800",
    "user": "https://github.com/williamstein"
}
```

Changed commit from **[b15febb](https://github.com/sagemath/sagetrac-mirror/commit/b15febbb9c7ef9be0163d572343bef5297e3611d)** to **[961b615](https://github.com/sagemath/sagetrac-mirror/commit/961b6152a6187b88d9a536aa8929762f850b8d1c)**



---

archive/issue_comments_350801.json:
```json
{
    "body": "<a id='comment:53'>Comment 53:</a>\n~~The tests in projective_curve pass for me.~~ Sorry, forgot --long.",
    "created_at": "2017-07-21T22:25:23Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350801",
    "user": "https://github.com/saraedum"
}
```

<a id='comment:53'>Comment 53:</a>
~~The tests in projective_curve pass for me.~~ Sorry, forgot --long.



---

archive/issue_comments_350802.json:
```json
{
    "body": "Work Issues: **failing doctests**",
    "created_at": "2017-07-21T22:27:01Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350802",
    "user": "https://github.com/saraedum"
}
```

Work Issues: **failing doctests**



---

archive/issue_comments_350803.json:
```json
{
    "body": "<a id='comment:55'>Comment 55:</a>\nReplying to [@williamstein](#comment%3A52):\n> Replying to [@tscrim](#comment%3A50):\n> > You do not get to set a positive review just because you fixed some trivial doctest failures. Code needs to be peer-reviewed, not self-reviewed. Furthermore, that was not the only problem according to the patchbot:\n> > \n> > ```\n> > sage -t --long src/sage/schemes/curves/projective_curve.py  # 3 doctests failed\n> > ```\n> > and again the documentation does not build according to the patchbot.\n> > \n> > Most importantly, there is still ongoing discussion about the code in this ticket, and you do not get to unilaterally say the code is good. You cannot set a positive review until the debate is resolved.\n\n> \n> Your tone is condescending and you\u2019re making incorrect assumptions about the person who set the positive review having not read the code, etc.\n\nI am sorry, I did not mean to sound condescending. However, there was no information given in that post to suggest that anyone else read the code and without consideration for the discussion did annoy me.",
    "created_at": "2017-07-21T22:34:07Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350803",
    "user": "https://github.com/tscrim"
}
```

<a id='comment:55'>Comment 55:</a>
Replying to [@williamstein](#comment%3A52):
> Replying to [@tscrim](#comment%3A50):
> > You do not get to set a positive review just because you fixed some trivial doctest failures. Code needs to be peer-reviewed, not self-reviewed. Furthermore, that was not the only problem according to the patchbot:
> > 
> > ```
> > sage -t --long src/sage/schemes/curves/projective_curve.py  # 3 doctests failed
> > ```
> > and again the documentation does not build according to the patchbot.
> > 
> > Most importantly, there is still ongoing discussion about the code in this ticket, and you do not get to unilaterally say the code is good. You cannot set a positive review until the debate is resolved.

> 
> Your tone is condescending and you’re making incorrect assumptions about the person who set the positive review having not read the code, etc.

I am sorry, I did not mean to sound condescending. However, there was no information given in that post to suggest that anyone else read the code and without consideration for the discussion did annoy me.



---

archive/issue_comments_350804.json:
```json
{
    "body": "Changed work issues from **failing doctests** to none",
    "created_at": "2017-07-21T22:41:23Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350804",
    "user": "https://github.com/saraedum"
}
```

Changed work issues from **failing doctests** to none



---

archive/issue_comments_350805.json:
```json
{
    "body": "<a id='comment:56'>Comment 56:</a>\n`projective_curve.py` works now. Let's see what the patchbot thinks about the rest.",
    "created_at": "2017-07-21T22:41:23Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350805",
    "user": "https://github.com/saraedum"
}
```

<a id='comment:56'>Comment 56:</a>
`projective_curve.py` works now. Let's see what the patchbot thinks about the rest.



---

archive/issue_events_305388.json:
```json
{
    "actor": "https://github.com/saraedum",
    "created_at": "2017-07-21T22:41:23Z",
    "event": "unlabeled",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "label": "https://github.com/sagemath/sage/labels/needs%20work",
    "label_color": "ffff00",
    "label_name": "needs work",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/23204#event-305388"
}
```



---

archive/issue_events_305389.json:
```json
{
    "actor": "https://github.com/saraedum",
    "created_at": "2017-07-21T22:41:23Z",
    "event": "labeled",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "label": "https://github.com/sagemath/sage/labels/needs%20review",
    "label_color": "7fff00",
    "label_name": "needs review",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/23204#event-305389"
}
```



---

archive/issue_comments_350806.json:
```json
{
    "body": "<a id='comment:57'>Comment 57:</a>\nBranch pushed to git repo; I updated commit sha1. New commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/d5affa709b63178eac8a74eac90b8d61ff77d209\">d5affa7</a></td><td><code>The \"morphism\" might just be a \"Map\", namely a DefaultConvertMap</code></td></tr></table>\n",
    "created_at": "2017-07-21T22:41:29Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350806",
    "user": "https://github.com/sagetrac-git"
}
```

<a id='comment:57'>Comment 57:</a>
Branch pushed to git repo; I updated commit sha1. New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/d5affa709b63178eac8a74eac90b8d61ff77d209">d5affa7</a></td><td><code>The "morphism" might just be a "Map", namely a DefaultConvertMap</code></td></tr></table>




---

archive/issue_comments_350807.json:
```json
{
    "body": "Changed commit from **[961b615](https://github.com/sagemath/sagetrac-mirror/commit/961b6152a6187b88d9a536aa8929762f850b8d1c)** to **[d5affa7](https://github.com/sagemath/sagetrac-mirror/commit/d5affa709b63178eac8a74eac90b8d61ff77d209)**",
    "created_at": "2017-07-21T22:41:29Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350807",
    "user": "https://github.com/sagetrac-git"
}
```

Changed commit from **[961b615](https://github.com/sagemath/sagetrac-mirror/commit/961b6152a6187b88d9a536aa8929762f850b8d1c)** to **[d5affa7](https://github.com/sagemath/sagetrac-mirror/commit/d5affa709b63178eac8a74eac90b8d61ff77d209)**



---

archive/issue_comments_350808.json:
```json
{
    "body": "<a id='comment:58'>Comment 58:</a>\nReplying to [@tscrim](#comment%3A55):\n> > Your tone is condescending and you\u2019re making incorrect assumptions about the person who set the positive review having not read the code, etc.\n\n> \n> I am sorry, I did not mean to sound condescending. However, there was no information given in that post to suggest that anyone else read the code and without consideration for the discussion did annoy me.\n\nFair enough \u2013 electronic communication can be difficult.  Everybody else involved in the discussion is in the same place (at Sage Days 87).",
    "created_at": "2017-07-21T22:46:10Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350808",
    "user": "https://github.com/williamstein"
}
```

<a id='comment:58'>Comment 58:</a>
Replying to [@tscrim](#comment%3A55):
> > Your tone is condescending and you’re making incorrect assumptions about the person who set the positive review having not read the code, etc.

> 
> I am sorry, I did not mean to sound condescending. However, there was no information given in that post to suggest that anyone else read the code and without consideration for the discussion did annoy me.

Fair enough – electronic communication can be difficult.  Everybody else involved in the discussion is in the same place (at Sage Days 87).



---

archive/issue_comments_350809.json:
```json
{
    "body": "<a id='comment:59'>Comment 59:</a>\nI still strongly disagree with adding the check to up the category MRO in the generic `FormalCompositeMap` for a few special cases. Compare:\n\n```\nsage: V1 = QQ^2\nsage: V2 = QQ^3\nsage: phi1 = (QQ^1).hom(Matrix([[1, 1]]), V1)\nsage: phi2 = V1.hom(Matrix([[1, 2, 3], [4, 5, 6]]), V2)\nsage: from sage.categories.map import FormalCompositeMap\nsage: c1 = FormalCompositeMap(Hom(QQ^1, V2, phi1.category_for()), phi1, phi2)\nsage: %timeit c1.is_injective()\nThe slowest run took 3132.19 times longer than the fastest. This could mean that an intermediate result is being cached.\n10000 loops, best of 3: 41.9 \u00b5s per loop\nsage: %timeit for _ in range(1000): c1.is_injective()\n10 loops, best of 3: 40.8 ms per loop\n```\nvs before:\n\n```\nsage: %timeit c1.is_injective()\nThe slowest run took 34968.62 times longer than the fastest. This could mean that an intermediate result is being cached.\n100000 loops, best of 3: 4.88 \u00b5s per loop\nsage: %timeit for _ in range(1000): c1.is_injective()\n100 loops, best of 3: 4.98 ms per loop\n```\nThat is a 10x slowdown for checking if injectivity/surjectivity is done at the category level, and I believe that most categories will not be able to implement either. At least the only one given so far is injectivity for `Fields`.",
    "created_at": "2017-07-21T23:08:01Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350809",
    "user": "https://github.com/tscrim"
}
```

<a id='comment:59'>Comment 59:</a>
I still strongly disagree with adding the check to up the category MRO in the generic `FormalCompositeMap` for a few special cases. Compare:

```
sage: V1 = QQ^2
sage: V2 = QQ^3
sage: phi1 = (QQ^1).hom(Matrix([[1, 1]]), V1)
sage: phi2 = V1.hom(Matrix([[1, 2, 3], [4, 5, 6]]), V2)
sage: from sage.categories.map import FormalCompositeMap
sage: c1 = FormalCompositeMap(Hom(QQ^1, V2, phi1.category_for()), phi1, phi2)
sage: %timeit c1.is_injective()
The slowest run took 3132.19 times longer than the fastest. This could mean that an intermediate result is being cached.
10000 loops, best of 3: 41.9 µs per loop
sage: %timeit for _ in range(1000): c1.is_injective()
10 loops, best of 3: 40.8 ms per loop
```
vs before:

```
sage: %timeit c1.is_injective()
The slowest run took 34968.62 times longer than the fastest. This could mean that an intermediate result is being cached.
100000 loops, best of 3: 4.88 µs per loop
sage: %timeit for _ in range(1000): c1.is_injective()
100 loops, best of 3: 4.98 ms per loop
```
That is a 10x slowdown for checking if injectivity/surjectivity is done at the category level, and I believe that most categories will not be able to implement either. At least the only one given so far is injectivity for `Fields`.



---

archive/issue_comments_350810.json:
```json
{
    "body": "<a id='comment:60'>Comment 60:</a>\nAlso, the doc is probably failing to build because you need to do these changes (addendum, there are 2 places, this one explicitly and a similar one):\n\n```diff\n     .. WARNING::\n \n-        Comparison of FpT_Polyring_section objects is not currently \n-        implemented. See :trac: `23469`. ::\n-\n-        sage: fprime = loads(dumps(f))\n-        sage: fprime == f\n-        False   \n+        Comparison of ``FpT_Polyring_section`` objects is not currently\n+        implemented. See :trac:`23469`. ::\n+\n+            sage: fprime = loads(dumps(f))\n+            sage: fprime == f\n+            False\n \n-        sage: fprime(1+t) == f(1+t)\n-        True\n+            sage: fprime(1+t) == f(1+t)\n+            True\n```\nNote also the trailing whitespace and other changes to the comment.\n\nThinking a little bit more, it probably is better to have `is_RingHomomorphism` as it does something non-trivial. However, my comment was more of that in some cases, it might be possible to instead replace `is_RingHomomorphism` with the `isinstance` check. This may not be possible, but I don't know the schemes code well enough to answer this.",
    "created_at": "2017-07-21T23:25:25Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350810",
    "user": "https://github.com/tscrim"
}
```

<a id='comment:60'>Comment 60:</a>
Also, the doc is probably failing to build because you need to do these changes (addendum, there are 2 places, this one explicitly and a similar one):

```diff
     .. WARNING::
 
-        Comparison of FpT_Polyring_section objects is not currently 
-        implemented. See :trac: `23469`. ::
-
-        sage: fprime = loads(dumps(f))
-        sage: fprime == f
-        False   
+        Comparison of ``FpT_Polyring_section`` objects is not currently
+        implemented. See :trac:`23469`. ::
+
+            sage: fprime = loads(dumps(f))
+            sage: fprime == f
+            False
 
-        sage: fprime(1+t) == f(1+t)
-        True
+            sage: fprime(1+t) == f(1+t)
+            True
```
Note also the trailing whitespace and other changes to the comment.

Thinking a little bit more, it probably is better to have `is_RingHomomorphism` as it does something non-trivial. However, my comment was more of that in some cases, it might be possible to instead replace `is_RingHomomorphism` with the `isinstance` check. This may not be possible, but I don't know the schemes code well enough to answer this.



---

archive/issue_events_305390.json:
```json
{
    "actor": "https://github.com/tscrim",
    "created_at": "2017-07-21T23:27:24Z",
    "event": "unlabeled",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "label": "https://github.com/sagemath/sage/labels/needs%20review",
    "label_color": "7fff00",
    "label_name": "needs review",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/23204#event-305390"
}
```



---

archive/issue_events_305391.json:
```json
{
    "actor": "https://github.com/tscrim",
    "created_at": "2017-07-21T23:27:24Z",
    "event": "labeled",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "label": "https://github.com/sagemath/sage/labels/needs%20work",
    "label_color": "ffff00",
    "label_name": "needs work",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/23204#event-305391"
}
```



---

archive/issue_comments_350811.json:
```json
{
    "body": "Work Issues: **docbuild**",
    "created_at": "2017-07-21T23:27:46Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350811",
    "user": "https://github.com/tscrim"
}
```

Work Issues: **docbuild**



---

archive/issue_comments_350812.json:
```json
{
    "body": "<a id='comment:63'>Comment 63:</a>\nReplying to [@tscrim](#comment%3A59):\n> I still strongly disagree with adding the check to up the category MRO in the generic `FormalCompositeMap` for a few special cases. Compare:\n> \n> ```\n> sage: V1 = QQ^2\n> sage: V2 = QQ^3\n> sage: phi1 = (QQ^1).hom(Matrix([[1, 1]]), V1)\n> sage: phi2 = V1.hom(Matrix([[1, 2, 3], [4, 5, 6]]), V2)\n> sage: from sage.categories.map import FormalCompositeMap\n> sage: c1 = FormalCompositeMap(Hom(QQ^1, V2, phi1.category_for()), phi1, phi2)\n> sage: %timeit c1.is_injective()\n> The slowest run took 3132.19 times longer than the fastest. This could mean that an intermediate result is being cached.\n> 10000 loops, best of 3: 41.9 \u00b5s per loop\n> sage: %timeit for _ in range(1000): c1.is_injective()\n> 10 loops, best of 3: 40.8 ms per loop\n> ```\n> vs before:\n> \n> ```\n> sage: %timeit c1.is_injective()\n> The slowest run took 34968.62 times longer than the fastest. This could mean that an intermediate result is being cached.\n> 100000 loops, best of 3: 4.88 \u00b5s per loop\n> sage: %timeit for _ in range(1000): c1.is_injective()\n> 100 loops, best of 3: 4.98 ms per loop\n> ```\n> That is a 10x slowdown for checking if injectivity/surjectivity is done at the category level, and I believe that most categories will not be able to implement either. At least the only one given so far is injectivity for `Fields`.\n\nFeel free to try the generic code first and ask the category only if that fails/throws. Then again, when the category can answer it, it's probably faster\u2026Anyway, I just want `is_injective` to work. I personally do not care about a few \u00b5s in `is_injective` since it currently almost always throws a `NotImplementedError` for me anyway.\n\nPS: I could also imagine that some categories could quickly answer `False` when there are dimension reasons why a morphism can not be injective/surjective.",
    "created_at": "2017-07-21T23:36:42Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350812",
    "user": "https://github.com/saraedum"
}
```

<a id='comment:63'>Comment 63:</a>
Replying to [@tscrim](#comment%3A59):
> I still strongly disagree with adding the check to up the category MRO in the generic `FormalCompositeMap` for a few special cases. Compare:
> 
> ```
> sage: V1 = QQ^2
> sage: V2 = QQ^3
> sage: phi1 = (QQ^1).hom(Matrix([[1, 1]]), V1)
> sage: phi2 = V1.hom(Matrix([[1, 2, 3], [4, 5, 6]]), V2)
> sage: from sage.categories.map import FormalCompositeMap
> sage: c1 = FormalCompositeMap(Hom(QQ^1, V2, phi1.category_for()), phi1, phi2)
> sage: %timeit c1.is_injective()
> The slowest run took 3132.19 times longer than the fastest. This could mean that an intermediate result is being cached.
> 10000 loops, best of 3: 41.9 µs per loop
> sage: %timeit for _ in range(1000): c1.is_injective()
> 10 loops, best of 3: 40.8 ms per loop
> ```
> vs before:
> 
> ```
> sage: %timeit c1.is_injective()
> The slowest run took 34968.62 times longer than the fastest. This could mean that an intermediate result is being cached.
> 100000 loops, best of 3: 4.88 µs per loop
> sage: %timeit for _ in range(1000): c1.is_injective()
> 100 loops, best of 3: 4.98 ms per loop
> ```
> That is a 10x slowdown for checking if injectivity/surjectivity is done at the category level, and I believe that most categories will not be able to implement either. At least the only one given so far is injectivity for `Fields`.

Feel free to try the generic code first and ask the category only if that fails/throws. Then again, when the category can answer it, it's probably faster…Anyway, I just want `is_injective` to work. I personally do not care about a few µs in `is_injective` since it currently almost always throws a `NotImplementedError` for me anyway.

PS: I could also imagine that some categories could quickly answer `False` when there are dimension reasons why a morphism can not be injective/surjective.



---

archive/issue_comments_350813.json:
```json
{
    "body": "<a id='comment:64'>Comment 64:</a>\nIt still will work when you remove the call up to the category in `FormalCompositeMap`. However, in those cases, the `FormalCompositeMap` should still return the answer fairly quickly unless you have a long chain to test.",
    "created_at": "2017-07-21T23:48:53Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350813",
    "user": "https://github.com/tscrim"
}
```

<a id='comment:64'>Comment 64:</a>
It still will work when you remove the call up to the category in `FormalCompositeMap`. However, in those cases, the `FormalCompositeMap` should still return the answer fairly quickly unless you have a long chain to test.



---

archive/issue_comments_350814.json:
```json
{
    "body": "<a id='comment:65'>Comment 65:</a>\nBranch pushed to git repo; I updated commit sha1. New commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/382e2d180772eb9d355aa7fbf539dcc52607a379\">382e2d1</a></td><td><code>fix docbuild</code></td></tr></table>\n",
    "created_at": "2017-07-22T01:55:54Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350814",
    "user": "https://github.com/sagetrac-git"
}
```

<a id='comment:65'>Comment 65:</a>
Branch pushed to git repo; I updated commit sha1. New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/382e2d180772eb9d355aa7fbf539dcc52607a379">382e2d1</a></td><td><code>fix docbuild</code></td></tr></table>




---

archive/issue_comments_350815.json:
```json
{
    "body": "Changed commit from **[d5affa7](https://github.com/sagemath/sagetrac-mirror/commit/d5affa709b63178eac8a74eac90b8d61ff77d209)** to **[382e2d1](https://github.com/sagemath/sagetrac-mirror/commit/382e2d180772eb9d355aa7fbf539dcc52607a379)**",
    "created_at": "2017-07-22T01:55:54Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350815",
    "user": "https://github.com/sagetrac-git"
}
```

Changed commit from **[d5affa7](https://github.com/sagemath/sagetrac-mirror/commit/d5affa709b63178eac8a74eac90b8d61ff77d209)** to **[382e2d1](https://github.com/sagemath/sagetrac-mirror/commit/382e2d180772eb9d355aa7fbf539dcc52607a379)**



---

archive/issue_comments_350816.json:
```json
{
    "body": "<a id='comment:66'>Comment 66:</a>\nLet's see if this fixes the docbuild. I am running tests right now. I also created a followup to improve the performance of `is_injective`/`is_surjective` #23513.",
    "created_at": "2017-07-22T01:59:19Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350816",
    "user": "https://github.com/saraedum"
}
```

<a id='comment:66'>Comment 66:</a>
Let's see if this fixes the docbuild. I am running tests right now. I also created a followup to improve the performance of `is_injective`/`is_surjective` #23513.



---

archive/issue_events_305392.json:
```json
{
    "actor": "https://github.com/saraedum",
    "created_at": "2017-07-22T01:59:38Z",
    "event": "unlabeled",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "label": "https://github.com/sagemath/sage/labels/needs%20work",
    "label_color": "ffff00",
    "label_name": "needs work",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/23204#event-305392"
}
```



---

archive/issue_events_305393.json:
```json
{
    "actor": "https://github.com/saraedum",
    "created_at": "2017-07-22T01:59:38Z",
    "event": "labeled",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "label": "https://github.com/sagemath/sage/labels/needs%20review",
    "label_color": "7fff00",
    "label_name": "needs review",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/23204#event-305393"
}
```



---

archive/issue_comments_350817.json:
```json
{
    "body": "Changed work issues from **docbuild** to none",
    "created_at": "2017-07-22T01:59:38Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350817",
    "user": "https://github.com/saraedum"
}
```

Changed work issues from **docbuild** to none



---

archive/issue_comments_350818.json:
```json
{
    "body": "<a id='comment:68'>Comment 68:</a>\nAll long tests pass. Documentation builds.",
    "created_at": "2017-07-22T02:20:36Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350818",
    "user": "https://github.com/saraedum"
}
```

<a id='comment:68'>Comment 68:</a>
All long tests pass. Documentation builds.



---

archive/issue_comments_350819.json:
```json
{
    "body": "<a id='comment:69'>Comment 69:</a>\nHi,\n\nThere\u2019s a lot of commits and discussion above.  However, it seems to me that the entire point of this ticket is to remove some ugly code that David Roe wrote in 2007 when he was Cythonizing my morphisms code.  The relevant git blame is:\n\n```\n03eb48258ab src/sage/rings/morphism.pyx (William Stein     2009-04-11 16:28:50 -0700  901)         return self._lift(x)\n^a4928b0cf4 src/sage/rings/morphism.py  (tornaria          2006-02-11 01:13:08 +0000  902)\n1d3fa4a1bbb src/sage/rings/morphism.pyx (David Roe         2007-10-05 04:10:04 -0400  903) cdef class RingHomomorphism_coercion(RingHomomorphism):\n8093d7d53d0 src/sage/rings/morphism.pyx (David Roe         2007-11-11 21:53:35 +0000  904)     def __init__(self, parent, check = True):\n03eb48258ab src/sage/rings/morphism.pyx (William Stein     2009-04-11 16:28:50 -0700  905)         \"\"\"\n4b97d1d434c src/sage/rings/morphism.pyx (Travis Scrimshaw  2012-11-04 11:16:05 -0800  906)         Initialize ``self``.\n4b97d1d434c src/sage/rings/morphism.pyx (Travis Scrimshaw  2012-11-04 11:16:05 -0800  907)\n03eb48258ab src/sage/rings/morphism.pyx (William Stein     2009-04-11 16:28:50 -0700  908)         INPUT:\n4b97d1d434c src/sage/rings/morphism.pyx (Travis Scrimshaw  2012-11-04 11:16:05 -0800  909)\n4b97d1d434c src/sage/rings/morphism.pyx (Travis Scrimshaw  2012-11-04 11:16:05 -0800  910)         - ``parent`` -- ring homset\n4b97d1d434c src/sage/rings/morphism.pyx (Travis Scrimshaw  2012-11-04 11:16:05 -0800  911)\n4b97d1d434c src/sage/rings/morphism.pyx (Travis Scrimshaw  2012-11-04 11:16:05 -0800  912)         - ``check`` -- bool (default: ``True``)\n03eb48258ab src/sage/rings/morphism.pyx (William Stein     2009-04-11 16:28:50 -0700  913)\n03eb48258ab src/sage/rings/morphism.pyx (William Stein     2009-04-11 16:28:50 -0700  914)         EXAMPLES::\n03eb48258ab src/sage/rings/morphism.pyx (William Stein     2009-04-11 16:28:50 -0700  915)\n03eb48258ab src/sage/rings/morphism.pyx (William Stein     2009-04-11 16:28:50 -0700  916)             sage: f = ZZ.hom(QQ); f                    # indirect doctes\nt\n03eb48258ab src/sage/rings/morphism.pyx (William Stein     2009-04-11 16:28:50 -0700  917)             Ring Coercion morphism:\n03eb48258ab src/sage/rings/morphism.pyx (William Stein     2009-04-11 16:28:50 -0700  918)               From: Int\n```\n\nThey did the hard work to fix all the doctests, everything passes, and the speed regression has no real impact on other code (you can just compare to None), and there is a followup ticket for that.   The modified code seems much cleaner.  I\u2019m giving this a positive review.   \n\nI\u2019ve always thought our coercion code is too complicated/ugly, so anything that cleans it up \u2013 and this surely does \u2013 is worth it.  Please start discussing ideas for optimization on #23513.\n\n(Travis \u2013 I\u2019m sitting here talking with the authors of this code, so maybe have more context than you.)",
    "created_at": "2017-07-22T03:10:32Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350819",
    "user": "https://github.com/williamstein"
}
```

<a id='comment:69'>Comment 69:</a>
Hi,

There’s a lot of commits and discussion above.  However, it seems to me that the entire point of this ticket is to remove some ugly code that David Roe wrote in 2007 when he was Cythonizing my morphisms code.  The relevant git blame is:

```
03eb48258ab src/sage/rings/morphism.pyx (William Stein     2009-04-11 16:28:50 -0700  901)         return self._lift(x)
^a4928b0cf4 src/sage/rings/morphism.py  (tornaria          2006-02-11 01:13:08 +0000  902)
1d3fa4a1bbb src/sage/rings/morphism.pyx (David Roe         2007-10-05 04:10:04 -0400  903) cdef class RingHomomorphism_coercion(RingHomomorphism):
8093d7d53d0 src/sage/rings/morphism.pyx (David Roe         2007-11-11 21:53:35 +0000  904)     def __init__(self, parent, check = True):
03eb48258ab src/sage/rings/morphism.pyx (William Stein     2009-04-11 16:28:50 -0700  905)         """
4b97d1d434c src/sage/rings/morphism.pyx (Travis Scrimshaw  2012-11-04 11:16:05 -0800  906)         Initialize ``self``.
4b97d1d434c src/sage/rings/morphism.pyx (Travis Scrimshaw  2012-11-04 11:16:05 -0800  907)
03eb48258ab src/sage/rings/morphism.pyx (William Stein     2009-04-11 16:28:50 -0700  908)         INPUT:
4b97d1d434c src/sage/rings/morphism.pyx (Travis Scrimshaw  2012-11-04 11:16:05 -0800  909)
4b97d1d434c src/sage/rings/morphism.pyx (Travis Scrimshaw  2012-11-04 11:16:05 -0800  910)         - ``parent`` -- ring homset
4b97d1d434c src/sage/rings/morphism.pyx (Travis Scrimshaw  2012-11-04 11:16:05 -0800  911)
4b97d1d434c src/sage/rings/morphism.pyx (Travis Scrimshaw  2012-11-04 11:16:05 -0800  912)         - ``check`` -- bool (default: ``True``)
03eb48258ab src/sage/rings/morphism.pyx (William Stein     2009-04-11 16:28:50 -0700  913)
03eb48258ab src/sage/rings/morphism.pyx (William Stein     2009-04-11 16:28:50 -0700  914)         EXAMPLES::
03eb48258ab src/sage/rings/morphism.pyx (William Stein     2009-04-11 16:28:50 -0700  915)
03eb48258ab src/sage/rings/morphism.pyx (William Stein     2009-04-11 16:28:50 -0700  916)             sage: f = ZZ.hom(QQ); f                    # indirect doctes
t
03eb48258ab src/sage/rings/morphism.pyx (William Stein     2009-04-11 16:28:50 -0700  917)             Ring Coercion morphism:
03eb48258ab src/sage/rings/morphism.pyx (William Stein     2009-04-11 16:28:50 -0700  918)               From: Int
```

They did the hard work to fix all the doctests, everything passes, and the speed regression has no real impact on other code (you can just compare to None), and there is a followup ticket for that.   The modified code seems much cleaner.  I’m giving this a positive review.   

I’ve always thought our coercion code is too complicated/ugly, so anything that cleans it up – and this surely does – is worth it.  Please start discussing ideas for optimization on #23513.

(Travis – I’m sitting here talking with the authors of this code, so maybe have more context than you.)



---

archive/issue_events_305394.json:
```json
{
    "actor": "https://github.com/williamstein",
    "created_at": "2017-07-22T03:10:32Z",
    "event": "unlabeled",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "label": "https://github.com/sagemath/sage/labels/needs%20review",
    "label_color": "7fff00",
    "label_name": "needs review",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/23204#event-305394"
}
```



---

archive/issue_events_305395.json:
```json
{
    "actor": "https://github.com/williamstein",
    "created_at": "2017-07-22T03:10:32Z",
    "event": "labeled",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "label": "https://github.com/sagemath/sage/labels/positive%20review",
    "label_color": "dfffc0",
    "label_name": "positive review",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/23204#event-305395"
}
```



---

archive/issue_comments_350820.json:
```json
{
    "body": "Changed reviewer from **Travis Scrimshaw, Aly Deines** to **Travis Scrimshaw, Aly Deines, William Stein**",
    "created_at": "2017-07-22T03:59:54Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350820",
    "user": "https://github.com/saraedum"
}
```

Changed reviewer from **Travis Scrimshaw, Aly Deines** to **Travis Scrimshaw, Aly Deines, William Stein**



---

archive/issue_comments_350821.json:
```json
{
    "body": "<a id='comment:71'>Comment 71:</a>\nI apologize in advance for any frustration in my response. I tried to make it as constructive as I can.\n\nIf you really think that this regression in the rest of Sage is worth the speed increase for rings (to which is only benefits certain rings and there is a check to see if the ring is in `Fields`, which can be expensive), then leave it as a positive review. I'm not going to\n\nReplying to [@williamstein](#comment%3A69):\n> There\u2019s a lot of commits and discussion above.  However, it seems to me that the entire point of this ticket is to remove some ugly code that David Roe wrote in 2007 when he was Cythonizing my morphisms code.  The relevant git blame is:\n\n>\n> [snip]\n\n> They did the hard work to fix all the doctests, everything passes,\n\nI think this alone does not merit a positive review.\n\n> and the speed regression has no real impact on other code (you can just compare to None), and there is a followup ticket for that.\n\nThat is not true. Checking in/surjectivity for a formal composite morphism that is not in the category of rings will be slowed down by a 10x factor as demonstrated above, and it is not simply comparing something to `None`. However, I am not fully convinced that the new code results in a speedup:\n\n```\nsage: K.<x> = FunctionField(QQ)\nsage: L.<y> = FunctionField(QQ)\nsage: M.<z> = FunctionField(QQ)\nsage: f = K.hom([y])\nsage: g = L.hom([z])\nsage: %timeit (g*f).is_injective()\n10000 loops, best of 3: 81.3 \u00b5s per loop\n```\nvs before\n\n```\nsage: %timeit (g*f).is_injective()\n10000 loops, best of 3: 46 \u00b5s per loop\n```\nThe reason why I am unsure is because the first time I run with the new code, I get ~28.5 \u00b5s. However, this does not happen on re-trails. Do you also experience this? Actually, when I removed the caching on `is_injective` in `Rings()` (for testing purposes, which is fair since this would most likely get called repeatedly on new maps if done in a tight loop), I consistently get the faster time and\n\n```\nsage: %timeit phi.is_injective()\nThe slowest run took 16.17 times longer than the fastest. This could mean that an intermediate result is being cached.\n100000 loops, best of 3: 9.33 \u00b5s per loop\n```\nvs old:\n\n```\nsage: %timeit phi.is_injective()\nThe slowest run took 6.60 times longer than the fastest. This could mean that an intermediate result is being cached.\n10000 loops, best of 3: 19.8 \u00b5s per loop\n```\nSo this does in fact net a ~2x speedup for this case.\n\nHowever, given the check for if something is in fields, that made me think of the following situation. Suppose we implement a special morphism for ring morphisms out of **Z**/n**Z** that checks for injectivity by looking at the image of `1` and the characteristic of the codomain (at least this seems like a reasonable thing to do at some point). Then compose this with some other map that knows about its injectivity (maybe checking if some sort of representation is faithful over various characteristics). With the proposed change, it would require computing the primality of `n` and not take advantage of the specialized code at all, resulting in a major slowdown with no clear way around other than reimplementing `FormalCompositeMap`.\n\nWell, this argument is moot if we put the check for `Fields()` last, but the general principle holds. Some/all of the maps are specialized, but it tries the generic code that can be very expensive (maybe getting the cardinality requires iterating over the entire (large) object, but it is free as an `R`-module).\n\n> The modified code seems much cleaner.  I\u2019m giving this a positive review.   \n\nThe `git blame` above is not that relevant to the code I am referring to in `FormalCompositeMorphism`. That added code is the only issue that I (currently) have with this ticket.\n\nWhat about my comment about undoing the deprecation on `is_RingHomomorphism`?\n\nI can think of some cases where not having the `FormalCompositeMap` call up to the category results in errors being raised that otherwise would not. Specifically, if one of the maps in the formal composition being something that cannot say anything. So it is good to have it in there, but I strongly believe it should not be called first. Instead, it should be the fallback.\n\n> I\u2019ve always thought our coercion code is too complicated/ugly, so anything that cleans it up \u2013 and this surely does \u2013 is worth it.  Please start discussing ideas for optimization on #23513.\n\nI agree that removing the `RingHomomorphism_coercion` does simplify the coercion code. However, my objection is to the code added to `FormalCompositeMap`, which makes things more complicated IMO. I can at least hack around it when I do need the speed. I do not work only with objects in `Rings()` but can formally compose morphisms. Really, there is no way around this regression other than not having these checks up through the category first.\n\n> (Travis \u2013 I\u2019m sitting here talking with the authors of this code, so maybe have more context than you.)\n\nI am not. Also, anybody trying to understand why this change was made latter will not have this context either. Since a few people in a room are making decisions and not giving me this context, I am going to stop reviewing and writing any code involving ring morphisms/coercions. I've spent too much time, energy, and political capital working on this.",
    "created_at": "2017-07-22T13:26:16Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350821",
    "user": "https://github.com/tscrim"
}
```

<a id='comment:71'>Comment 71:</a>
I apologize in advance for any frustration in my response. I tried to make it as constructive as I can.

If you really think that this regression in the rest of Sage is worth the speed increase for rings (to which is only benefits certain rings and there is a check to see if the ring is in `Fields`, which can be expensive), then leave it as a positive review. I'm not going to

Replying to [@williamstein](#comment%3A69):
> There’s a lot of commits and discussion above.  However, it seems to me that the entire point of this ticket is to remove some ugly code that David Roe wrote in 2007 when he was Cythonizing my morphisms code.  The relevant git blame is:

>
> [snip]

> They did the hard work to fix all the doctests, everything passes,

I think this alone does not merit a positive review.

> and the speed regression has no real impact on other code (you can just compare to None), and there is a followup ticket for that.

That is not true. Checking in/surjectivity for a formal composite morphism that is not in the category of rings will be slowed down by a 10x factor as demonstrated above, and it is not simply comparing something to `None`. However, I am not fully convinced that the new code results in a speedup:

```
sage: K.<x> = FunctionField(QQ)
sage: L.<y> = FunctionField(QQ)
sage: M.<z> = FunctionField(QQ)
sage: f = K.hom([y])
sage: g = L.hom([z])
sage: %timeit (g*f).is_injective()
10000 loops, best of 3: 81.3 µs per loop
```
vs before

```
sage: %timeit (g*f).is_injective()
10000 loops, best of 3: 46 µs per loop
```
The reason why I am unsure is because the first time I run with the new code, I get ~28.5 µs. However, this does not happen on re-trails. Do you also experience this? Actually, when I removed the caching on `is_injective` in `Rings()` (for testing purposes, which is fair since this would most likely get called repeatedly on new maps if done in a tight loop), I consistently get the faster time and

```
sage: %timeit phi.is_injective()
The slowest run took 16.17 times longer than the fastest. This could mean that an intermediate result is being cached.
100000 loops, best of 3: 9.33 µs per loop
```
vs old:

```
sage: %timeit phi.is_injective()
The slowest run took 6.60 times longer than the fastest. This could mean that an intermediate result is being cached.
10000 loops, best of 3: 19.8 µs per loop
```
So this does in fact net a ~2x speedup for this case.

However, given the check for if something is in fields, that made me think of the following situation. Suppose we implement a special morphism for ring morphisms out of **Z**/n**Z** that checks for injectivity by looking at the image of `1` and the characteristic of the codomain (at least this seems like a reasonable thing to do at some point). Then compose this with some other map that knows about its injectivity (maybe checking if some sort of representation is faithful over various characteristics). With the proposed change, it would require computing the primality of `n` and not take advantage of the specialized code at all, resulting in a major slowdown with no clear way around other than reimplementing `FormalCompositeMap`.

Well, this argument is moot if we put the check for `Fields()` last, but the general principle holds. Some/all of the maps are specialized, but it tries the generic code that can be very expensive (maybe getting the cardinality requires iterating over the entire (large) object, but it is free as an `R`-module).

> The modified code seems much cleaner.  I’m giving this a positive review.   

The `git blame` above is not that relevant to the code I am referring to in `FormalCompositeMorphism`. That added code is the only issue that I (currently) have with this ticket.

What about my comment about undoing the deprecation on `is_RingHomomorphism`?

I can think of some cases where not having the `FormalCompositeMap` call up to the category results in errors being raised that otherwise would not. Specifically, if one of the maps in the formal composition being something that cannot say anything. So it is good to have it in there, but I strongly believe it should not be called first. Instead, it should be the fallback.

> I’ve always thought our coercion code is too complicated/ugly, so anything that cleans it up – and this surely does – is worth it.  Please start discussing ideas for optimization on #23513.

I agree that removing the `RingHomomorphism_coercion` does simplify the coercion code. However, my objection is to the code added to `FormalCompositeMap`, which makes things more complicated IMO. I can at least hack around it when I do need the speed. I do not work only with objects in `Rings()` but can formally compose morphisms. Really, there is no way around this regression other than not having these checks up through the category first.

> (Travis – I’m sitting here talking with the authors of this code, so maybe have more context than you.)

I am not. Also, anybody trying to understand why this change was made latter will not have this context either. Since a few people in a room are making decisions and not giving me this context, I am going to stop reviewing and writing any code involving ring morphisms/coercions. I've spent too much time, energy, and political capital working on this.



---

archive/issue_comments_350822.json:
```json
{
    "body": "<a id='comment:72'>Comment 72:</a>\n>  I am going to stop reviewing and writing any code involving ring morphisms/coercions. \n\nBummer.  Hopefully, you can focus more on the millions of other interesting things you\u2019ve done.\n\n> I've spent too much time, energy, and political capital working on this.\n\nFor what it is worth, I greatly respect and appreciation your many contributions to Sage, and your careful feedback related to this and many other ticket.   Looking forward to seeing you sometime in the future at a Sage days...",
    "created_at": "2017-07-22T15:09:56Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350822",
    "user": "https://github.com/williamstein"
}
```

<a id='comment:72'>Comment 72:</a>
>  I am going to stop reviewing and writing any code involving ring morphisms/coercions. 

Bummer.  Hopefully, you can focus more on the millions of other interesting things you’ve done.

> I've spent too much time, energy, and political capital working on this.

For what it is worth, I greatly respect and appreciation your many contributions to Sage, and your careful feedback related to this and many other ticket.   Looking forward to seeing you sometime in the future at a Sage days...



---

archive/issue_comments_350823.json:
```json
{
    "body": "<a id='comment:73'>Comment 73:</a>\nReplying to [@williamstein](#comment%3A72):\n> > I've spent too much time, energy, and political capital working on this.\n\n> \n> For what it is worth, I greatly respect and appreciation your many contributions to Sage, and your careful feedback related to this and many other ticket.   Looking forward to seeing you sometime in the future at a Sage days...\n\nIt is worth quite a bit, thank you. It will be nice to see you again too at a future Sage days.\n\nAlso thank you to everyone else who worked on this ticket. I do think it is overall a step in the right direction.",
    "created_at": "2017-07-22T15:42:47Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350823",
    "user": "https://github.com/tscrim"
}
```

<a id='comment:73'>Comment 73:</a>
Replying to [@williamstein](#comment%3A72):
> > I've spent too much time, energy, and political capital working on this.

> 
> For what it is worth, I greatly respect and appreciation your many contributions to Sage, and your careful feedback related to this and many other ticket.   Looking forward to seeing you sometime in the future at a Sage days...

It is worth quite a bit, thank you. It will be nice to see you again too at a future Sage days.

Also thank you to everyone else who worked on this ticket. I do think it is overall a step in the right direction.



---

archive/issue_events_305396.json:
```json
{
    "actor": "https://github.com/vbraun",
    "created_at": "2017-07-29T15:30:52Z",
    "event": "unlabeled",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "label": "https://github.com/sagemath/sage/labels/positive%20review",
    "label_color": "dfffc0",
    "label_name": "positive review",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/23204#event-305396"
}
```



---

archive/issue_events_305397.json:
```json
{
    "actor": "https://github.com/vbraun",
    "commit_id": "cb44570b8e1643b82f7de0f84cfab9a86c006a8e",
    "commit_repository": "https://github.com/sagemath/sage",
    "created_at": "2017-07-29T15:30:52Z",
    "event": "closed",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/23204#event-305397"
}
```



---

archive/issue_comments_350824.json:
```json
{
    "body": "Changed branch from **[u/saraedum/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/saraedum/remove_ringhomomorphism_coercion)** to **[382e2d1](https://github.com/sagemath/sagetrac-mirror/commit/382e2d180772eb9d355aa7fbf539dcc52607a379)**",
    "created_at": "2017-07-29T15:30:52Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/23204",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/23204#issuecomment-350824",
    "user": "https://github.com/vbraun"
}
```

Changed branch from **[u/saraedum/remove_ringhomomorphism_coercion](https://github.com/sagemath/sagetrac-mirror/tree/u/saraedum/remove_ringhomomorphism_coercion)** to **[382e2d1](https://github.com/sagemath/sagetrac-mirror/commit/382e2d180772eb9d355aa7fbf539dcc52607a379)**
