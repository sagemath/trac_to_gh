# Issue 21859: Clean up linking against flint

archive/issues_021622.json:
```json
{
    "body": "Remove all uses of `flint` from `module_list.py`. Instead ensure that `flint` is found through `# distutils` directives and cimports.\n\nCC:  @kiwifb\n\nBranch/Commit: 5d2a574e10d9e061fa8e884291f04b317c62399c\n\nReviewer: Fran\u00e7ois Bissey\n\nAuthor: Jeroen Demeyer\n\nDependencies: #21853\n\nResolution: fixed\n\nIssue created by migration from https://trac.sagemath.org/ticket/21859\n\n",
    "closed_at": "2016-11-15T23:25:00Z",
    "created_at": "2016-11-10T21:30:13Z",
    "labels": [
        "component: build",
        "bug"
    ],
    "milestone": "https://github.com/sagemath/sagetest/milestones/sage-7.5",
    "title": "Clean up linking against flint",
    "type": "issue",
    "url": "https://github.com/sagemath/sagetest/issues/21859",
    "user": "https://github.com/jdemeyer"
}
```
Remove all uses of `flint` from `module_list.py`. Instead ensure that `flint` is found through `# distutils` directives and cimports.

CC:  @kiwifb

Branch/Commit: 5d2a574e10d9e061fa8e884291f04b317c62399c

Reviewer: François Bissey

Author: Jeroen Demeyer

Dependencies: #21853

Resolution: fixed

Issue created by migration from https://trac.sagemath.org/ticket/21859





---

archive/issue_comments_299761.json:
```json
{
    "body": "Changing status from new to needs_review.",
    "created_at": "2016-11-11T15:49:32Z",
    "issue": "https://github.com/sagemath/sagetest/issues/21859",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/21859#issuecomment-299761",
    "user": "https://github.com/jdemeyer"
}
```

Changing status from new to needs_review.



---

archive/issue_comments_299762.json:
```json
{
    "body": "<a id='comment:2'></a>New commits:",
    "created_at": "2016-11-11T15:49:32Z",
    "issue": "https://github.com/sagemath/sagetest/issues/21859",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/21859#issuecomment-299762",
    "user": "https://github.com/jdemeyer"
}
```

<a id='comment:2'></a>New commits:



---

archive/issue_comments_299763.json:
```json
{
    "body": "<a id='comment:3'></a>I must say I had to check that we already have a directive in `flint.pxd` as I couldn't see `flint` being added again.\n\nThere are several instances of `# distutils: libraries = flint` in `pxd` files inside `sage/libs/flint`, are they all needed? I would have thought some of them could be trimmed.",
    "created_at": "2016-11-11T19:11:19Z",
    "issue": "https://github.com/sagemath/sagetest/issues/21859",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/21859#issuecomment-299763",
    "user": "https://github.com/kiwifb"
}
```

<a id='comment:3'></a>I must say I had to check that we already have a directive in `flint.pxd` as I couldn't see `flint` being added again.

There are several instances of `# distutils: libraries = flint` in `pxd` files inside `sage/libs/flint`, are they all needed? I would have thought some of them could be trimmed.



---

archive/issue_comments_299764.json:
```json
{
    "body": "<a id='comment:4'></a>Replying to [comment:3 fbissey]:\n> There are several instances of `# distutils: libraries = flint` in `pxd` files inside `sage/libs/flint`, are they all needed? I would have thought some of them could be trimmed.\n\n\nDo you have concrete suggestions about which ones should be removed?",
    "created_at": "2016-11-11T19:34:07Z",
    "issue": "https://github.com/sagemath/sagetest/issues/21859",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/21859#issuecomment-299764",
    "user": "https://github.com/jdemeyer"
}
```

<a id='comment:4'></a>Replying to [comment:3 fbissey]:
> There are several instances of `# distutils: libraries = flint` in `pxd` files inside `sage/libs/flint`, are they all needed? I would have thought some of them could be trimmed.


Do you have concrete suggestions about which ones should be removed?



---

archive/issue_comments_299765.json:
```json
{
    "body": "<a id='comment:5'></a>`fmpq_poly.pxd`? It should get it through `fmpz_vec.pxd`. After that, it would need more complicated stuff. If `types.pxd` had a distutils directive we could drop it from many other places, but may be I think of it wrong.",
    "created_at": "2016-11-11T19:48:51Z",
    "issue": "https://github.com/sagemath/sagetest/issues/21859",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/21859#issuecomment-299765",
    "user": "https://github.com/kiwifb"
}
```

<a id='comment:5'></a>`fmpq_poly.pxd`? It should get it through `fmpz_vec.pxd`. After that, it would need more complicated stuff. If `types.pxd` had a distutils directive we could drop it from many other places, but may be I think of it wrong.



---

archive/issue_comments_299766.json:
```json
{
    "body": "<a id='comment:6'></a>Replying to [comment:5 fbissey]:\n> `fmpq_poly.pxd`? It should get it through `fmpz_vec.pxd`.\n\n\nWhy should we remove `# distutils: libraries = flint` from a file just because it happens to `cimport` a file which also has `# distutils: libraries = flint`? Imagine that we remove that and that somebody in the future decides to stop `cimport`ing `sage.libs.flint.fmpz_vec`. Then you get breakage.\n\nI prefer that each `# distutils` directive declares whatever is in *that* file, regardless of which other things it happens to `cimport`.",
    "created_at": "2016-11-11T20:27:28Z",
    "issue": "https://github.com/sagemath/sagetest/issues/21859",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/21859#issuecomment-299766",
    "user": "https://github.com/jdemeyer"
}
```

<a id='comment:6'></a>Replying to [comment:5 fbissey]:
> `fmpq_poly.pxd`? It should get it through `fmpz_vec.pxd`.


Why should we remove `# distutils: libraries = flint` from a file just because it happens to `cimport` a file which also has `# distutils: libraries = flint`? Imagine that we remove that and that somebody in the future decides to stop `cimport`ing `sage.libs.flint.fmpz_vec`. Then you get breakage.

I prefer that each `# distutils` directive declares whatever is in *that* file, regardless of which other things it happens to `cimport`.



---

archive/issue_comments_299767.json:
```json
{
    "body": "<a id='comment:7'></a>Replying to [comment:5 fbissey]:\n> If `types.pxd` had a distutils directive we could drop it from many other places, but may be I think of it wrong.\n\n\nThat *intentionally* does *not* have a distutils directive. Separating the types from the library functions is good because many `.pxd` files need some FLINT type but do not need to be linked against `-lflint`.\n\nLook at `sage.rings.integer` for example: everything `cimport`ing that needs to know about the `mpz_t` type. But it doesn't need to be linked against `-lgmp` because of that. By separating the types like this, we only link the library if we really use it.",
    "created_at": "2016-11-11T20:35:04Z",
    "issue": "https://github.com/sagemath/sagetest/issues/21859",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/21859#issuecomment-299767",
    "user": "https://github.com/jdemeyer"
}
```

<a id='comment:7'></a>Replying to [comment:5 fbissey]:
> If `types.pxd` had a distutils directive we could drop it from many other places, but may be I think of it wrong.


That *intentionally* does *not* have a distutils directive. Separating the types from the library functions is good because many `.pxd` files need some FLINT type but do not need to be linked against `-lflint`.

Look at `sage.rings.integer` for example: everything `cimport`ing that needs to know about the `mpz_t` type. But it doesn't need to be linked against `-lgmp` because of that. By separating the types like this, we only link the library if we really use it.



---

archive/issue_comments_299768.json:
```json
{
    "body": "<a id='comment:8'></a>This is actually documented in `src/sage/libs/pari/types.pxd` but not in other `types.pxd` files.",
    "created_at": "2016-11-11T20:36:46Z",
    "issue": "https://github.com/sagemath/sagetest/issues/21859",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/21859#issuecomment-299768",
    "user": "https://github.com/jdemeyer"
}
```

<a id='comment:8'></a>This is actually documented in `src/sage/libs/pari/types.pxd` but not in other `types.pxd` files.



---

archive/issue_comments_299769.json:
```json
{
    "body": "Changing status from needs_review to positive_review.",
    "created_at": "2016-11-12T02:05:31Z",
    "issue": "https://github.com/sagemath/sagetest/issues/21859",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/21859#issuecomment-299769",
    "user": "https://github.com/kiwifb"
}
```

Changing status from needs_review to positive_review.



---

archive/issue_comments_299770.json:
```json
{
    "body": "Resolution: fixed",
    "created_at": "2016-11-15T23:25:00Z",
    "issue": "https://github.com/sagemath/sagetest/issues/21859",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/21859#issuecomment-299770",
    "user": "https://github.com/vbraun"
}
```

Resolution: fixed



---

archive/issue_events_057740.json:
```json
{
    "actor": "https://github.com/vbraun",
    "created_at": "2016-11-15T23:25:00Z",
    "event": "closed",
    "issue": "https://github.com/sagemath/sagetest/issues/21859",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sagetest/issues/21859#event-57740"
}
```
