# Issue 25998: implement set and get nthreads for magma interface

archive/issues_025998.json:
```json
{
    "body": "CC:  slelievre\n\n\n\nIssue created by migration from https://trac.sagemath.org/ticket/26235\n\n",
    "created_at": "2018-09-10T19:38:50Z",
    "labels": [
        "PLEASE CHANGE",
        "major"
    ],
    "milestone": "https://github.com/sagemath/sagetest/milestones/sage-8.4",
    "title": "implement set and get nthreads for magma interface",
    "type": "issue",
    "url": "https://github.com/sagemath/sagetest/issues/25998",
    "user": "ruhm"
}
```
CC:  slelievre



Issue created by migration from https://trac.sagemath.org/ticket/26235





---

archive/issue_comments_366576.json:
```json
{
    "body": "Changing type from PLEASE CHANGE to enhancement.",
    "created_at": "2018-09-10T19:48:41Z",
    "issue": "https://github.com/sagemath/sagetest/issues/25998",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/25998#issuecomment-366576",
    "user": "ruhm"
}
```

Changing type from PLEASE CHANGE to enhancement.



---

archive/issue_comments_366577.json:
```json
{
    "body": "New commits:",
    "created_at": "2018-09-10T19:48:41Z",
    "issue": "https://github.com/sagemath/sagetest/issues/25998",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/25998#issuecomment-366577",
    "user": "ruhm"
}
```

New commits:



---

archive/issue_comments_366578.json:
```json
{
    "body": "Changing status from new to needs_review.",
    "created_at": "2018-09-10T19:48:41Z",
    "issue": "https://github.com/sagemath/sagetest/issues/25998",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/25998#issuecomment-366578",
    "user": "ruhm"
}
```

Changing status from new to needs_review.



---

archive/issue_comments_366579.json:
```json
{
    "body": "Changing keywords from \"\" to \"magma, threads\".",
    "created_at": "2018-09-10T19:48:41Z",
    "issue": "https://github.com/sagemath/sagetest/issues/25998",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/25998#issuecomment-366579",
    "user": "ruhm"
}
```

Changing keywords from "" to "magma, threads".



---

archive/issue_comments_366580.json:
```json
{
    "body": "Changing priority from major to minor.",
    "created_at": "2018-09-10T19:48:41Z",
    "issue": "https://github.com/sagemath/sagetest/issues/25998",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/25998#issuecomment-366580",
    "user": "ruhm"
}
```

Changing priority from major to minor.



---

archive/issue_comments_366581.json:
```json
{
    "body": "Changing component from PLEASE CHANGE to interfaces: optional.",
    "created_at": "2018-09-10T19:48:41Z",
    "issue": "https://github.com/sagemath/sagetest/issues/25998",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/25998#issuecomment-366581",
    "user": "ruhm"
}
```

Changing component from PLEASE CHANGE to interfaces: optional.



---

archive/issue_comments_366582.json:
```json
{
    "body": "LGTM.",
    "created_at": "2018-09-10T23:29:07Z",
    "issue": "https://github.com/sagemath/sagetest/issues/25998",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/25998#issuecomment-366582",
    "user": "tscrim"
}
```

LGTM.



---

archive/issue_comments_366583.json:
```json
{
    "body": "Changing status from needs_review to positive_review.",
    "created_at": "2018-09-10T23:29:07Z",
    "issue": "https://github.com/sagemath/sagetest/issues/25998",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/25998#issuecomment-366583",
    "user": "tscrim"
}
```

Changing status from needs_review to positive_review.



---

archive/issue_comments_366584.json:
```json
{
    "body": "It seems useless clutter to have each time two methods for the same thing.\n\nThe current proposition for this ticket is:\n\n\n```\n    def set_nthreads(self, n):\n        \"\"\"\n        Set the number of threads used for parallelized algorithms in Magma.\n\n        INPUT:\n\n        - ``n`` - number of threads\n\n        EXAMPLES::\n\n            sage: magma.set_nthreads(2)                #optional - magma\n            sage: magma.get_nthreads()                 #optional - magma\n            2\n        \"\"\"\n        self.SetNthreads(n)\n\n    def SetNthreads(self, n):\n        \"\"\"\n        Set the number of threads used for parallelized algorithms in Magma.\n\n        INPUT:\n\n        - ``n`` - number of threads\n\n        .. note::\n\n           This method is provided to be consistent with the Magma\n           naming convention.\n\n        EXAMPLES::\n\n            sage: magma.SetNthreads(2)                 #optional - magma\n            sage: magma.GetNthreads()                  #optional - magma\n            2\n        \"\"\"\n        self.eval('SetNthreads(%d)' % (n))\n\n    def get_nthreads(self):\n        \"\"\"\n        Get the number of threads used in Magma.\n\n        EXAMPLES::\n\n            sage: magma.set_nthreads(2)                #optional - magma\n            sage: magma.get_nthreads()                 #optional - magma\n            2\n        \"\"\"\n        return self.GetNthreads()\n\n    def GetNthreads(self):\n        \"\"\"\n        Get the number of threads used in Magma.\n\n        .. note::\n\n           This method is provided to be consistent with the Magma\n           naming convention.\n\n        EXAMPLES::\n\n            sage: magma.SetNthreads(2)                 #optional - magma\n            sage: magma.GetNthreads()                  #optional - magma\n            2\n        \"\"\"\n        return int(self.eval('GetNthreads()'))\n```\n\n\nAmong existing methods for the Magma class, one can find\n\n- four examples with only Python-style names; they call the corresponding\n  Magma function using `self.eval(...)`:\n\n\n```\n    def set_seed(self, seed=None):\n        self.eval('SetSeed(%d)' % seed)\n\n    def cputime(self, t=None):\n        if t:\n            return float(self.eval('Cputime(%s)' % t))\n        else:\n            return float(self.eval('Cputime()'))\n\n    def chdir(self, dir):\n        self.eval('ChangeDirectory(\"%s\")' % dir, strip=False)\n\n    def version(self):\n        t = tuple([int(n) for n in self.eval('GetVersion()').split()])\n        return t, 'V%s.%s-%s' % t\n```\n\n\n- two examples with python-style names, which work like the four above,\n  and are followed by synonyms with magma-style names:\n\n\n```\n    def attach(self, filename):\n        self.eval('Attach(\"%s\")' % filename)\n\n    Attach = attach\n\n    def attach_spec(self, filename):\n        s = self.eval('AttachSpec(\"%s\")' % filename)\n        if s:\n            raise RuntimeError(s.strip())\n\n    AttachSpec = attach_spec\n```\n\n\n- two examples where there is each time two methods, with the two naming styles,\n  one calling the other, much like the current proposition in this ticket:\n\n\n```\n    def set_verbose(self, type, level):\n        self.SetVerbose(type, level)\n\n    def SetVerbose(self, type, level):\n        if level < 0:\n            raise TypeError(\"level must be >= 0\")\n        self.eval('SetVerbose(\"%s\",%d)' % (type, level))\n\n    def get_verbose(self, type):\n        return self.GetVerbose(type)\n\n    def GetVerbose(self, type):\n        return int(self.eval('GetVerbose(\"%s\")' % type))\n```\n\n\nFor the current ticket, I would suggest using synonyms rather than\ndefining two separate methods each time:\n\n\n```\n    def set_nthreads(self, n):\n        \"\"\"\n        Set the number of threads used for parallelized algorithms in Magma.\n\n        INPUT:\n\n        - ``n`` - number of threads\n\n        EXAMPLES::\n\n            sage: magma.set_nthreads(2)                #optional - magma\n            sage: magma.get_nthreads()                 #optional - magma\n            2\n        \"\"\"\n        self.eval('SetNthreads(%d)' % (n))\n\n    SetNthreads = set_nthreads\n\n    def get_nthreads(self):\n        \"\"\"\n        Get the number of threads used in Magma.\n\n        EXAMPLES::\n\n            sage: magma.set_nthreads(2)                #optional - magma\n            sage: magma.get_nthreads()                 #optional - magma\n            2\n        \"\"\"\n        return int(self.eval('GetNthreads()'))\n\n    GetNthreads = get_nthreads\n```\n\n\nAnd I would suggest taking the opportunity to do the same for\n`set_verbose`/`SetVerbose` and `get_verbose`/`GetVerbose`.\n\nOr... this cleaning-up can all be done in a follow-up ticket --- possibly\na better idea, especially since this ticket already has `positive_review`.",
    "created_at": "2018-09-11T10:52:37Z",
    "issue": "https://github.com/sagemath/sagetest/issues/25998",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/25998#issuecomment-366584",
    "user": "slelievre"
}
```

It seems useless clutter to have each time two methods for the same thing.

The current proposition for this ticket is:


```
    def set_nthreads(self, n):
        """
        Set the number of threads used for parallelized algorithms in Magma.

        INPUT:

        - ``n`` - number of threads

        EXAMPLES::

            sage: magma.set_nthreads(2)                #optional - magma
            sage: magma.get_nthreads()                 #optional - magma
            2
        """
        self.SetNthreads(n)

    def SetNthreads(self, n):
        """
        Set the number of threads used for parallelized algorithms in Magma.

        INPUT:

        - ``n`` - number of threads

        .. note::

           This method is provided to be consistent with the Magma
           naming convention.

        EXAMPLES::

            sage: magma.SetNthreads(2)                 #optional - magma
            sage: magma.GetNthreads()                  #optional - magma
            2
        """
        self.eval('SetNthreads(%d)' % (n))

    def get_nthreads(self):
        """
        Get the number of threads used in Magma.

        EXAMPLES::

            sage: magma.set_nthreads(2)                #optional - magma
            sage: magma.get_nthreads()                 #optional - magma
            2
        """
        return self.GetNthreads()

    def GetNthreads(self):
        """
        Get the number of threads used in Magma.

        .. note::

           This method is provided to be consistent with the Magma
           naming convention.

        EXAMPLES::

            sage: magma.SetNthreads(2)                 #optional - magma
            sage: magma.GetNthreads()                  #optional - magma
            2
        """
        return int(self.eval('GetNthreads()'))
```


Among existing methods for the Magma class, one can find

- four examples with only Python-style names; they call the corresponding
  Magma function using `self.eval(...)`:


```
    def set_seed(self, seed=None):
        self.eval('SetSeed(%d)' % seed)

    def cputime(self, t=None):
        if t:
            return float(self.eval('Cputime(%s)' % t))
        else:
            return float(self.eval('Cputime()'))

    def chdir(self, dir):
        self.eval('ChangeDirectory("%s")' % dir, strip=False)

    def version(self):
        t = tuple([int(n) for n in self.eval('GetVersion()').split()])
        return t, 'V%s.%s-%s' % t
```


- two examples with python-style names, which work like the four above,
  and are followed by synonyms with magma-style names:


```
    def attach(self, filename):
        self.eval('Attach("%s")' % filename)

    Attach = attach

    def attach_spec(self, filename):
        s = self.eval('AttachSpec("%s")' % filename)
        if s:
            raise RuntimeError(s.strip())

    AttachSpec = attach_spec
```


- two examples where there is each time two methods, with the two naming styles,
  one calling the other, much like the current proposition in this ticket:


```
    def set_verbose(self, type, level):
        self.SetVerbose(type, level)

    def SetVerbose(self, type, level):
        if level < 0:
            raise TypeError("level must be >= 0")
        self.eval('SetVerbose("%s",%d)' % (type, level))

    def get_verbose(self, type):
        return self.GetVerbose(type)

    def GetVerbose(self, type):
        return int(self.eval('GetVerbose("%s")' % type))
```


For the current ticket, I would suggest using synonyms rather than
defining two separate methods each time:


```
    def set_nthreads(self, n):
        """
        Set the number of threads used for parallelized algorithms in Magma.

        INPUT:

        - ``n`` - number of threads

        EXAMPLES::

            sage: magma.set_nthreads(2)                #optional - magma
            sage: magma.get_nthreads()                 #optional - magma
            2
        """
        self.eval('SetNthreads(%d)' % (n))

    SetNthreads = set_nthreads

    def get_nthreads(self):
        """
        Get the number of threads used in Magma.

        EXAMPLES::

            sage: magma.set_nthreads(2)                #optional - magma
            sage: magma.get_nthreads()                 #optional - magma
            2
        """
        return int(self.eval('GetNthreads()'))

    GetNthreads = get_nthreads
```


And I would suggest taking the opportunity to do the same for
`set_verbose`/`SetVerbose` and `get_verbose`/`GetVerbose`.

Or... this cleaning-up can all be done in a follow-up ticket --- possibly
a better idea, especially since this ticket already has `positive_review`.



---

archive/issue_comments_366585.json:
```json
{
    "body": "Thanks for the remark. I will create a new ticket to deal with the cleaning up.",
    "created_at": "2018-09-12T09:42:51Z",
    "issue": "https://github.com/sagemath/sagetest/issues/25998",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/25998#issuecomment-366585",
    "user": "ruhm"
}
```

Thanks for the remark. I will create a new ticket to deal with the cleaning up.



---

archive/issue_comments_366586.json:
```json
{
    "body": "The ticket is #26259",
    "created_at": "2018-09-12T09:44:45Z",
    "issue": "https://github.com/sagemath/sagetest/issues/25998",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/25998#issuecomment-366586",
    "user": "ruhm"
}
```

The ticket is #26259



---

archive/issue_comments_366587.json:
```json
{
    "body": "Resolution: fixed",
    "created_at": "2018-09-12T19:58:30Z",
    "issue": "https://github.com/sagemath/sagetest/issues/25998",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/25998#issuecomment-366587",
    "user": "vbraun"
}
```

Resolution: fixed
