# Issue 15496: substitution in symbolic function fails with latex_name

archive/issues_015496.json:
```json
{
    "body": "Keywords: substitute, function, solve, latex_name\n\nThe following substitution does not work:\n\n```\nsage: function('f', latex_name = 'blub')\nf\nsage: eq = solve((ln(f(x))==x).diff(x), f(x))[0]\nsage: eq\nf(x) == D[0](f)(x)\nsage: eq.subs(f(x) == 42)\nf(x) == D[0](f)(x)\n```\n\nNote that\n\n```\nsage: bool(eq.lhs() == f(x))\nTrue\nsage: id(eq.lhs()), id(f(x))\n(119698064, 119698064)\n```\n\n\nWhen removing `latex_name`, everything works fine:\n\n```\nsage: function('f')\nf\nsage: eq = solve((ln(f(x))==x).diff(x), f(x))[0]\nsage: eq.subs(f(x) == 42)\n42 == D[0](f)(x)\n```\n\n\nIssue created by migration from https://trac.sagemath.org/ticket/15733\n\n",
    "created_at": "2014-01-26T11:08:52Z",
    "labels": [
        "symbolics",
        "major",
        "bug"
    ],
    "milestone": "https://github.com/sagemath/sagetest/milestones/sage-6.4",
    "title": "substitution in symbolic function fails with latex_name",
    "type": "issue",
    "url": "https://github.com/sagemath/sagetest/issues/15496",
    "user": "@dkrenn"
}
```
Keywords: substitute, function, solve, latex_name

The following substitution does not work:

```
sage: function('f', latex_name = 'blub')
f
sage: eq = solve((ln(f(x))==x).diff(x), f(x))[0]
sage: eq
f(x) == D[0](f)(x)
sage: eq.subs(f(x) == 42)
f(x) == D[0](f)(x)
```

Note that

```
sage: bool(eq.lhs() == f(x))
True
sage: id(eq.lhs()), id(f(x))
(119698064, 119698064)
```


When removing `latex_name`, everything works fine:

```
sage: function('f')
f
sage: eq = solve((ln(f(x))==x).diff(x), f(x))[0]
sage: eq.subs(f(x) == 42)
42 == D[0](f)(x)
```


Issue created by migration from https://trac.sagemath.org/ticket/15733





---

archive/issue_comments_200218.json:
```json
{
    "body": "Still there in 6.6.",
    "created_at": "2015-04-16T20:14:20Z",
    "issue": "https://github.com/sagemath/sagetest/issues/15496",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/15496#issuecomment-200218",
    "user": "@dkrenn"
}
```

Still there in 6.6.



---

archive/issue_comments_200219.json:
```json
{
    "body": "Note that #6480 will change the r.h.s. too.",
    "created_at": "2017-02-22T08:32:30Z",
    "issue": "https://github.com/sagemath/sagetest/issues/15496",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/15496#issuecomment-200219",
    "user": "@rwst"
}
```

Note that #6480 will change the r.h.s. too.
