# Issue 12999: limit of expression that tends to zeta(1/2)

archive/issues_012999.json:
```json
{
    "body": "Assignee: @burcin\n\nKeywords: limit expression sum zeta mathematica\n\nThe following cannot be evaluated in Sage\n\n```\nsage: var('k N')\n(k, N)\nsage: (sum(1/sqrt(k),k,1,N) - 2*sqrt(N)).limit(N=oo)\nlimit(-2*sqrt(N) + sum(1/sqrt(k), k, 1, N), N, +Infinity)\n```\n\nBut Mathematica can\n\n```\nIn[1]:= Limit[Sum[1/Sqrt[k], {k, 1, N}] - 2 Sqrt[N], N -> \\[Infinity]]\n\n             1\nOut[1]= Zeta[-]\n             2\n```\n\n\nIssue created by migration from https://trac.sagemath.org/ticket/13171\n\n",
    "created_at": "2012-06-27T16:42:38Z",
    "labels": [
        "component: symbolics"
    ],
    "milestone": "https://github.com/sagemath/sagetest/milestones/sage-wishlist",
    "title": "limit of expression that tends to zeta(1/2)",
    "type": "issue",
    "url": "https://github.com/sagemath/sagetest/issues/12999",
    "user": "https://github.com/dkrenn"
}
```
Assignee: @burcin

Keywords: limit expression sum zeta mathematica

The following cannot be evaluated in Sage

```
sage: var('k N')
(k, N)
sage: (sum(1/sqrt(k),k,1,N) - 2*sqrt(N)).limit(N=oo)
limit(-2*sqrt(N) + sum(1/sqrt(k), k, 1, N), N, +Infinity)
```

But Mathematica can

```
In[1]:= Limit[Sum[1/Sqrt[k], {k, 1, N}] - 2 Sqrt[N], N -> \[Infinity]]

             1
Out[1]= Zeta[-]
             2
```


Issue created by migration from https://trac.sagemath.org/ticket/13171





---

archive/issue_comments_156958.json:
```json
{
    "body": "Is this only a problem in the way we convert `sum()` to maxima? Otherwise, this ticket should be put in the `sage-wishlist` milestone.",
    "created_at": "2012-06-28T09:08:18Z",
    "issue": "https://github.com/sagemath/sagetest/issues/12999",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/12999#issuecomment-156958",
    "user": "https://github.com/burcin"
}
```

Is this only a problem in the way we convert `sum()` to maxima? Otherwise, this ticket should be put in the `sage-wishlist` milestone.
