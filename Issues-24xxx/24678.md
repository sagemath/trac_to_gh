# Issue 24678: Add class for greedy linear extensions

archive/issues_024441.json:
```json
{
    "body": "This patch adds a class for greedy linear extensions.\n\nCC:  @tscrim\n\nAuthor: Jori M\u00e4ntysalo\n\nBranch: u/jmantysalo/greedy-class\n\nStatus: new\n\nDependencies: #24639\n\nCommit: ddf5a93ec05d15dc50c1917a04d939964b9f56b5\n\nIssue created by migration from https://trac.sagemath.org/ticket/24678\n\n",
    "created_at": "2018-02-07T09:29:40Z",
    "labels": [
        "component: combinatorics"
    ],
    "milestone": "https://github.com/sagemath/sagetest/milestones/sage-8.2",
    "title": "Add class for greedy linear extensions",
    "type": "issue",
    "url": "https://github.com/sagemath/sagetest/issues/24678",
    "user": "https://github.com/jm58660"
}
```
This patch adds a class for greedy linear extensions.

CC:  @tscrim

Author: Jori Mäntysalo

Branch: u/jmantysalo/greedy-class

Status: new

Dependencies: #24639

Commit: ddf5a93ec05d15dc50c1917a04d939964b9f56b5

Issue created by migration from https://trac.sagemath.org/ticket/24678





---

archive/issue_comments_468511.json:
```json
{
    "body": "Changing branch from \"\" to \"u/jmantysalo/greedy-class\"",
    "created_at": "2018-02-07T09:30:06Z",
    "issue": "https://github.com/sagemath/sagetest/issues/24678",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/24678#issuecomment-468511",
    "user": "https://github.com/jm58660"
}
```

Changing branch from "" to "u/jmantysalo/greedy-class"



---

archive/issue_comments_468512.json:
```json
{
    "body": "Changing commit from \"\" to \"10da60ded9ceddc83cf9745276ae6f892b13fcb1\"",
    "created_at": "2018-02-07T09:31:54Z",
    "issue": "https://github.com/sagemath/sagetest/issues/24678",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/24678#issuecomment-468512",
    "user": "https://github.com/jm58660"
}
```

Changing commit from "" to "10da60ded9ceddc83cf9745276ae6f892b13fcb1"



---

archive/issue_comments_468513.json:
```json
{
    "body": "<a id='comment:2'></a>First ever Python class by me. At least some of this works.\n\n---\nNew commits:",
    "created_at": "2018-02-07T09:31:54Z",
    "issue": "https://github.com/sagemath/sagetest/issues/24678",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/24678#issuecomment-468513",
    "user": "https://github.com/jm58660"
}
```

<a id='comment:2'></a>First ever Python class by me. At least some of this works.

---
New commits:



---

archive/issue_comments_468514.json:
```json
{
    "body": "<a id='comment:3'></a>At least `tau` returns wrong type.",
    "created_at": "2018-02-07T10:20:23Z",
    "issue": "https://github.com/sagemath/sagetest/issues/24678",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/24678#issuecomment-468514",
    "user": "https://github.com/jm58660"
}
```

<a id='comment:3'></a>At least `tau` returns wrong type.



---

archive/issue_comments_468515.json:
```json
{
    "body": "<a id='comment:4'></a>In some ways, this is not an ideal way to learn how to write a class. Although after this, writing any other class will be a cakewalk (i.e., extremely easy). I will take a more detailed look in the morning when I get to my office (I am on Australian time) to see what a good approach might be. If you could tell me all of the methods which currently return the wrong type, that would be useful.",
    "created_at": "2018-02-07T13:04:51Z",
    "issue": "https://github.com/sagemath/sagetest/issues/24678",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/24678#issuecomment-468515",
    "user": "https://github.com/tscrim"
}
```

<a id='comment:4'></a>In some ways, this is not an ideal way to learn how to write a class. Although after this, writing any other class will be a cakewalk (i.e., extremely easy). I will take a more detailed look in the morning when I get to my office (I am on Australian time) to see what a good approach might be. If you could tell me all of the methods which currently return the wrong type, that would be useful.



---

archive/issue_comments_468516.json:
```json
{
    "body": "<a id='comment:5'></a>Thanks, and there is no horry at all.\n\nFunctions are `evacuation`, `tau` and `promotion`, but propably I can do two if I see how to make one.",
    "created_at": "2018-02-07T13:23:16Z",
    "issue": "https://github.com/sagemath/sagetest/issues/24678",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/24678#issuecomment-468516",
    "user": "https://github.com/jm58660"
}
```

<a id='comment:5'></a>Thanks, and there is no horry at all.

Functions are `evacuation`, `tau` and `promotion`, but propably I can do two if I see how to make one.



---

archive/issue_comments_468517.json:
```json
{
    "body": "<a id='comment:6'></a>It looks like it is sufficient to just overwrite `tau` with something like\n\n```python\n    def tau(self, i):\n        P = self.poset()\n        a = self[i-1]\n        b = self[i  ]\n        if P.lt(a,b) or P.lt(b,a):\n            return self\n        q = list(self)\n        q[i-1] = b\n        q[i  ] = a\n        return P.linear_extensions()(q, check=False) # We know this is a linear extension\n```\nNote that this will never return a greedy linear extension class, but since `evacuation` and `promotion` use `tau`, those will return standard linear extensions by default.",
    "created_at": "2018-02-07T22:48:04Z",
    "issue": "https://github.com/sagemath/sagetest/issues/24678",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/24678#issuecomment-468517",
    "user": "https://github.com/tscrim"
}
```

<a id='comment:6'></a>It looks like it is sufficient to just overwrite `tau` with something like

```python
    def tau(self, i):
        P = self.poset()
        a = self[i-1]
        b = self[i  ]
        if P.lt(a,b) or P.lt(b,a):
            return self
        q = list(self)
        q[i-1] = b
        q[i  ] = a
        return P.linear_extensions()(q, check=False) # We know this is a linear extension
```
Note that this will never return a greedy linear extension class, but since `evacuation` and `promotion` use `tau`, those will return standard linear extensions by default.



---

archive/issue_comments_468518.json:
```json
{
    "body": "<a id='comment:7'></a>Replying to [tscrim](#comment%3A6):\n> It looks like it is sufficient to just overwrite `tau` with something like\n\n\nAlso `return self` must be corrected. But is this supposed to be in the class `GreedyLinearExtension`? If so, I think for a user it is better to have `_tau()` that is called from `tau()`, so that there is no visible function with the same name.\n\nSo, in any case we need classes `LinearExtensionOfPoset` (without `s`) and `LinearExtensionsOfPoset`.",
    "created_at": "2018-02-08T07:25:20Z",
    "issue": "https://github.com/sagemath/sagetest/issues/24678",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/24678#issuecomment-468518",
    "user": "https://github.com/jm58660"
}
```

<a id='comment:7'></a>Replying to [tscrim](#comment%3A6):
> It looks like it is sufficient to just overwrite `tau` with something like


Also `return self` must be corrected. But is this supposed to be in the class `GreedyLinearExtension`? If so, I think for a user it is better to have `_tau()` that is called from `tau()`, so that there is no visible function with the same name.

So, in any case we need classes `LinearExtensionOfPoset` (without `s`) and `LinearExtensionsOfPoset`.



---

archive/issue_comments_468519.json:
```json
{
    "body": "<a id='comment:8'></a>Replying to [jmantysalo](#comment%3A7):\n> Replying to [tscrim](#comment%3A6):\n> > It looks like it is sufficient to just overwrite `tau` with something like\n\n> \n> Also `return self` must be corrected. But is this supposed to be in the class `GreedyLinearExtension`? If so, I think for a user it is better to have `_tau()` that is called from `tau()`, so that there is no visible function with the same name.\n\n\nIt is okay to do `return self` as the greedy class will be a subclass of the generic. Polymorphism (or duck-typing if you prefer) takes care of the rest. You don't need a `_tau()` method.\n\n> So, in any case we need classes `LinearExtensionOfPoset` (without `s`) and `LinearExtensionsOfPoset`.\n\n\nRight, we still need those classes. I wasn't suggesting otherwise.",
    "created_at": "2018-02-08T07:32:01Z",
    "issue": "https://github.com/sagemath/sagetest/issues/24678",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/24678#issuecomment-468519",
    "user": "https://github.com/tscrim"
}
```

<a id='comment:8'></a>Replying to [jmantysalo](#comment%3A7):
> Replying to [tscrim](#comment%3A6):
> > It looks like it is sufficient to just overwrite `tau` with something like

> 
> Also `return self` must be corrected. But is this supposed to be in the class `GreedyLinearExtension`? If so, I think for a user it is better to have `_tau()` that is called from `tau()`, so that there is no visible function with the same name.


It is okay to do `return self` as the greedy class will be a subclass of the generic. Polymorphism (or duck-typing if you prefer) takes care of the rest. You don't need a `_tau()` method.

> So, in any case we need classes `LinearExtensionOfPoset` (without `s`) and `LinearExtensionsOfPoset`.


Right, we still need those classes. I wasn't suggesting otherwise.



---

archive/issue_comments_468520.json:
```json
{
    "body": "<a id='comment:9'></a>Replying to [tscrim](#comment%3A8):\n\n> It is okay to do `return self` as the greedy class will be a subclass of the generic. Polymorphism (or duck-typing if you prefer) takes care of the rest. You don't need a `_tau()` method.\n\n\nOK, trying to understand... But `type(12/4)` says that it is rational, i.e. return value is based on input type, not by input value. But so it OK to have `tau(3)` to return a (generic) linear extension and `tau(4)` to return a greedy linear extension?",
    "created_at": "2018-02-08T07:35:26Z",
    "issue": "https://github.com/sagemath/sagetest/issues/24678",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/24678#issuecomment-468520",
    "user": "https://github.com/jm58660"
}
```

<a id='comment:9'></a>Replying to [tscrim](#comment%3A8):

> It is okay to do `return self` as the greedy class will be a subclass of the generic. Polymorphism (or duck-typing if you prefer) takes care of the rest. You don't need a `_tau()` method.


OK, trying to understand... But `type(12/4)` says that it is rational, i.e. return value is based on input type, not by input value. But so it OK to have `tau(3)` to return a (generic) linear extension and `tau(4)` to return a greedy linear extension?



---

archive/issue_comments_468521.json:
```json
{
    "body": "<a id='comment:10'></a>Replying to [jmantysalo](#comment%3A9):\n> Replying to [tscrim](#comment%3A8):\n> \n> > It is okay to do `return self` as the greedy class will be a subclass of the generic. Polymorphism (or duck-typing if you prefer) takes care of the rest. You don't need a `_tau()` method.\n\n> \n> OK, trying to understand... But `type(12/4)` says that it is rational, i.e. return value is based on input type, not by input value. But so it OK to have `tau(3)` to return a (generic) linear extension and `tau(4)` to return a greedy linear extension?\n\n\nIn principle we could have `12/4` be an `Integer`, but that would mean checking the denominator, which has a performance penalty (yes, it is minor, but it matters because of how essential arithmetic operations are). Because of duck-typing, we generally do not care about the distinction between `Rational` and `Integer`. Perhaps a more fair example would be\n\n```\nsage: R.<x> = LaurentPolynomialRing(ZZ)\nsage: type(1/x)\n<type 'sage.rings.polynomial.laurent_polynomial.LaurentPolynomial_univariate'>\nsage: type(1/(x+1))\n<type 'sage.rings.fraction_field_element.FractionFieldElement'>\n```\nHowever, this is not quite our case because we are returning a *subclass*, which is a stronger notion because it will satisfy, e.g., an `isinstance(p, LinearExtensionOfPoset)` check and it has to have the same API as `LinearExtensionOfPoset`.",
    "created_at": "2018-02-08T07:41:32Z",
    "issue": "https://github.com/sagemath/sagetest/issues/24678",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/24678#issuecomment-468521",
    "user": "https://github.com/tscrim"
}
```

<a id='comment:10'></a>Replying to [jmantysalo](#comment%3A9):
> Replying to [tscrim](#comment%3A8):
> 
> > It is okay to do `return self` as the greedy class will be a subclass of the generic. Polymorphism (or duck-typing if you prefer) takes care of the rest. You don't need a `_tau()` method.

> 
> OK, trying to understand... But `type(12/4)` says that it is rational, i.e. return value is based on input type, not by input value. But so it OK to have `tau(3)` to return a (generic) linear extension and `tau(4)` to return a greedy linear extension?


In principle we could have `12/4` be an `Integer`, but that would mean checking the denominator, which has a performance penalty (yes, it is minor, but it matters because of how essential arithmetic operations are). Because of duck-typing, we generally do not care about the distinction between `Rational` and `Integer`. Perhaps a more fair example would be

```
sage: R.<x> = LaurentPolynomialRing(ZZ)
sage: type(1/x)
<type 'sage.rings.polynomial.laurent_polynomial.LaurentPolynomial_univariate'>
sage: type(1/(x+1))
<type 'sage.rings.fraction_field_element.FractionFieldElement'>
```
However, this is not quite our case because we are returning a *subclass*, which is a stronger notion because it will satisfy, e.g., an `isinstance(p, LinearExtensionOfPoset)` check and it has to have the same API as `LinearExtensionOfPoset`.



---

archive/issue_comments_468522.json:
```json
{
    "body": "<a id='comment:11'></a>OK, I will proceed with this information.",
    "created_at": "2018-02-08T07:47:27Z",
    "issue": "https://github.com/sagemath/sagetest/issues/24678",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/24678#issuecomment-468522",
    "user": "https://github.com/jm58660"
}
```

<a id='comment:11'></a>OK, I will proceed with this information.



---

archive/issue_comments_468523.json:
```json
{
    "body": "Changing commit from \"10da60ded9ceddc83cf9745276ae6f892b13fcb1\" to \"ddf5a93ec05d15dc50c1917a04d939964b9f56b5\"",
    "created_at": "2018-02-08T10:02:36Z",
    "issue": "https://github.com/sagemath/sagetest/issues/24678",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/24678#issuecomment-468523",
    "user": "https://trac.sagemath.org/admin/accounts/users/git"
}
```

Changing commit from "10da60ded9ceddc83cf9745276ae6f892b13fcb1" to "ddf5a93ec05d15dc50c1917a04d939964b9f56b5"



---

archive/issue_comments_468524.json:
```json
{
    "body": "<a id='comment:12'></a>Branch pushed to git repo; I updated commit sha1. New commits:",
    "created_at": "2018-02-08T10:02:36Z",
    "issue": "https://github.com/sagemath/sagetest/issues/24678",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/24678#issuecomment-468524",
    "user": "https://trac.sagemath.org/admin/accounts/users/git"
}
```

<a id='comment:12'></a>Branch pushed to git repo; I updated commit sha1. New commits:



---

archive/issue_comments_468525.json:
```json
{
    "body": "<a id='comment:13'></a>Note, what I was saying was not to *change* `LinearExtensionOfPoset.tau()`, but to override it with a completely *new* method in the subclass `GreedyLinearExtensionOfPoset`.",
    "created_at": "2018-02-08T12:16:38Z",
    "issue": "https://github.com/sagemath/sagetest/issues/24678",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/24678#issuecomment-468525",
    "user": "https://github.com/tscrim"
}
```

<a id='comment:13'></a>Note, what I was saying was not to *change* `LinearExtensionOfPoset.tau()`, but to override it with a completely *new* method in the subclass `GreedyLinearExtensionOfPoset`.



---

archive/issue_comments_468526.json:
```json
{
    "body": "<a id='comment:14'></a>Replying to [tscrim](#comment%3A13):\n> Note, what I was saying was not to *change* `LinearExtensionOfPoset.tau()`, but to override it with a completely *new* method in the subclass `GreedyLinearExtensionOfPoset`.\n\n\nBut that means that the user sees documentation for both `LinearExtensionOfPoset.tau()` and `GreedyLinearExtensionOfPoset.tau()` even when they have no difference.",
    "created_at": "2018-02-08T12:45:39Z",
    "issue": "https://github.com/sagemath/sagetest/issues/24678",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/24678#issuecomment-468526",
    "user": "https://github.com/jm58660"
}
```

<a id='comment:14'></a>Replying to [tscrim](#comment%3A13):
> Note, what I was saying was not to *change* `LinearExtensionOfPoset.tau()`, but to override it with a completely *new* method in the subclass `GreedyLinearExtensionOfPoset`.


But that means that the user sees documentation for both `LinearExtensionOfPoset.tau()` and `GreedyLinearExtensionOfPoset.tau()` even when they have no difference.



---

archive/issue_comments_468527.json:
```json
{
    "body": "<a id='comment:15'></a>Replying to [jmantysalo](#comment%3A14):\n> Replying to [tscrim](#comment%3A13):\n> > Note, what I was saying was not to *change* `LinearExtensionOfPoset.tau()`, but to override it with a completely *new* method in the subclass `GreedyLinearExtensionOfPoset`.\n\n> \n> But that means that the user sees documentation for both `LinearExtensionOfPoset.tau()` and `GreedyLinearExtensionOfPoset.tau()` even when they have no difference.\n\n\nWell, in the built doc for each individual class, in principle, yes. However, they will have different doctests, specifically, the latter one should demonstrate that it is only a generic linear extension in a non-trivial case. By doing `L.tau?`, they will only see one.",
    "created_at": "2018-02-08T12:52:29Z",
    "issue": "https://github.com/sagemath/sagetest/issues/24678",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sagetest/issues/24678#issuecomment-468527",
    "user": "https://github.com/tscrim"
}
```

<a id='comment:15'></a>Replying to [jmantysalo](#comment%3A14):
> Replying to [tscrim](#comment%3A13):
> > Note, what I was saying was not to *change* `LinearExtensionOfPoset.tau()`, but to override it with a completely *new* method in the subclass `GreedyLinearExtensionOfPoset`.

> 
> But that means that the user sees documentation for both `LinearExtensionOfPoset.tau()` and `GreedyLinearExtensionOfPoset.tau()` even when they have no difference.


Well, in the built doc for each individual class, in principle, yes. However, they will have different doctests, specifically, the latter one should demonstrate that it is only a generic linear extension in a non-trivial case. By doing `L.tau?`, they will only see one.
