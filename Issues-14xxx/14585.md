# Issue 14585: Make lazy_format and lazy_string faster

archive/issues_014381.json:
```json
{
    "assignees": [
        "https://github.com/robertwb"
    ],
    "body": "At [sage-devel](https://groups.google.com/forum/?fromgroups=#!topic/sage-devel/Gnw389Bd-0k), Nils suggests to use some kind of lazy object for the error messages of errors that are deeply buried in the coercion model.\n\nThis approach is similar to what we already do with `sage.structure.misc.AttributeErrorMassage`. However, this is specifically restricted to error messages of attribute errors. In the more general case, it makes sense to use either `sage.misc.lazy_format.LazyFormat` or `sage.misc.lazy_string.lazy_string`. The latter seems to be faster, according to these timings that are according to Nils' example:\n\n```\nsage: from sage.misc.lazy_string import lazy_string\nsage: f = lambda op,A,B:\"unsupported operand parent(s) for '%s': '%s' and '%s'\"%(op,A,B)\nsage: R = GF(5)\nsage: S = GF(3)\nsage: %timeit Exception(lazy_string(f, '+', R, S))\n1000000 loops, best of 3: 1.47 us per loop\nsage: lazy_string(f, '+', R, S)\nl\"unsupported operand parent(s) for '+': 'Finite Field of size 5' and 'Finite Field of size 3'\"\nsage: %timeit Exception(\"unsupported operand parent(s) for '%s': '%s' and '%s'\"%('+', R, S))\n100000 loops, best of 3: 5.04 us per loop\nsage: from sage.misc.lazy_format import LazyFormat\nsage: ErrMess = LazyFormat(\"unsupported operand parent(s) for '%s': '%s' and '%s'\")\nsage: %timeit Exception(ErrMess%('+', R, S))\n10000 loops, best of 3: 23.1 us per loop\nsage: ErrMess%('+', R, S)\nunsupported operand parent(s) for '+': 'Finite Field of size 5' and 'Finite Field of size 3'\n```\nWe're better off making a fresh `LazyFormat` object every time, due to an inefficiency in the copying operation required for rebinding such an object:\n\n```\nsage: %timeit LazyFormat(\"unsupported operand parent(s) for '%s': '%s' and '%s'\")%('+', R, S)\n1000000 loops, best of 3: 2.08 \u00b5s per loop\n```\nLuckily, present use of `LazyFormat` follows this pattern.\n\nSo, it would already make sense to replace all error messages using string formatting by a call to `lazy_string`, while `LazyFormat` seems to be quite sluggish.\n\nTwo tasks:\n1. Make `lazy_string` and `LazyFormat` a lot faster (perhaps by using Cython)\n2. Use it for errors raised when trying to find coercions. The problem is particularly urgent if the string representation of an object is difficult to compute.\n\n**__Note__**\n\nFor the second task, we meanwhile have #14592\n\nApart from making coercion faster, there is the following problem that we would fix as well. Consider creation of a parent. It may happen that relatively early during initialisation, a coercion is needed, but only later, the string representation of the parent will become available. Hence, if there is an error raised (and caught) inside of the coercion system that relies on the string representation of the parent, then we are in trouble.\n\n__Apply__\n\n[attachment: trac14585-cythoned_lazy_string.patch](https://github.com/sagemath/sage/files/ticket14585/trac14585-cythoned_lazy_string.patch.gz)\n\nCC:  @nbruin @videlec\n\nBranch/Commit: **[96b61e3](https://github.com/sagemath/sagetrac-mirror/commit/96b61e39a4669296ea042d6f491997319dd36c5e)**\n\nReviewer: **Simon King, Jeroen Demeyer**\n\nAuthor: **Simon King, Jeroen Demeyer**\n\nComponent: **coercion**\n\n_Issue created by migration from https://trac.sagemath.org/ticket/14585_\n\n",
    "closed_at": "2015-07-27T15:16:00Z",
    "created_at": "2013-05-15T09:38:38Z",
    "labels": [
        "https://github.com/sagemath/sage/labels/c%3A%20coercion",
        "https://github.com/sagemath/sage/labels/p%3A%203%20%E2%80%93%20major",
        "https://github.com/sagemath/sage/labels/bug"
    ],
    "milestone": "https://github.com/sagemath/sage/milestones/sage-6.4",
    "reactions": [],
    "repository": "https://github.com/sagemath/sage",
    "title": "Make lazy_format and lazy_string faster",
    "type": "issue",
    "updated_at": "2015-07-27T15:16:00Z",
    "url": "https://github.com/sagemath/sage/issues/14585",
    "user": "https://github.com/simon-king-jena"
}
```
At [sage-devel](https://groups.google.com/forum/?fromgroups=#!topic/sage-devel/Gnw389Bd-0k), Nils suggests to use some kind of lazy object for the error messages of errors that are deeply buried in the coercion model.

This approach is similar to what we already do with `sage.structure.misc.AttributeErrorMassage`. However, this is specifically restricted to error messages of attribute errors. In the more general case, it makes sense to use either `sage.misc.lazy_format.LazyFormat` or `sage.misc.lazy_string.lazy_string`. The latter seems to be faster, according to these timings that are according to Nils' example:

```
sage: from sage.misc.lazy_string import lazy_string
sage: f = lambda op,A,B:"unsupported operand parent(s) for '%s': '%s' and '%s'"%(op,A,B)
sage: R = GF(5)
sage: S = GF(3)
sage: %timeit Exception(lazy_string(f, '+', R, S))
1000000 loops, best of 3: 1.47 us per loop
sage: lazy_string(f, '+', R, S)
l"unsupported operand parent(s) for '+': 'Finite Field of size 5' and 'Finite Field of size 3'"
sage: %timeit Exception("unsupported operand parent(s) for '%s': '%s' and '%s'"%('+', R, S))
100000 loops, best of 3: 5.04 us per loop
sage: from sage.misc.lazy_format import LazyFormat
sage: ErrMess = LazyFormat("unsupported operand parent(s) for '%s': '%s' and '%s'")
sage: %timeit Exception(ErrMess%('+', R, S))
10000 loops, best of 3: 23.1 us per loop
sage: ErrMess%('+', R, S)
unsupported operand parent(s) for '+': 'Finite Field of size 5' and 'Finite Field of size 3'
```
We're better off making a fresh `LazyFormat` object every time, due to an inefficiency in the copying operation required for rebinding such an object:

```
sage: %timeit LazyFormat("unsupported operand parent(s) for '%s': '%s' and '%s'")%('+', R, S)
1000000 loops, best of 3: 2.08 µs per loop
```
Luckily, present use of `LazyFormat` follows this pattern.

So, it would already make sense to replace all error messages using string formatting by a call to `lazy_string`, while `LazyFormat` seems to be quite sluggish.

Two tasks:
1. Make `lazy_string` and `LazyFormat` a lot faster (perhaps by using Cython)
2. Use it for errors raised when trying to find coercions. The problem is particularly urgent if the string representation of an object is difficult to compute.

**__Note__**

For the second task, we meanwhile have #14592

Apart from making coercion faster, there is the following problem that we would fix as well. Consider creation of a parent. It may happen that relatively early during initialisation, a coercion is needed, but only later, the string representation of the parent will become available. Hence, if there is an error raised (and caught) inside of the coercion system that relies on the string representation of the parent, then we are in trouble.

__Apply__

[attachment: trac14585-cythoned_lazy_string.patch](https://github.com/sagemath/sage/files/ticket14585/trac14585-cythoned_lazy_string.patch.gz)

CC:  @nbruin @videlec

Branch/Commit: **[96b61e3](https://github.com/sagemath/sagetrac-mirror/commit/96b61e39a4669296ea042d6f491997319dd36c5e)**

Reviewer: **Simon King, Jeroen Demeyer**

Author: **Simon King, Jeroen Demeyer**

Component: **coercion**

_Issue created by migration from https://trac.sagemath.org/ticket/14585_





---

archive/issue_events_206512.json:
```json
{
    "actor": "https://github.com/simon-king-jena",
    "created_at": "2013-05-15T09:38:38Z",
    "event": "milestoned",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "milestone_number": null,
    "milestone_title": "sage-5.11",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/14585#event-206512"
}
```



---

archive/issue_events_206513.json:
```json
{
    "actor": "https://github.com/simon-king-jena",
    "created_at": "2013-05-15T09:38:38Z",
    "event": "labeled",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "label": "https://github.com/sagemath/sage/labels/c%3A%20coercion",
    "label_color": "0000ff",
    "label_name": "c: coercion",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/14585#event-206513"
}
```



---

archive/issue_events_206514.json:
```json
{
    "actor": "https://github.com/simon-king-jena",
    "created_at": "2013-05-15T09:38:38Z",
    "event": "labeled",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "label": "https://github.com/sagemath/sage/labels/p%3A%203%20%E2%80%93%20major",
    "label_color": "ffbb00",
    "label_name": "p: 3 \u2013 major",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/14585#event-206514"
}
```



---

archive/issue_events_206515.json:
```json
{
    "actor": "https://github.com/simon-king-jena",
    "created_at": "2013-05-15T09:38:38Z",
    "event": "labeled",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "label": "https://github.com/sagemath/sage/labels/bug",
    "label_color": "d73a4a",
    "label_name": "bug",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/14585#event-206515"
}
```



---

archive/issue_events_206516.json:
```json
{
    "actor": "https://github.com/robertwb",
    "created_at": "2013-05-15T09:38:38Z",
    "event": "assigned",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "subject": "https://github.com/simon-king-jena",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/14585#event-206516"
}
```



---

archive/issue_comments_176795.json:
```json
{
    "body": "<div id=\"comment:1\" align=\"right\">comment:1</div>\n\nPS: It might make sense to put both `lazy_string` and `LazyFormat` into one file.",
    "created_at": "2013-05-15T09:40:58Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176795",
    "user": "https://github.com/simon-king-jena"
}
```

<div id="comment:1" align="right">comment:1</div>

PS: It might make sense to put both `lazy_string` and `LazyFormat` into one file.



---

archive/issue_comments_176796.json:
```json
{
    "body": "Author: **Simon King**",
    "created_at": "2013-05-15T10:08:53Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176796",
    "user": "https://github.com/simon-king-jena"
}
```

Author: **Simon King**



---

archive/issue_comments_176797.json:
```json
{
    "body": "<div id=\"comment:2\" align=\"right\">comment:2</div>\n\nHere is a first patch, that cythons lazy_string. Result:\n\n```\nsage: from sage.misc.lazy_string import lazy_string\nsage: f = lambda op,A,B:\"unsupported operand parent(s) for '%s': '%s' and '%s'\"%(op,A,B)\nsage: R = GF(5)\nsage: S = GF(3)\nsage: lazy_string(f, '+', R, S)\nl\"unsupported operand parent(s) for '+': 'Finite Field of size 5' and 'Finite Field of size 3'\"\nsage: %timeit Exception(lazy_string(f, '+', R, S))\n1000000 loops, best of 3: 595 ns per loop\n```\nSo, the time for raising the exception drops by more than 50% compared with the old Python lazy_string.\n\nNext will be lazy_format.",
    "created_at": "2013-05-15T10:08:53Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176797",
    "user": "https://github.com/simon-king-jena"
}
```

<div id="comment:2" align="right">comment:2</div>

Here is a first patch, that cythons lazy_string. Result:

```
sage: from sage.misc.lazy_string import lazy_string
sage: f = lambda op,A,B:"unsupported operand parent(s) for '%s': '%s' and '%s'"%(op,A,B)
sage: R = GF(5)
sage: S = GF(3)
sage: lazy_string(f, '+', R, S)
l"unsupported operand parent(s) for '+': 'Finite Field of size 5' and 'Finite Field of size 3'"
sage: %timeit Exception(lazy_string(f, '+', R, S))
1000000 loops, best of 3: 595 ns per loop
```
So, the time for raising the exception drops by more than 50% compared with the old Python lazy_string.

Next will be lazy_format.



---

archive/issue_comments_176798.json:
```json
{
    "body": "<div id=\"comment:3\" align=\"right\">comment:3</div>\n\nAvoiding the function call:\n\n```\nsage: from sage.misc.lazy_string import _LazyString\nsage: _LazyString(f, ('+', R, S), {})\nl\"unsupported operand parent(s) for '+': 'Finite Field of size 5' and 'Finite Field of size 3'\"\nsage: %timeit Exception(_LazyString(f, ('+', R, S), {}))\n1000000 loops, best of 3: 545 ns per loop\n```\nGoes down by another 10%.",
    "created_at": "2013-05-15T10:10:45Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176798",
    "user": "https://github.com/simon-king-jena"
}
```

<div id="comment:3" align="right">comment:3</div>

Avoiding the function call:

```
sage: from sage.misc.lazy_string import _LazyString
sage: _LazyString(f, ('+', R, S), {})
l"unsupported operand parent(s) for '+': 'Finite Field of size 5' and 'Finite Field of size 3'"
sage: %timeit Exception(_LazyString(f, ('+', R, S), {}))
1000000 loops, best of 3: 545 ns per loop
```
Goes down by another 10%.



---

archive/issue_comments_176799.json:
```json
{
    "body": "<div id=\"comment:4\" align=\"right\">comment:4</div>\n\nPerhaps we could use a dummy string (similar to what I did in `sage.structure.misc.AttributeErrorMessage`)? The additional code would be\n\n```\ncdef _LazyString _DummyLazyString = _LazyString(None, (), {})\n\ndef lazy_string_dummy(f, *args, **kwds):\n    _DummyLazyString._func = f\n    _DummyLazyString._args = args\n    _DummyLazyString._kwargs = kwds\n    return _DummyLazyString\n```\n\nAnd then, then one can raise errors even quicker:\n\n```\nsage: from sage.misc.lazy_string import lazy_string_dummy\nsage: f = lambda op,A,B:\"unsupported operand parent(s) for '%s': '%s' and '%s'\"%(op,A,B)\nsage: R = GF(5)\nsage: S = GF(3)\nsage: lazy_string_dummy(f, ('+', R, S), {})\nl\"unsupported operand parent(s) for '+': 'Finite Field of size 5' and 'Finite Field of size 3'\"\nsage: %timeit Exception(lazy_string_dummy(f, ('+', R, S), {}))\n1000000 loops, best of 3: 480 ns per loop\n```\n\nDo you think this would be feasible to have a unique message object for the errors raised and caught internally?",
    "created_at": "2013-05-15T10:22:25Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176799",
    "user": "https://github.com/simon-king-jena"
}
```

<div id="comment:4" align="right">comment:4</div>

Perhaps we could use a dummy string (similar to what I did in `sage.structure.misc.AttributeErrorMessage`)? The additional code would be

```
cdef _LazyString _DummyLazyString = _LazyString(None, (), {})

def lazy_string_dummy(f, *args, **kwds):
    _DummyLazyString._func = f
    _DummyLazyString._args = args
    _DummyLazyString._kwargs = kwds
    return _DummyLazyString
```

And then, then one can raise errors even quicker:

```
sage: from sage.misc.lazy_string import lazy_string_dummy
sage: f = lambda op,A,B:"unsupported operand parent(s) for '%s': '%s' and '%s'"%(op,A,B)
sage: R = GF(5)
sage: S = GF(3)
sage: lazy_string_dummy(f, ('+', R, S), {})
l"unsupported operand parent(s) for '+': 'Finite Field of size 5' and 'Finite Field of size 3'"
sage: %timeit Exception(lazy_string_dummy(f, ('+', R, S), {}))
1000000 loops, best of 3: 480 ns per loop
```

Do you think this would be feasible to have a unique message object for the errors raised and caught internally?



---

archive/issue_comments_176800.json:
```json
{
    "body": "<div id=\"comment:5\" align=\"right\">comment:5</div>\n\nFWIW, I have attached a second patch, which uses a lazy string dummy. I am not sure if an in-place change of an error message is a good thing. But it did work in the case of `AttributeError`, so, I would not drop the idea too soon.",
    "created_at": "2013-05-15T10:39:16Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176800",
    "user": "https://github.com/simon-king-jena"
}
```

<div id="comment:5" align="right">comment:5</div>

FWIW, I have attached a second patch, which uses a lazy string dummy. I am not sure if an in-place change of an error message is a good thing. But it did work in the case of `AttributeError`, so, I would not drop the idea too soon.



---

archive/issue_comments_176801.json:
```json
{
    "body": "<div id=\"comment:6\" align=\"right\">comment:6</div>\n\nHmm. To make it more scalable, it would be better to rename `lazy_string_dummy` into `update_lazy_string` and give it an existing lazy string, that is merely updated.",
    "created_at": "2013-05-15T14:40:33Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176801",
    "user": "https://github.com/simon-king-jena"
}
```

<div id="comment:6" align="right">comment:6</div>

Hmm. To make it more scalable, it would be better to rename `lazy_string_dummy` into `update_lazy_string` and give it an existing lazy string, that is merely updated.



---

archive/issue_comments_176802.json:
```json
{
    "body": "Does the reviewer think that an in-place change of a lazy string dummy makes sense?",
    "created_at": "2013-05-15T14:52:31Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176802",
    "user": "https://github.com/simon-king-jena"
}
```

Does the reviewer think that an in-place change of a lazy string dummy makes sense?



---

archive/issue_comments_176803.json:
```json
{
    "body": "<div id=\"comment:7\" align=\"right\">comment:7</div>\n\nAttachment: **[trac14585_dummy_lazy_string.patch.gz](https://github.com/sagemath/sage/files/ticket14585/trac14585_dummy_lazy_string.patch.gz)**\n\nOK, I have updated the second patch accordingly. There is now not just a single lazy dummy, but an arbitrary existing lazy string (with fixed underlying function) can be updated by providing new arguments to the underlying function.",
    "created_at": "2013-05-15T14:53:45Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176803",
    "user": "https://github.com/simon-king-jena"
}
```

<div id="comment:7" align="right">comment:7</div>

Attachment: **[trac14585_dummy_lazy_string.patch.gz](https://github.com/sagemath/sage/files/ticket14585/trac14585_dummy_lazy_string.patch.gz)**

OK, I have updated the second patch accordingly. There is now not just a single lazy dummy, but an arbitrary existing lazy string (with fixed underlying function) can be updated by providing new arguments to the underlying function.



---

archive/issue_comments_176804.json:
```json
{
    "body": "<div id=\"comment:8\" align=\"right\">comment:8</div>\n\nReplying to [@simon-king-jena](#comment:3):\n\n> sage: %timeit Exception(_LazyString(f, ('+', R, S), {}))\n\n- do we need the `{}` for kwargs?\n- does ditching it provide a worthwhile speedup?\n- how do we do if we instead have a `*kwargs` collect the arguments in a tuple rather than pass them as a tuple preformed?\n\nNote that `LazyFormat` would be completely analogous. In fact, internally they could be the same code, with just a flag that says \"interpolate f with args\" rather than \"call f with args\".\n\nThe latter has the advantage that it's a little easier: You don't have to prewrap a string in a lambda to get decent performance.",
    "created_at": "2013-05-15T15:13:03Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176804",
    "user": "https://github.com/nbruin"
}
```

<div id="comment:8" align="right">comment:8</div>

Replying to [@simon-king-jena](#comment:3):

> sage: %timeit Exception(_LazyString(f, ('+', R, S), {}))

- do we need the `{}` for kwargs?
- does ditching it provide a worthwhile speedup?
- how do we do if we instead have a `*kwargs` collect the arguments in a tuple rather than pass them as a tuple preformed?

Note that `LazyFormat` would be completely analogous. In fact, internally they could be the same code, with just a flag that says "interpolate f with args" rather than "call f with args".

The latter has the advantage that it's a little easier: You don't have to prewrap a string in a lambda to get decent performance.



---

archive/issue_comments_176805.json:
```json
{
    "body": "<div id=\"comment:9\" align=\"right\">comment:9</div>\n\nReplying to [@simon-king-jena](#comment:7):\n> OK, I have updated the second patch accordingly. There is now not just a single lazy dummy, but an arbitrary existing lazy string (with fixed underlying function) can be updated by providing new arguments to the underlying function.\n\nHm ... are these strings hashable (and hence immutable)? Then this would be bad. In fact, if they're hashable they NEED to be caching, which goes counter to existing use of lazy_string, where it's used as a window on an environment variable.\n\nI'd say at least underscore this method to make it clear it's bad. And only use it in our code if you can show that in real-world examples it makes a considerable difference (which I doubt: producing exceptions is only going to be a small percentage of total processing time, so possible gain is limited)",
    "created_at": "2013-05-15T15:18:55Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176805",
    "user": "https://github.com/nbruin"
}
```

<div id="comment:9" align="right">comment:9</div>

Replying to [@simon-king-jena](#comment:7):
> OK, I have updated the second patch accordingly. There is now not just a single lazy dummy, but an arbitrary existing lazy string (with fixed underlying function) can be updated by providing new arguments to the underlying function.

Hm ... are these strings hashable (and hence immutable)? Then this would be bad. In fact, if they're hashable they NEED to be caching, which goes counter to existing use of lazy_string, where it's used as a window on an environment variable.

I'd say at least underscore this method to make it clear it's bad. And only use it in our code if you can show that in real-world examples it makes a considerable difference (which I doubt: producing exceptions is only going to be a small percentage of total processing time, so possible gain is limited)



---

archive/issue_comments_176806.json:
```json
{
    "body": "<div id=\"comment:10\" align=\"right\">comment:10</div>\n\nReplying to [@nbruin](#comment:9):\n> Hm ... are these strings hashable (and hence immutable)?\n\nNo, it is not hashable.\n\n> I'd say at least underscore this method to make it clear it's bad.\n\nOr: cimport `_LazyString` in the coercion code and modify the arguments directly.\n\n> And only use it in our code if you can show that in real-world examples it makes a considerable difference (which I doubt: producing exceptions is only going to be a small percentage of total processing time, so possible gain is limited)\n\nSo I thought, before I worked on #14100. But there, the speed-up has been quite noticeable.\n\nOK, #14100 was about attribute errors, and I think accessing attributes is a more common operation than raising an error. So, it could be that you are right, and that it only contributes to a minor slowness. But a striking example should be: Test coercion of many different parents (which is typical for elliptic curve computations, it seems) into a parent that has an expensive string representation.\n\nAnyway, I think *improving* lazy_string should be separated from *using* lazy_string---the latter should be done on #14592.",
    "created_at": "2013-05-15T15:29:37Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176806",
    "user": "https://github.com/simon-king-jena"
}
```

<div id="comment:10" align="right">comment:10</div>

Replying to [@nbruin](#comment:9):
> Hm ... are these strings hashable (and hence immutable)?

No, it is not hashable.

> I'd say at least underscore this method to make it clear it's bad.

Or: cimport `_LazyString` in the coercion code and modify the arguments directly.

> And only use it in our code if you can show that in real-world examples it makes a considerable difference (which I doubt: producing exceptions is only going to be a small percentage of total processing time, so possible gain is limited)

So I thought, before I worked on #14100. But there, the speed-up has been quite noticeable.

OK, #14100 was about attribute errors, and I think accessing attributes is a more common operation than raising an error. So, it could be that you are right, and that it only contributes to a minor slowness. But a striking example should be: Test coercion of many different parents (which is typical for elliptic curve computations, it seems) into a parent that has an expensive string representation.

Anyway, I think *improving* lazy_string should be separated from *using* lazy_string---the latter should be done on #14592.



---

archive/issue_comments_176807.json:
```json
{
    "body": "<div id=\"comment:11\" align=\"right\">comment:11</div>\n\nReplying to [@nbruin](#comment:8):\n> Note that `LazyFormat` would be completely analogous. In fact, internally they could be the same code, with just a flag that says \"interpolate f with args\" rather than \"call f with args\".\n\nIn fact, perhaps just allow `LazyString` to be initialized with a string or a callable and interpolate with arguments rather than call with arguments if it's a string. That should be a very cheap test when the value gets requested.\n\nThe only difference with (present) `LazyFormat` would be that `LazyFormat` caches its value after first interpolation, but that's likely not an important trait (certainly not for our applications). Then you can basically just do away with `LazyFormat`.\n\nI did a quick experiment. For reference, I first tried the patch as given here:\n\n```\nsage: E=Exception(_LazyString(f, ('+', R, S)))\nsage: %timeit repr(E)\n100000 loops, best of 3: 6.76 us per loop\n```\n\nThen I tried it with\n\n```\n    @property\n    def value(self):\n        cdef object f=self._func\n        if PyString_Check(f):\n           return f%self._args\n        return f(*self._args)\n```\n\nwhich resulted in\n\n```\nsage: s=\"unsupported operand parent(s) for '%s': '%s' and '%s'\"\nsage: E=Exception(_LazyString(s, ('+', R, S)))\nsage: %timeit repr(E)\n100000 loops, best of 3: 6.48 us per loop\nsage: E=Exception(_LazyString(f, ('+', R, S)))\nsage: %timeit repr(E)\n100000 loops, best of 3: 6.54 us per loop\n```\nfrom which I don't dare to conclude that the code is MORE efficient with the branching, so I'll settle for not slower. (it seems putting or not putting a kwargs in there hardly makes a measurable difference, so we might as well).\n\nYou really do want to do a `PyString_Check` there. Doing an `isinstance` is noticeably slower.",
    "created_at": "2013-05-16T03:33:27Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176807",
    "user": "https://github.com/nbruin"
}
```

<div id="comment:11" align="right">comment:11</div>

Replying to [@nbruin](#comment:8):
> Note that `LazyFormat` would be completely analogous. In fact, internally they could be the same code, with just a flag that says "interpolate f with args" rather than "call f with args".

In fact, perhaps just allow `LazyString` to be initialized with a string or a callable and interpolate with arguments rather than call with arguments if it's a string. That should be a very cheap test when the value gets requested.

The only difference with (present) `LazyFormat` would be that `LazyFormat` caches its value after first interpolation, but that's likely not an important trait (certainly not for our applications). Then you can basically just do away with `LazyFormat`.

I did a quick experiment. For reference, I first tried the patch as given here:

```
sage: E=Exception(_LazyString(f, ('+', R, S)))
sage: %timeit repr(E)
100000 loops, best of 3: 6.76 us per loop
```

Then I tried it with

```
    @property
    def value(self):
        cdef object f=self._func
        if PyString_Check(f):
           return f%self._args
        return f(*self._args)
```

which resulted in

```
sage: s="unsupported operand parent(s) for '%s': '%s' and '%s'"
sage: E=Exception(_LazyString(s, ('+', R, S)))
sage: %timeit repr(E)
100000 loops, best of 3: 6.48 us per loop
sage: E=Exception(_LazyString(f, ('+', R, S)))
sage: %timeit repr(E)
100000 loops, best of 3: 6.54 us per loop
```
from which I don't dare to conclude that the code is MORE efficient with the branching, so I'll settle for not slower. (it seems putting or not putting a kwargs in there hardly makes a measurable difference, so we might as well).

You really do want to do a `PyString_Check` there. Doing an `isinstance` is noticeably slower.



---

archive/issue_comments_176808.json:
```json
{
    "body": "<div id=\"comment:12\" align=\"right\">comment:12</div>\n\nReplying to [@nbruin](#comment:11):\n> I did a quick experiment. For reference, I first tried the patch as given here:\n> \n> ```\n> sage: E=Exception(_LazyString(f, ('+', R, S)))\n> sage: %timeit repr(E)\n> 100000 loops, best of 3: 6.76 us per loop\n> ```\n> \n> Then I tried it with\n> \n> ```\n>     @property\n>     def value(self):\n>         cdef object f=self._func\n>         if PyString_Check(f):\n>            return f%self._args\n>         return f(*self._args)\n> ```\n> \n> which resulted in\n> \n> ```\n> sage: s=\"unsupported operand parent(s) for '%s': '%s' and '%s'\"\n> sage: E=Exception(_LazyString(s, ('+', R, S)))\n> sage: %timeit repr(E)\n> 100000 loops, best of 3: 6.48 us per loop\n> sage: E=Exception(_LazyString(f, ('+', R, S)))\n> sage: %timeit repr(E)\n> 100000 loops, best of 3: 6.54 us per loop\n> ```\n> from which I don't dare to conclude that the code is MORE efficient with the branching, so I'll settle for not slower. (it seems putting or not putting a kwargs in there hardly makes a measurable difference, so we might as well).\n> \n> You really do want to do a `PyString_Check` there. Doing an `isinstance` is noticeably slower.\n\nOK, I'll change the patch accordingly, also deprecating `LazyFormat`, and creating a pxd file for lazy_string, so that it will later become possible to change a lazy string in-place without calling a function (but instead by cimporting it and then directly inserting stuff into the cdef attributes).",
    "created_at": "2013-05-16T08:15:58Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176808",
    "user": "https://github.com/simon-king-jena"
}
```

<div id="comment:12" align="right">comment:12</div>

Replying to [@nbruin](#comment:11):
> I did a quick experiment. For reference, I first tried the patch as given here:
> 
> ```
> sage: E=Exception(_LazyString(f, ('+', R, S)))
> sage: %timeit repr(E)
> 100000 loops, best of 3: 6.76 us per loop
> ```
> 
> Then I tried it with
> 
> ```
>     @property
>     def value(self):
>         cdef object f=self._func
>         if PyString_Check(f):
>            return f%self._args
>         return f(*self._args)
> ```
> 
> which resulted in
> 
> ```
> sage: s="unsupported operand parent(s) for '%s': '%s' and '%s'"
> sage: E=Exception(_LazyString(s, ('+', R, S)))
> sage: %timeit repr(E)
> 100000 loops, best of 3: 6.48 us per loop
> sage: E=Exception(_LazyString(f, ('+', R, S)))
> sage: %timeit repr(E)
> 100000 loops, best of 3: 6.54 us per loop
> ```
> from which I don't dare to conclude that the code is MORE efficient with the branching, so I'll settle for not slower. (it seems putting or not putting a kwargs in there hardly makes a measurable difference, so we might as well).
> 
> You really do want to do a `PyString_Check` there. Doing an `isinstance` is noticeably slower.

OK, I'll change the patch accordingly, also deprecating `LazyFormat`, and creating a pxd file for lazy_string, so that it will later become possible to change a lazy string in-place without calling a function (but instead by cimporting it and then directly inserting stuff into the cdef attributes).



---

archive/issue_comments_176809.json:
```json
{
    "body": "Description changed:\n``````diff\n--- \n+++ \n@@ -28,3 +28,7 @@\n 2. Use it for errors raised when trying to find coercions. The problem is particularly urgent if the string representation of an object is difficult to compute.\n \n Apart from making coercion faster, there is the following problem that we would fix as well. Consider creation of a parent. It may happen that relatively early during initialisation, a coercion is needed, but only later, the string representation of the parent will become available. Hence, if there is an error raised (and caught) inside of the coercion system that relies on the string representation of the parent, then we are in trouble.\n+\n+__Apply__\n+\n+[attachment: trac14585-cythoned_lazy_string.patch](https://github.com/sagemath/sage/files/ticket14585/trac14585-cythoned_lazy_string.patch.gz)\n``````\n",
    "created_at": "2013-05-16T09:04:33Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176809",
    "user": "https://github.com/simon-king-jena"
}
```

Description changed:
``````diff
--- 
+++ 
@@ -28,3 +28,7 @@
 2. Use it for errors raised when trying to find coercions. The problem is particularly urgent if the string representation of an object is difficult to compute.
 
 Apart from making coercion faster, there is the following problem that we would fix as well. Consider creation of a parent. It may happen that relatively early during initialisation, a coercion is needed, but only later, the string representation of the parent will become available. Hence, if there is an error raised (and caught) inside of the coercion system that relies on the string representation of the parent, then we are in trouble.
+
+__Apply__
+
+[attachment: trac14585-cythoned_lazy_string.patch](https://github.com/sagemath/sage/files/ticket14585/trac14585-cythoned_lazy_string.patch.gz)
``````




---

archive/issue_comments_176810.json:
```json
{
    "body": "<div id=\"comment:13\" align=\"right\">comment:13</div>\n\nWith the updated patch,\n\n- `_LazyString` accepts both functions and format strings as arguments\n- It has a cpdef method allowing to change it in-place. I think that's better than a function modifying a global object.\n- I don't know how to deprecate `LazyFormat`. It has no `__init__` method (only the one inherited from str), and hence I don't know where to put a deprecation warning. But I added a comment in the documentation.\n- I added more tests.\n\nApply trac14585-cythoned_lazy_string.patch",
    "created_at": "2013-05-16T09:04:33Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176810",
    "user": "https://github.com/simon-king-jena"
}
```

<div id="comment:13" align="right">comment:13</div>

With the updated patch,

- `_LazyString` accepts both functions and format strings as arguments
- It has a cpdef method allowing to change it in-place. I think that's better than a function modifying a global object.
- I don't know how to deprecate `LazyFormat`. It has no `__init__` method (only the one inherited from str), and hence I don't know where to put a deprecation warning. But I added a comment in the documentation.
- I added more tests.

Apply trac14585-cythoned_lazy_string.patch



---

archive/issue_events_206517.json:
```json
{
    "actor": "https://github.com/simon-king-jena",
    "created_at": "2013-05-16T09:04:33Z",
    "event": "labeled",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "label": "https://github.com/sagemath/sage/labels/needs%20review",
    "label_color": "7fff00",
    "label_name": "needs review",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/14585#event-206517"
}
```



---

archive/issue_comments_176811.json:
```json
{
    "body": "Description changed:\n``````diff\n--- \n+++ \n@@ -27,6 +27,10 @@\n 1. Make `lazy_string` and `LazyFormat` a lot faster (perhaps by using Cython)\n 2. Use it for errors raised when trying to find coercions. The problem is particularly urgent if the string representation of an object is difficult to compute.\n \n+**__Note__**\n+\n+For the second task, we meanwhile have #14592\n+\n Apart from making coercion faster, there is the following problem that we would fix as well. Consider creation of a parent. It may happen that relatively early during initialisation, a coercion is needed, but only later, the string representation of the parent will become available. Hence, if there is an error raised (and caught) inside of the coercion system that relies on the string representation of the parent, then we are in trouble.\n \n __Apply__\n``````\n",
    "created_at": "2013-05-16T09:06:54Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176811",
    "user": "https://github.com/simon-king-jena"
}
```

Description changed:
``````diff
--- 
+++ 
@@ -27,6 +27,10 @@
 1. Make `lazy_string` and `LazyFormat` a lot faster (perhaps by using Cython)
 2. Use it for errors raised when trying to find coercions. The problem is particularly urgent if the string representation of an object is difficult to compute.
 
+**__Note__**
+
+For the second task, we meanwhile have #14592
+
 Apart from making coercion faster, there is the following problem that we would fix as well. Consider creation of a parent. It may happen that relatively early during initialisation, a coercion is needed, but only later, the string representation of the parent will become available. Hence, if there is an error raised (and caught) inside of the coercion system that relies on the string representation of the parent, then we are in trouble.
 
 __Apply__
``````




---

archive/issue_comments_176812.json:
```json
{
    "body": "<div id=\"comment:15\" align=\"right\">comment:15</div>\n\nReplying to [@simon-king-jena](#comment:13):\n> With the updated patch,\n> \n> - `_LazyString` accepts both functions and format strings as arguments\n> - It has a cpdef method allowing to change it in-place. I think that's better than a function modifying a global object.\n> - I don't know how to deprecate `LazyFormat`. It has no `__init__` method (only the one inherited from str), and hence I don't know where to put a deprecation warning. But I added a comment in the documentation.\n> - I added more tests.\n> \n> Apply trac14585-cythoned_lazy_string.patch\n\nHow about just changing `LazyFormat` to a factory function for `_LazyString` objects? Then we at least get the benefit (provided chaching behaviour is not required for it. Otherwise `LazyString` might grow a `cache_result` flag, if that doesn't affect performance.\n\nAlso, I think `_LazyString` has an easy enough interface to be called `LazyString` and be used directly (and then just leave `lazy_string` around for compatibility reasons).",
    "created_at": "2013-05-16T15:42:26Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176812",
    "user": "https://github.com/nbruin"
}
```

<div id="comment:15" align="right">comment:15</div>

Replying to [@simon-king-jena](#comment:13):
> With the updated patch,
> 
> - `_LazyString` accepts both functions and format strings as arguments
> - It has a cpdef method allowing to change it in-place. I think that's better than a function modifying a global object.
> - I don't know how to deprecate `LazyFormat`. It has no `__init__` method (only the one inherited from str), and hence I don't know where to put a deprecation warning. But I added a comment in the documentation.
> - I added more tests.
> 
> Apply trac14585-cythoned_lazy_string.patch

How about just changing `LazyFormat` to a factory function for `_LazyString` objects? Then we at least get the benefit (provided chaching behaviour is not required for it. Otherwise `LazyString` might grow a `cache_result` flag, if that doesn't affect performance.

Also, I think `_LazyString` has an easy enough interface to be called `LazyString` and be used directly (and then just leave `lazy_string` around for compatibility reasons).



---

archive/issue_events_206518.json:
```json
{
    "actor": "https://github.com/nbruin",
    "created_at": "2013-05-16T15:56:48Z",
    "event": "renamed",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "title_is": "Make lazy_format and lazy_string faster",
    "title_was": "Make lazy_format and lazy_string faster and use it internally in coercion",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/14585#event-206518"
}
```



---

archive/issue_comments_176813.json:
```json
{
    "body": "<div id=\"comment:16\" align=\"right\">comment:16</div>\n\nChanged ticket title to reflect that this ticket is about improving the routines themselves. Simon says that #14592 is for applying them more widely to error messages (if that turns out to be worthwhile)",
    "created_at": "2013-05-16T15:56:48Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176813",
    "user": "https://github.com/nbruin"
}
```

<div id="comment:16" align="right">comment:16</div>

Changed ticket title to reflect that this ticket is about improving the routines themselves. Simon says that #14592 is for applying them more widely to error messages (if that turns out to be worthwhile)



---

archive/issue_comments_176814.json:
```json
{
    "body": "<div id=\"comment:17\" align=\"right\">comment:17</div>\n\nI also realized that the following spelling leads to more efficient code in cython:\n\n```\n    property value:\n        def __get__(self):\n            cdef object f=self._func\n            if PyString_Check(f):\n               return f%self._args\n            return f(*self._args)\n```\nWith the old code:\n\n```\nsage: from sage.misc.lazy_string import lazy_string, _LazyString\nsage: f=lambda :\"hi\"\nsage: s=_LazyString(f,())\nsage: %timeit str(s)\n1000000 loops, best of 3: 425 ns per loop\n```\nand with cython property spelling:\n\n```\nsage: %timeit str(s)\n1000000 loops, best of 3: 282 ns per loop\n```\nFinally:\n\n```\nsage: f=lambda n:\"hi%s\"%n\nsage: s=_LazyString(f,('ho',))\nsage: %timeit str(s)\n1000000 loops, best of 3: 428 ns per loop\nsage: %timeit str(s)\n1000000 loops, best of 3: 423 ns per loop\nsage: f=\"hi%s\"\nsage: s=_LazyString(f,('ho',))\nsage: %timeit str(s)\n1000000 loops, best of 3: 319 ns per loop\nsage: %timeit str(s)\n1000000 loops, best of 3: 316 ns per loop\n```\nso special casing string values to directly interpolate seems to be a measurable saving (on something that doesn't seem to contribute much to total running time anyway)",
    "created_at": "2013-05-16T16:13:27Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176814",
    "user": "https://github.com/nbruin"
}
```

<div id="comment:17" align="right">comment:17</div>

I also realized that the following spelling leads to more efficient code in cython:

```
    property value:
        def __get__(self):
            cdef object f=self._func
            if PyString_Check(f):
               return f%self._args
            return f(*self._args)
```
With the old code:

```
sage: from sage.misc.lazy_string import lazy_string, _LazyString
sage: f=lambda :"hi"
sage: s=_LazyString(f,())
sage: %timeit str(s)
1000000 loops, best of 3: 425 ns per loop
```
and with cython property spelling:

```
sage: %timeit str(s)
1000000 loops, best of 3: 282 ns per loop
```
Finally:

```
sage: f=lambda n:"hi%s"%n
sage: s=_LazyString(f,('ho',))
sage: %timeit str(s)
1000000 loops, best of 3: 428 ns per loop
sage: %timeit str(s)
1000000 loops, best of 3: 423 ns per loop
sage: f="hi%s"
sage: s=_LazyString(f,('ho',))
sage: %timeit str(s)
1000000 loops, best of 3: 319 ns per loop
sage: %timeit str(s)
1000000 loops, best of 3: 316 ns per loop
```
so special casing string values to directly interpolate seems to be a measurable saving (on something that doesn't seem to contribute much to total running time anyway)



---

archive/issue_comments_176815.json:
```json
{
    "body": "<div id=\"comment:18\" align=\"right\">comment:18</div>\n\nReplying to [@nbruin](#comment:17):\n> I also realized that the following spelling leads to more efficient code in cython:\n> \n> ```\n>     property value:\n>         def __get__(self):\n>             cdef object f=self._func\n>             if PyString_Check(f):\n>                return f%self._args\n>             return f(*self._args)\n> ```\n\nIs this the Cython spelling of \"@property\"? Why can't Cython automatically transform Python's `@`property accordingly?",
    "created_at": "2013-05-16T16:22:14Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176815",
    "user": "https://github.com/simon-king-jena"
}
```

<div id="comment:18" align="right">comment:18</div>

Replying to [@nbruin](#comment:17):
> I also realized that the following spelling leads to more efficient code in cython:
> 
> ```
>     property value:
>         def __get__(self):
>             cdef object f=self._func
>             if PyString_Check(f):
>                return f%self._args
>             return f(*self._args)
> ```

Is this the Cython spelling of "@property"? Why can't Cython automatically transform Python's `@`property accordingly?



---

archive/issue_comments_176816.json:
```json
{
    "body": "<div id=\"comment:19\" align=\"right\">comment:19</div>\n\nCould the failures observed by the patchbot be due to this patch? How can it be amended?\n\nIf unpickling of `_LazyString` is the problem, then your suggestion to rename it into `LazyString` has another benefit: One could use the old `_LazyString` to convert old lazy strings to new lazy strings. I just don't know how (I guess pickling is by class and `__dict__`).",
    "created_at": "2013-05-16T16:25:43Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176816",
    "user": "https://github.com/simon-king-jena"
}
```

<div id="comment:19" align="right">comment:19</div>

Could the failures observed by the patchbot be due to this patch? How can it be amended?

If unpickling of `_LazyString` is the problem, then your suggestion to rename it into `LazyString` has another benefit: One could use the old `_LazyString` to convert old lazy strings to new lazy strings. I just don't know how (I guess pickling is by class and `__dict__`).



---

archive/issue_comments_176817.json:
```json
{
    "body": "<div id=\"comment:20\" align=\"right\">comment:20</div>\n\nReplying to [@simon-king-jena](#comment:18):\n> Is this the Cython spelling of \"@property\"? Why can't Cython automatically transform Python's `@`property accordingly?\n\nWell ... `@property` is valid in in cython as well, but has a different meaning and effect. I'm not sure if it's doable and desirable for cython to transcribe it to `property:` because most of the time that's what the author intends. We'll see: [cython-users thread](http://groups.google.com/group/cython-users/browse_thread/thread/1a3a6defe99c6fd0)\n\nIt's just that on C-API level some lower level hooks are available that `property:` can hook into.",
    "created_at": "2013-05-16T17:32:12Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176817",
    "user": "https://github.com/nbruin"
}
```

<div id="comment:20" align="right">comment:20</div>

Replying to [@simon-king-jena](#comment:18):
> Is this the Cython spelling of "@property"? Why can't Cython automatically transform Python's `@`property accordingly?

Well ... `@property` is valid in in cython as well, but has a different meaning and effect. I'm not sure if it's doable and desirable for cython to transcribe it to `property:` because most of the time that's what the author intends. We'll see: [cython-users thread](http://groups.google.com/group/cython-users/browse_thread/thread/1a3a6defe99c6fd0)

It's just that on C-API level some lower level hooks are available that `property:` can hook into.



---

archive/issue_comments_176818.json:
```json
{
    "body": "<div id=\"comment:21\" align=\"right\">comment:21</div>\n\nReplying to [@nbruin](#comment:17):\n>             return f(*self._args)\n\nand here you can shave a few nanoseconds off by using\n\n```\nreturn PyObject_CallObject(f,self._args)\n```\nor\n\n```\nreturn PyObject_Call(f,self._args,self._kwargs)\n```\nCython still generates some code to ensure that the resulting bit from * is a tuple and not a general iterable, but we've already forced it to be a tuple.",
    "created_at": "2013-05-16T22:19:18Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176818",
    "user": "https://github.com/nbruin"
}
```

<div id="comment:21" align="right">comment:21</div>

Replying to [@nbruin](#comment:17):
>             return f(*self._args)

and here you can shave a few nanoseconds off by using

```
return PyObject_CallObject(f,self._args)
```
or

```
return PyObject_Call(f,self._args,self._kwargs)
```
Cython still generates some code to ensure that the resulting bit from * is a tuple and not a general iterable, but we've already forced it to be a tuple.



---

archive/issue_comments_176819.json:
```json
{
    "body": "<div id=\"comment:22\" align=\"right\">comment:22</div>\n\nReplying to [@nbruin](#comment:21):\n> Replying to [@nbruin](#comment:17):\n> >             return f(*self._args)\n\n> and here you can shave a few nanoseconds off by using\n> \n> ```\n> return PyObject_CallObject(f,self._args)\n> ```\n> or\n> \n> ```\n> return PyObject_Call(f,self._args,self._kwargs)\n> ```\n> Cython still generates some code to ensure that the resulting bit from * is a tuple and not a general iterable, but we've already forced it to be a tuple.\n\nAre the two versions giving essentially the same C-code?",
    "created_at": "2013-05-17T05:11:03Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176819",
    "user": "https://github.com/simon-king-jena"
}
```

<div id="comment:22" align="right">comment:22</div>

Replying to [@nbruin](#comment:21):
> Replying to [@nbruin](#comment:17):
> >             return f(*self._args)

> and here you can shave a few nanoseconds off by using
> 
> ```
> return PyObject_CallObject(f,self._args)
> ```
> or
> 
> ```
> return PyObject_Call(f,self._args,self._kwargs)
> ```
> Cython still generates some code to ensure that the resulting bit from * is a tuple and not a general iterable, but we've already forced it to be a tuple.

Are the two versions giving essentially the same C-code?



---

archive/issue_comments_176820.json:
```json
{
    "body": "<div id=\"comment:23\" align=\"right\">comment:23</div>\n\nReplying to [@simon-king-jena](#comment:22):\n> > return PyObject_CallObject(f,self._args)\n> > return PyObject_Call(f,self._args,self._kwargs)\n\n \n> Are the two versions giving essentially the same C-code?\n\nI'm not entirely sure what you're asking, so I'll try to just generally clarify. If you write `f(*args)` then cython will produce code to package the contents of `args` into a tuple (since `args` could be any iterable). We don't need to do that.\n\nThe difference between `Call` and `CallObject` is whether there's a `kwargs` there. Not having that saves a couple of nanoseconds too.\n\nThese `PyObject_Call*` routines seem to be the official API for calling things, so Cython generates this too.",
    "created_at": "2013-05-17T06:12:27Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176820",
    "user": "https://github.com/nbruin"
}
```

<div id="comment:23" align="right">comment:23</div>

Replying to [@simon-king-jena](#comment:22):
> > return PyObject_CallObject(f,self._args)
> > return PyObject_Call(f,self._args,self._kwargs)

 
> Are the two versions giving essentially the same C-code?

I'm not entirely sure what you're asking, so I'll try to just generally clarify. If you write `f(*args)` then cython will produce code to package the contents of `args` into a tuple (since `args` could be any iterable). We don't need to do that.

The difference between `Call` and `CallObject` is whether there's a `kwargs` there. Not having that saves a couple of nanoseconds too.

These `PyObject_Call*` routines seem to be the official API for calling things, so Cython generates this too.



---

archive/issue_comments_176821.json:
```json
{
    "body": "<div id=\"comment:24\" align=\"right\">comment:24</div>\n\nReplying to [@nbruin](#comment:23):\n> The difference between `Call` and `CallObject` is whether there's a `kwargs` there. Not having that saves a couple of nanoseconds too.\n\nOK. I don't know if named arguments are really used, but the possibility to use them is currently provided. So, I think `PyObject_Call(f, args, kwargs)` should be the right thing to do.\n\nCan you confirm the unpickling errors reported by the patchbot?",
    "created_at": "2013-05-17T07:40:24Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176821",
    "user": "https://github.com/simon-king-jena"
}
```

<div id="comment:24" align="right">comment:24</div>

Replying to [@nbruin](#comment:23):
> The difference between `Call` and `CallObject` is whether there's a `kwargs` there. Not having that saves a couple of nanoseconds too.

OK. I don't know if named arguments are really used, but the possibility to use them is currently provided. So, I think `PyObject_Call(f, args, kwargs)` should be the right thing to do.

Can you confirm the unpickling errors reported by the patchbot?



---

archive/issue_comments_176822.json:
```json
{
    "body": "<div id=\"comment:25\" align=\"right\">comment:25</div>\n\nI think we should resume working on this patch. Currently, it fails to build with this error:\n\n```\nCython.Compiler.Errors.InternalError: Internal compiler error: '../ext/python_string.pxi' not found\nError installing modified sage library code.\n```\nHas this file been moved? Renamed even? I can not find it under SAGE_ROOT/devel/sage/.",
    "created_at": "2013-08-11T13:39:45Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176822",
    "user": "https://github.com/simon-king-jena"
}
```

<div id="comment:25" align="right">comment:25</div>

I think we should resume working on this patch. Currently, it fails to build with this error:

```
Cython.Compiler.Errors.InternalError: Internal compiler error: '../ext/python_string.pxi' not found
Error installing modified sage library code.
```
Has this file been moved? Renamed even? I can not find it under SAGE_ROOT/devel/sage/.



---

archive/issue_events_206519.json:
```json
{
    "actor": "https://github.com/simon-king-jena",
    "created_at": "2013-08-11T13:39:45Z",
    "event": "unlabeled",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "label": "https://github.com/sagemath/sage/labels/needs%20review",
    "label_color": "7fff00",
    "label_name": "needs review",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/14585#event-206519"
}
```



---

archive/issue_events_206520.json:
```json
{
    "actor": "https://github.com/simon-king-jena",
    "created_at": "2013-08-11T13:39:45Z",
    "event": "labeled",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "label": "https://github.com/sagemath/sage/labels/needs%20work",
    "label_color": "ffff00",
    "label_name": "needs work",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/14585#event-206520"
}
```



---

archive/issue_comments_176823.json:
```json
{
    "body": "<div id=\"comment:26\" align=\"right\">comment:26</div>\n\nReplying to [@simon-king-jena](#comment:25):\n> I think we should resume working on this patch. Currently, it fails to build with this error:\n> \n> ```\n> Cython.Compiler.Errors.InternalError: Internal compiler error: '../ext/python_string.pxi' not found\n> Error installing modified sage library code.\n> ```\n> Has this file been moved? Renamed even? I can not find it under SAGE_ROOT/devel/sage/.\n\nIt has been #6223.",
    "created_at": "2013-08-11T13:48:06Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176823",
    "user": "https://github.com/simon-king-jena"
}
```

<div id="comment:26" align="right">comment:26</div>

Replying to [@simon-king-jena](#comment:25):
> I think we should resume working on this patch. Currently, it fails to build with this error:
> 
> ```
> Cython.Compiler.Errors.InternalError: Internal compiler error: '../ext/python_string.pxi' not found
> Error installing modified sage library code.
> ```
> Has this file been moved? Renamed even? I can not find it under SAGE_ROOT/devel/sage/.

It has been #6223.



---

archive/issue_comments_176824.json:
```json
{
    "body": "Only apply this patch",
    "created_at": "2013-08-11T13:54:49Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176824",
    "user": "https://github.com/simon-king-jena"
}
```

Only apply this patch



---

archive/issue_events_206521.json:
```json
{
    "actor": "https://github.com/simon-king-jena",
    "created_at": "2013-08-11T13:55:52Z",
    "event": "unlabeled",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "label": "https://github.com/sagemath/sage/labels/needs%20work",
    "label_color": "ffff00",
    "label_name": "needs work",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/14585#event-206521"
}
```



---

archive/issue_events_206522.json:
```json
{
    "actor": "https://github.com/simon-king-jena",
    "created_at": "2013-08-11T13:55:52Z",
    "event": "labeled",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "label": "https://github.com/sagemath/sage/labels/needs%20review",
    "label_color": "7fff00",
    "label_name": "needs review",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/14585#event-206522"
}
```



---

archive/issue_comments_176825.json:
```json
{
    "body": "<div id=\"comment:27\" align=\"right\">comment:27</div>\n\nAttachment: **[trac14585-cythoned_lazy_string.patch.gz](https://github.com/sagemath/sage/files/ticket14585/trac14585-cythoned_lazy_string.patch.gz)**\n\nI have updated the patch so that it correctly builds. Note that I did not add the improvements suggested by Nils, yet.\n\nApply trac14585-cythoned_lazy_string.patch",
    "created_at": "2013-08-11T13:55:52Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176825",
    "user": "https://github.com/simon-king-jena"
}
```

<div id="comment:27" align="right">comment:27</div>

Attachment: **[trac14585-cythoned_lazy_string.patch.gz](https://github.com/sagemath/sage/files/ticket14585/trac14585-cythoned_lazy_string.patch.gz)**

I have updated the patch so that it correctly builds. Note that I did not add the improvements suggested by Nils, yet.

Apply trac14585-cythoned_lazy_string.patch



---

archive/issue_events_206523.json:
```json
{
    "actor": "https://github.com/jdemeyer",
    "created_at": "2013-08-13T15:35:53Z",
    "event": "demilestoned",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "milestone_number": null,
    "milestone_title": "sage-5.11",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/14585#event-206523"
}
```



---

archive/issue_events_206524.json:
```json
{
    "actor": "https://github.com/jdemeyer",
    "created_at": "2013-08-13T15:35:53Z",
    "event": "milestoned",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "milestone_number": null,
    "milestone_title": "sage-5.12",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/14585#event-206524"
}
```



---

archive/issue_comments_176826.json:
```json
{
    "body": "<div id=\"comment:29\" align=\"right\">comment:29</div>\n\nDon't forget:\n\n```\n    property value:\n        def __get__(self):\n            cdef object f=self._func\n            if PyString_Check(f):\n               return f%self._args\n            return f(*self._args)\n```\ninstead of the `@property` decorator leads to much better (and faster) cython code.",
    "created_at": "2013-08-22T22:31:08Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176826",
    "user": "https://github.com/nbruin"
}
```

<div id="comment:29" align="right">comment:29</div>

Don't forget:

```
    property value:
        def __get__(self):
            cdef object f=self._func
            if PyString_Check(f):
               return f%self._args
            return f(*self._args)
```
instead of the `@property` decorator leads to much better (and faster) cython code.



---

archive/issue_events_206525.json:
```json
{
    "actor": "https://github.com/sagetrac-vbraun-spam",
    "created_at": "2014-01-30T21:20:52Z",
    "event": "demilestoned",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "milestone_number": null,
    "milestone_title": "sage-6.1",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/14585#event-206525"
}
```



---

archive/issue_events_206526.json:
```json
{
    "actor": "https://github.com/sagetrac-vbraun-spam",
    "created_at": "2014-01-30T21:20:52Z",
    "event": "milestoned",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "milestone_number": null,
    "milestone_title": "sage-6.2",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/14585#event-206526"
}
```



---

archive/issue_events_206527.json:
```json
{
    "actor": "https://github.com/rwst",
    "created_at": "2014-03-31T07:45:32Z",
    "event": "unlabeled",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "label": "https://github.com/sagemath/sage/labels/needs%20review",
    "label_color": "7fff00",
    "label_name": "needs review",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/14585#event-206527"
}
```



---

archive/issue_events_206528.json:
```json
{
    "actor": "https://github.com/rwst",
    "created_at": "2014-03-31T07:45:32Z",
    "event": "labeled",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "label": "https://github.com/sagemath/sage/labels/needs%20work",
    "label_color": "ffff00",
    "label_name": "needs work",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/14585#event-206528"
}
```



---

archive/issue_events_206529.json:
```json
{
    "actor": "https://github.com/sagetrac-vbraun-spam",
    "created_at": "2014-05-06T15:20:58Z",
    "event": "demilestoned",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "milestone_number": null,
    "milestone_title": "sage-6.2",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/14585#event-206529"
}
```



---

archive/issue_events_206530.json:
```json
{
    "actor": "https://github.com/sagetrac-vbraun-spam",
    "created_at": "2014-05-06T15:20:58Z",
    "event": "milestoned",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "milestone_number": null,
    "milestone_title": "sage-6.3",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/14585#event-206530"
}
```



---

archive/issue_events_206531.json:
```json
{
    "actor": "https://github.com/sagetrac-vbraun-spam",
    "created_at": "2014-08-10T16:51:03Z",
    "event": "demilestoned",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "milestone_number": null,
    "milestone_title": "sage-6.3",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/14585#event-206531"
}
```



---

archive/issue_events_206532.json:
```json
{
    "actor": "https://github.com/sagetrac-vbraun-spam",
    "created_at": "2014-08-10T16:51:03Z",
    "event": "milestoned",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "milestone_number": null,
    "milestone_title": "sage-6.4",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/14585#event-206532"
}
```



---

archive/issue_comments_176827.json:
```json
{
    "body": "<div id=\"comment:34\" align=\"right\">comment:34</div>\n\nPart of why `LazyFormat` seems so sluggish presently is the strategy how it copies itself when asked to rebind. It will usually be cheaper to make a fresh `LazyFormat` object (in other words, we should make the rebinding just do that). The problem presently is that the use of `copy` leads to the desperate act of using `__reduce__` (!) to do the copying. Much better already:\n\n```\nsage: class A(str): pass\nsage: a=A(\"a\")\nsage: %timeit copy(a)\n100000 loops, best of 3: 13 \u00b5s per loop\nsage: %timeit A(str.__repr__(a))\n1000000 loops, best of 3: 558 ns per loop\nsage: %timeit A(\"a\")\n10000000 loops, best of 3: 258 ns per loop\n```\nso at the very least we should use the latter incantation in `LazyFormat.__mod__`, and it would be even nicer be able to get the string value of a in a cheaper way.",
    "created_at": "2015-04-28T07:08:22Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176827",
    "user": "https://github.com/nbruin"
}
```

<div id="comment:34" align="right">comment:34</div>

Part of why `LazyFormat` seems so sluggish presently is the strategy how it copies itself when asked to rebind. It will usually be cheaper to make a fresh `LazyFormat` object (in other words, we should make the rebinding just do that). The problem presently is that the use of `copy` leads to the desperate act of using `__reduce__` (!) to do the copying. Much better already:

```
sage: class A(str): pass
sage: a=A("a")
sage: %timeit copy(a)
100000 loops, best of 3: 13 µs per loop
sage: %timeit A(str.__repr__(a))
1000000 loops, best of 3: 558 ns per loop
sage: %timeit A("a")
10000000 loops, best of 3: 258 ns per loop
```
so at the very least we should use the latter incantation in `LazyFormat.__mod__`, and it would be even nicer be able to get the string value of a in a cheaper way.



---

archive/issue_comments_176828.json:
```json
{
    "body": "Description changed:\n``````diff\n--- \n+++ \n@@ -20,6 +20,13 @@\n sage: ErrMess%('+', R, S)\n unsupported operand parent(s) for '+': 'Finite Field of size 5' and 'Finite Field of size 3'\n ```\n+We're better off making a fresh `LazyFormat` object every time, due to an inefficiency in the copying operation required for rebinding such an object:\n+\n+```\n+sage: %timeit LazyFormat(\"unsupported operand parent(s) for '%s': '%s' and '%s'\")%('+', R, S)\n+1000000 loops, best of 3: 2.08 \u00b5s per loop\n+```\n+Luckily, present use of `LazyFormat` follows this pattern.\n \n So, it would already make sense to replace all error messages using string formatting by a call to `lazy_string`, while `LazyFormat` seems to be quite sluggish.\n \n``````\n",
    "created_at": "2015-04-28T07:17:39Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176828",
    "user": "https://github.com/nbruin"
}
```

Description changed:
``````diff
--- 
+++ 
@@ -20,6 +20,13 @@
 sage: ErrMess%('+', R, S)
 unsupported operand parent(s) for '+': 'Finite Field of size 5' and 'Finite Field of size 3'
 ```
+We're better off making a fresh `LazyFormat` object every time, due to an inefficiency in the copying operation required for rebinding such an object:
+
+```
+sage: %timeit LazyFormat("unsupported operand parent(s) for '%s': '%s' and '%s'")%('+', R, S)
+1000000 loops, best of 3: 2.08 µs per loop
+```
+Luckily, present use of `LazyFormat` follows this pattern.
 
 So, it would already make sense to replace all error messages using string formatting by a call to `lazy_string`, while `LazyFormat` seems to be quite sluggish.
 
``````




---

archive/issue_comments_176829.json:
```json
{
    "body": "<div id=\"comment:36\" align=\"right\">comment:36</div>\n\ncc'ing me... it would be cool to have a follow up ticket for cleaning most error messages and also advertise lazy strings in the developer manual.\n\nVincent",
    "created_at": "2015-04-28T08:58:27Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176829",
    "user": "https://github.com/videlec"
}
```

<div id="comment:36" align="right">comment:36</div>

cc'ing me... it would be cool to have a follow up ticket for cleaning most error messages and also advertise lazy strings in the developer manual.

Vincent



---

archive/issue_comments_176830.json:
```json
{
    "body": "Branch: **[u/SimonKing/Make_lazy_format_faster](https://github.com/sagemath/sagetrac-mirror/tree/u/SimonKing/Make_lazy_format_faster)**",
    "created_at": "2015-07-23T10:06:03Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176830",
    "user": "https://github.com/simon-king-jena"
}
```

Branch: **[u/SimonKing/Make_lazy_format_faster](https://github.com/sagemath/sagetrac-mirror/tree/u/SimonKing/Make_lazy_format_faster)**



---

archive/issue_comments_176831.json:
```json
{
    "body": "Commit: **[75796e9](https://github.com/sagemath/sagetrac-mirror/commit/75796e94a5742aff992e56d058717dd2e94a97bf)**",
    "created_at": "2015-07-23T10:09:02Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176831",
    "user": "https://github.com/simon-king-jena"
}
```

Commit: **[75796e9](https://github.com/sagemath/sagetrac-mirror/commit/75796e94a5742aff992e56d058717dd2e94a97bf)**



---

archive/issue_comments_176832.json:
```json
{
    "body": "<div id=\"comment:38\" align=\"right\">comment:38</div>\n\nI have converted the patch to a branch. Note that [comment:27](#comment:27) still applies: I did not add the improvements suggested by Nils, yet.\n\n---\nNew commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/75796e94a5742aff992e56d058717dd2e94a97bf\">75796e9</a></td><td><code>Cythoning lazy_string</code></td></tr></table>\n",
    "created_at": "2015-07-23T10:09:02Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176832",
    "user": "https://github.com/simon-king-jena"
}
```

<div id="comment:38" align="right">comment:38</div>

I have converted the patch to a branch. Note that [comment:27](#comment:27) still applies: I did not add the improvements suggested by Nils, yet.

---
New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/75796e94a5742aff992e56d058717dd2e94a97bf">75796e9</a></td><td><code>Cythoning lazy_string</code></td></tr></table>




---

archive/issue_comments_176833.json:
```json
{
    "body": "<div id=\"comment:39\" align=\"right\">comment:39</div>\n\nWith the current branch, I get\n\n```\nsage: from sage.misc.lazy_string import lazy_string, _LazyString\nsage: f=lambda :\"hi\"\nsage: s=_LazyString(f,(), {})\nsage: %timeit str(s)\nThe slowest run took 21.90 times longer than the fastest. This could mean that an intermediate result is being cached \n1000000 loops, best of 3: 827 ns per loop\n```\nWith the change that Nils suggested in [comment:29](#comment:29), I get\n\n```\nsage: from sage.misc.lazy_string import lazy_string, _LazyString\nsage: f=lambda :\"hi\"\nsage: s=_LazyString(f,(), {})\nsage: %timeit str(s)\nThe slowest run took 11.15 times longer than the fastest. This could mean that an intermediate result is being cached \n1000000 loops, best of 3: 535 ns per loop\n```\nPushing the change...",
    "created_at": "2015-07-23T10:24:21Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176833",
    "user": "https://github.com/simon-king-jena"
}
```

<div id="comment:39" align="right">comment:39</div>

With the current branch, I get

```
sage: from sage.misc.lazy_string import lazy_string, _LazyString
sage: f=lambda :"hi"
sage: s=_LazyString(f,(), {})
sage: %timeit str(s)
The slowest run took 21.90 times longer than the fastest. This could mean that an intermediate result is being cached 
1000000 loops, best of 3: 827 ns per loop
```
With the change that Nils suggested in [comment:29](#comment:29), I get

```
sage: from sage.misc.lazy_string import lazy_string, _LazyString
sage: f=lambda :"hi"
sage: s=_LazyString(f,(), {})
sage: %timeit str(s)
The slowest run took 11.15 times longer than the fastest. This could mean that an intermediate result is being cached 
1000000 loops, best of 3: 535 ns per loop
```
Pushing the change...



---

archive/issue_comments_176834.json:
```json
{
    "body": "<div id=\"comment:40\" align=\"right\">comment:40</div>\n\nUpps. I noticed that the previous branch was invalid anyway.",
    "created_at": "2015-07-23T10:25:19Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176834",
    "user": "https://github.com/simon-king-jena"
}
```

<div id="comment:40" align="right">comment:40</div>

Upps. I noticed that the previous branch was invalid anyway.



---

archive/issue_comments_176835.json:
```json
{
    "body": "Changed commit from **[75796e9](https://github.com/sagemath/sagetrac-mirror/commit/75796e94a5742aff992e56d058717dd2e94a97bf)** to **[1c64d37](https://github.com/sagemath/sagetrac-mirror/commit/1c64d3704ca91250468a0f9270a2c08b4f9c6352)**",
    "created_at": "2015-07-23T10:26:33Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176835",
    "user": "https://github.com/sagetrac-git"
}
```

Changed commit from **[75796e9](https://github.com/sagemath/sagetrac-mirror/commit/75796e94a5742aff992e56d058717dd2e94a97bf)** to **[1c64d37](https://github.com/sagemath/sagetrac-mirror/commit/1c64d3704ca91250468a0f9270a2c08b4f9c6352)**



---

archive/issue_comments_176836.json:
```json
{
    "body": "<div id=\"comment:41\"></div>\n\nBranch pushed to git repo; I updated commit sha1. New commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/1c64d3704ca91250468a0f9270a2c08b4f9c6352\">1c64d37</a></td><td><code>Add the missing changed files.</code></td></tr></table>\n",
    "created_at": "2015-07-23T10:26:33Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176836",
    "user": "https://github.com/sagetrac-git"
}
```

<div id="comment:41"></div>

Branch pushed to git repo; I updated commit sha1. New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/1c64d3704ca91250468a0f9270a2c08b4f9c6352">1c64d37</a></td><td><code>Add the missing changed files.</code></td></tr></table>




---

archive/issue_comments_176837.json:
```json
{
    "body": "Changed commit from **[1c64d37](https://github.com/sagemath/sagetrac-mirror/commit/1c64d3704ca91250468a0f9270a2c08b4f9c6352)** to **[af6750b](https://github.com/sagemath/sagetrac-mirror/commit/af6750bdb2ff7d8a6b234cf3f021e2f603030be6)**",
    "created_at": "2015-07-23T10:33:17Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176837",
    "user": "https://github.com/sagetrac-git"
}
```

Changed commit from **[1c64d37](https://github.com/sagemath/sagetrac-mirror/commit/1c64d3704ca91250468a0f9270a2c08b4f9c6352)** to **[af6750b](https://github.com/sagemath/sagetrac-mirror/commit/af6750bdb2ff7d8a6b234cf3f021e2f603030be6)**



---

archive/issue_comments_176838.json:
```json
{
    "body": "<div id=\"comment:42\"></div>\n\nBranch pushed to git repo; I updated commit sha1. New commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/af6750bdb2ff7d8a6b234cf3f021e2f603030be6\">af6750b</a></td><td><code>Use PyObject_Call, to avoid *args,**kwds</code></td></tr></table>\n",
    "created_at": "2015-07-23T10:33:17Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176838",
    "user": "https://github.com/sagetrac-git"
}
```

<div id="comment:42"></div>

Branch pushed to git repo; I updated commit sha1. New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/af6750bdb2ff7d8a6b234cf3f021e2f603030be6">af6750b</a></td><td><code>Use PyObject_Call, to avoid *args,**kwds</code></td></tr></table>




---

archive/issue_comments_176839.json:
```json
{
    "body": "<div id=\"comment:43\" align=\"right\">comment:43</div>\n\nWithout the CallObject, we get\n\n```\nsage: from sage.misc.lazy_string import lazy_string\nsage: f = lambda x: \"laziness\" + repr(x)\nsage: s = lazy_string(f, 5)\nsage: %timeit str(s)\nThe slowest run took 13.06 times longer than the fastest. This could mean that an intermediate result is being cached \n1000000 loops, best of 3: 1.53 \u00b5s per loop\nsage: s = lazy_string(\"This is %s\", ZZ)\nsage: %timeit str(s)\nThe slowest run took 8.76 times longer than the fastest. This could mean that an intermediate result is being cached \n100000 loops, best of 3: 2.29 \u00b5s per loop\n```\n\nWith the CallObject, we get\n\n```\nsage: from sage.misc.lazy_string import lazy_string\nsage: f = lambda x: \"laziness\" + repr(x)\nsage: s = lazy_string(f, 5)\nsage: %timeit str(s)\nThe slowest run took 15.19 times longer than the fastest. This could mean that an intermediate result is being cached \n1000000 loops, best of 3: 1.59 \u00b5s per loop\nsage: s = lazy_string(\"This is %s\", ZZ)\nsage: %timeit str(s)\nThe slowest run took 10.33 times longer than the fastest. This could mean that an intermediate result is being cached \n100000 loops, best of 3: 2.42 \u00b5s per loop\n```\n\nSo, in contrast to what is claimed in [comment:21](#comment:21), I don't see an improvement. Should we revert it? Or can you provide an example where the change really matters?\n\n---\nNew commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/af6750bdb2ff7d8a6b234cf3f021e2f603030be6\">af6750b</a></td><td><code>Use PyObject_Call, to avoid *args,**kwds</code></td></tr></table>\n",
    "created_at": "2015-07-23T10:41:08Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176839",
    "user": "https://github.com/simon-king-jena"
}
```

<div id="comment:43" align="right">comment:43</div>

Without the CallObject, we get

```
sage: from sage.misc.lazy_string import lazy_string
sage: f = lambda x: "laziness" + repr(x)
sage: s = lazy_string(f, 5)
sage: %timeit str(s)
The slowest run took 13.06 times longer than the fastest. This could mean that an intermediate result is being cached 
1000000 loops, best of 3: 1.53 µs per loop
sage: s = lazy_string("This is %s", ZZ)
sage: %timeit str(s)
The slowest run took 8.76 times longer than the fastest. This could mean that an intermediate result is being cached 
100000 loops, best of 3: 2.29 µs per loop
```

With the CallObject, we get

```
sage: from sage.misc.lazy_string import lazy_string
sage: f = lambda x: "laziness" + repr(x)
sage: s = lazy_string(f, 5)
sage: %timeit str(s)
The slowest run took 15.19 times longer than the fastest. This could mean that an intermediate result is being cached 
1000000 loops, best of 3: 1.59 µs per loop
sage: s = lazy_string("This is %s", ZZ)
sage: %timeit str(s)
The slowest run took 10.33 times longer than the fastest. This could mean that an intermediate result is being cached 
100000 loops, best of 3: 2.42 µs per loop
```

So, in contrast to what is claimed in [comment:21](#comment:21), I don't see an improvement. Should we revert it? Or can you provide an example where the change really matters?

---
New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/af6750bdb2ff7d8a6b234cf3f021e2f603030be6">af6750b</a></td><td><code>Use PyObject_Call, to avoid *args,**kwds</code></td></tr></table>




---

archive/issue_comments_176840.json:
```json
{
    "body": "<div id=\"comment:44\" align=\"right\">comment:44</div>\n\nIf you never *use* the fact that `_args` is a tuple and `_kwds` is a dict, there is no point in doing\n\n```\ncdef tuple _args\ncdef dict _kwargs\n```\n\nYou can simplify this (and also drop the ugly underscores) to\n\n```\ncdef args\ncdef kwargs\n```\n(which is actually an epsilon faster since there are no checks needed)",
    "created_at": "2015-07-23T10:44:10Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176840",
    "user": "https://github.com/jdemeyer"
}
```

<div id="comment:44" align="right">comment:44</div>

If you never *use* the fact that `_args` is a tuple and `_kwds` is a dict, there is no point in doing

```
cdef tuple _args
cdef dict _kwargs
```

You can simplify this (and also drop the ugly underscores) to

```
cdef args
cdef kwargs
```
(which is actually an epsilon faster since there are no checks needed)



---

archive/issue_comments_176841.json:
```json
{
    "body": "<div id=\"comment:45\" align=\"right\">comment:45</div>\n\nFor `__richcmp__`, you can just use\n\n```\nreturn PyObject_RichCompare(self.value, other, v)\n```",
    "created_at": "2015-07-23T10:48:20Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176841",
    "user": "https://github.com/jdemeyer"
}
```

<div id="comment:45" align="right">comment:45</div>

For `__richcmp__`, you can just use

```
return PyObject_RichCompare(self.value, other, v)
```



---

archive/issue_comments_176842.json:
```json
{
    "body": "<div id=\"comment:46\" align=\"right\">comment:46</div>\n\nNext, I wish to address the slowness of `LazyFormat`. We have\n\n```\nsage: from sage.misc.lazy_format import LazyFormat\nsage: form = LazyFormat(\"<%s>\")\nsage: cform = LazyFormat(\"\"+form)\nsage: cform\nunbound LazyFormat(\"<%s>\")\nsage: %timeit copy(form)\nThe slowest run took 9.12 times longer than the fastest. This could mean that an intermediate result is being cached \n100000 loops, best of 3: 16.3 \u00b5s per loop\nsage: %timeit LazyFormat(\"\"+form)\nThe slowest run took 12.69 times longer than the fastest. This could mean that an intermediate result is being cached \n1000000 loops, best of 3: 395 ns per loop\n```\nSo, this is what we could use in `__mod__` for speed-up",
    "created_at": "2015-07-23T10:50:40Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176842",
    "user": "https://github.com/simon-king-jena"
}
```

<div id="comment:46" align="right">comment:46</div>

Next, I wish to address the slowness of `LazyFormat`. We have

```
sage: from sage.misc.lazy_format import LazyFormat
sage: form = LazyFormat("<%s>")
sage: cform = LazyFormat(""+form)
sage: cform
unbound LazyFormat("<%s>")
sage: %timeit copy(form)
The slowest run took 9.12 times longer than the fastest. This could mean that an intermediate result is being cached 
100000 loops, best of 3: 16.3 µs per loop
sage: %timeit LazyFormat(""+form)
The slowest run took 12.69 times longer than the fastest. This could mean that an intermediate result is being cached 
1000000 loops, best of 3: 395 ns per loop
```
So, this is what we could use in `__mod__` for speed-up



---

archive/issue_comments_176843.json:
```json
{
    "body": "<div id=\"comment:47\"></div>\n\nBranch pushed to git repo; I updated commit sha1. New commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/2eab18c54d05655257042468503dcf75c3baaa33\">2eab18c</a></td><td><code>Speed-up LazyFormat.__mod__ by deleting copy(self)</code></td></tr></table>\n",
    "created_at": "2015-07-23T10:53:19Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176843",
    "user": "https://github.com/sagetrac-git"
}
```

<div id="comment:47"></div>

Branch pushed to git repo; I updated commit sha1. New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/2eab18c54d05655257042468503dcf75c3baaa33">2eab18c</a></td><td><code>Speed-up LazyFormat.__mod__ by deleting copy(self)</code></td></tr></table>




---

archive/issue_comments_176844.json:
```json
{
    "body": "Changed commit from **[af6750b](https://github.com/sagemath/sagetrac-mirror/commit/af6750bdb2ff7d8a6b234cf3f021e2f603030be6)** to **[2eab18c](https://github.com/sagemath/sagetrac-mirror/commit/2eab18c54d05655257042468503dcf75c3baaa33)**",
    "created_at": "2015-07-23T10:53:19Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176844",
    "user": "https://github.com/sagetrac-git"
}
```

Changed commit from **[af6750b](https://github.com/sagemath/sagetrac-mirror/commit/af6750bdb2ff7d8a6b234cf3f021e2f603030be6)** to **[2eab18c](https://github.com/sagemath/sagetrac-mirror/commit/2eab18c54d05655257042468503dcf75c3baaa33)**



---

archive/issue_comments_176845.json:
```json
{
    "body": "<div id=\"comment:48\" align=\"right\">comment:48</div>\n\nThe improvement is clear:\n\nOld version:\n\n```\nsage: from sage.misc.lazy_format import LazyFormat\nsage: form = LazyFormat(\"<%s>\")\nsage: %timeit form%\"hi\"\n10000 loops, best of 3: 22 \u00b5s per loop\n```\n\nNew version:\n\n```\nsage: from sage.misc.lazy_format import LazyFormat\nsage: form = LazyFormat(\"<%s>\")\nsage: form%\"hi\"\n<hi>\nsage: %timeit form%\"hi\"\nThe slowest run took 6.91 times longer than the fastest. This could mean that an intermediate result is being cached \n1000000 loops, best of 3: 1.28 \u00b5s per loop\n```",
    "created_at": "2015-07-23T10:54:56Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176845",
    "user": "https://github.com/simon-king-jena"
}
```

<div id="comment:48" align="right">comment:48</div>

The improvement is clear:

Old version:

```
sage: from sage.misc.lazy_format import LazyFormat
sage: form = LazyFormat("<%s>")
sage: %timeit form%"hi"
10000 loops, best of 3: 22 µs per loop
```

New version:

```
sage: from sage.misc.lazy_format import LazyFormat
sage: form = LazyFormat("<%s>")
sage: form%"hi"
<hi>
sage: %timeit form%"hi"
The slowest run took 6.91 times longer than the fastest. This could mean that an intermediate result is being cached 
1000000 loops, best of 3: 1.28 µs per loop
```



---

archive/issue_comments_176846.json:
```json
{
    "body": "<div id=\"comment:49\" align=\"right\">comment:49</div>\n\nReplying to [@jdemeyer](#comment:44):\n> If you never *use* the fact that `_args` is a tuple and `_kwds` is a dict, there is no point in doing\n> \n> ```\n> cdef tuple _args\n> cdef dict _kwargs\n> ```\n> \n> You can simplify this (and also drop the ugly underscores) to\n> \n> ```\n> cdef args\n> cdef kwargs\n> ```\n> (which is actually an epsilon faster since there are no checks needed)\n\nDon't we use it being tuple/dict when we use `CallObject`?",
    "created_at": "2015-07-23T10:55:48Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176846",
    "user": "https://github.com/simon-king-jena"
}
```

<div id="comment:49" align="right">comment:49</div>

Replying to [@jdemeyer](#comment:44):
> If you never *use* the fact that `_args` is a tuple and `_kwds` is a dict, there is no point in doing
> 
> ```
> cdef tuple _args
> cdef dict _kwargs
> ```
> 
> You can simplify this (and also drop the ugly underscores) to
> 
> ```
> cdef args
> cdef kwargs
> ```
> (which is actually an epsilon faster since there are no checks needed)

Don't we use it being tuple/dict when we use `CallObject`?



---

archive/issue_comments_176847.json:
```json
{
    "body": "<div id=\"comment:50\" align=\"right\">comment:50</div>\n\nReplying to [@simon-king-jena](#comment:49):\n> Don't we use it being tuple/dict when we use `CallObject`?\n\nNo, Cython declares it as\n\n```\nobject PyObject_CallObject(object callable_object, object args)\n```",
    "created_at": "2015-07-23T11:01:17Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176847",
    "user": "https://github.com/jdemeyer"
}
```

<div id="comment:50" align="right">comment:50</div>

Replying to [@simon-king-jena](#comment:49):
> Don't we use it being tuple/dict when we use `CallObject`?

No, Cython declares it as

```
object PyObject_CallObject(object callable_object, object args)
```



---

archive/issue_comments_176848.json:
```json
{
    "body": "<div id=\"comment:51\" align=\"right\">comment:51</div>\n\nReplying to [@jdemeyer](#comment:45):\n> For `__richcmp__`, you can just use\n> \n> ```\n> return PyObject_RichCompare(self.value, other, v)\n> ```\n\nI tried it, and to my surprise it seems slightly slower:\n\nCurrent code\n\n```\nsage: from sage.misc.lazy_string import lazy_string\nsage: f = lambda: \"laziness\"\nsage: s = lazy_string(f)\nsage: %timeit s < 'laziness'\nThe slowest run took 17.05 times longer than the fastest. This could mean that an intermediate result is being cached \n1000000 loops, best of 3: 294 ns per loop\nsage: %timeit s <= 'aziness'\nThe slowest run took 19.35 times longer than the fastest. This could mean that an intermediate result is being cached \n1000000 loops, best of 3: 308 ns per loop\nsage: %timeit s == 'laziness'\nThe slowest run took 20.48 times longer than the fastest. This could mean that an intermediate result is being cached \n1000000 loops, best of 3: 291 ns per loop\nsage: %timeit s != 'aziness'\nThe slowest run took 40.70 times longer than the fastest. This could mean that an intermediate result is being cached \n1000000 loops, best of 3: 293 ns per loop\n```\n\nWith `PyObject_RichCompare`:\n\n```\nsage: from sage.misc.lazy_string import lazy_string\nsage: f = lambda: \"laziness\"\nsage: s = lazy_string(f)\nsage: %timeit s < 'laziness'\nThe slowest run took 17.50 times longer than the fastest. This could mean that an intermediate result is being cached \n1000000 loops, best of 3: 341 ns per loop\nsage: %timeit s <= 'aziness'\nThe slowest run took 20.08 times longer than the fastest. This could mean that an intermediate result is being cached \n1000000 loops, best of 3: 344 ns per loop\nsage: %timeit s == 'laziness'\nThe slowest run took 20.68 times longer than the fastest. This could mean that an intermediate result is being cached \n1000000 loops, best of 3: 334 ns per loop\nsage: %timeit s != 'aziness'\nThe slowest run took 21.79 times longer than the fastest. This could mean that an intermediate result is being cached \n1000000 loops, best of 3: 328 ns per loop\n```\n\nSo, we shouldn't do that.",
    "created_at": "2015-07-23T11:01:46Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176848",
    "user": "https://github.com/simon-king-jena"
}
```

<div id="comment:51" align="right">comment:51</div>

Replying to [@jdemeyer](#comment:45):
> For `__richcmp__`, you can just use
> 
> ```
> return PyObject_RichCompare(self.value, other, v)
> ```

I tried it, and to my surprise it seems slightly slower:

Current code

```
sage: from sage.misc.lazy_string import lazy_string
sage: f = lambda: "laziness"
sage: s = lazy_string(f)
sage: %timeit s < 'laziness'
The slowest run took 17.05 times longer than the fastest. This could mean that an intermediate result is being cached 
1000000 loops, best of 3: 294 ns per loop
sage: %timeit s <= 'aziness'
The slowest run took 19.35 times longer than the fastest. This could mean that an intermediate result is being cached 
1000000 loops, best of 3: 308 ns per loop
sage: %timeit s == 'laziness'
The slowest run took 20.48 times longer than the fastest. This could mean that an intermediate result is being cached 
1000000 loops, best of 3: 291 ns per loop
sage: %timeit s != 'aziness'
The slowest run took 40.70 times longer than the fastest. This could mean that an intermediate result is being cached 
1000000 loops, best of 3: 293 ns per loop
```

With `PyObject_RichCompare`:

```
sage: from sage.misc.lazy_string import lazy_string
sage: f = lambda: "laziness"
sage: s = lazy_string(f)
sage: %timeit s < 'laziness'
The slowest run took 17.50 times longer than the fastest. This could mean that an intermediate result is being cached 
1000000 loops, best of 3: 341 ns per loop
sage: %timeit s <= 'aziness'
The slowest run took 20.08 times longer than the fastest. This could mean that an intermediate result is being cached 
1000000 loops, best of 3: 344 ns per loop
sage: %timeit s == 'laziness'
The slowest run took 20.68 times longer than the fastest. This could mean that an intermediate result is being cached 
1000000 loops, best of 3: 334 ns per loop
sage: %timeit s != 'aziness'
The slowest run took 21.79 times longer than the fastest. This could mean that an intermediate result is being cached 
1000000 loops, best of 3: 328 ns per loop
```

So, we shouldn't do that.



---

archive/issue_comments_176849.json:
```json
{
    "body": "<div id=\"comment:52\" align=\"right\">comment:52</div>\n\nI am currently unable to check out this branch:\n\n```\nfatal: unable to connect to trac.sagemath.org:\ntrac.sagemath.org[0: 128.208.160.253]: errno=Connection timed out\n```\nbut you should check whether it makes sense to define a `cdef value(self)` function and use that everywhere in the private interface.",
    "created_at": "2015-07-23T11:03:49Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176849",
    "user": "https://github.com/jdemeyer"
}
```

<div id="comment:52" align="right">comment:52</div>

I am currently unable to check out this branch:

```
fatal: unable to connect to trac.sagemath.org:
trac.sagemath.org[0: 128.208.160.253]: errno=Connection timed out
```
but you should check whether it makes sense to define a `cdef value(self)` function and use that everywhere in the private interface.



---

archive/issue_comments_176850.json:
```json
{
    "body": "Changed commit from **[2eab18c](https://github.com/sagemath/sagetrac-mirror/commit/2eab18c54d05655257042468503dcf75c3baaa33)** to **[aafac26](https://github.com/sagemath/sagetrac-mirror/commit/aafac26f2b18dc81e79496d6eeba03d69f364038)**",
    "created_at": "2015-07-23T11:07:33Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176850",
    "user": "https://github.com/sagetrac-git"
}
```

Changed commit from **[2eab18c](https://github.com/sagemath/sagetrac-mirror/commit/2eab18c54d05655257042468503dcf75c3baaa33)** to **[aafac26](https://github.com/sagemath/sagetrac-mirror/commit/aafac26f2b18dc81e79496d6eeba03d69f364038)**



---

archive/issue_comments_176851.json:
```json
{
    "body": "<div id=\"comment:53\"></div>\n\nBranch pushed to git repo; I updated commit sha1. New commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/aafac26f2b18dc81e79496d6eeba03d69f364038\">aafac26</a></td><td><code>Avoid useless type declaration</code></td></tr></table>\n",
    "created_at": "2015-07-23T11:07:33Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176851",
    "user": "https://github.com/sagetrac-git"
}
```

<div id="comment:53"></div>

Branch pushed to git repo; I updated commit sha1. New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/aafac26f2b18dc81e79496d6eeba03d69f364038">aafac26</a></td><td><code>Avoid useless type declaration</code></td></tr></table>




---

archive/issue_events_206533.json:
```json
{
    "actor": "https://github.com/simon-king-jena",
    "created_at": "2015-07-23T11:09:54Z",
    "event": "unlabeled",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "label": "https://github.com/sagemath/sage/labels/needs%20work",
    "label_color": "ffff00",
    "label_name": "needs work",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/14585#event-206533"
}
```



---

archive/issue_events_206534.json:
```json
{
    "actor": "https://github.com/simon-king-jena",
    "created_at": "2015-07-23T11:09:54Z",
    "event": "labeled",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "label": "https://github.com/sagemath/sage/labels/needs%20review",
    "label_color": "7fff00",
    "label_name": "needs review",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/14585#event-206534"
}
```



---

archive/issue_comments_176852.json:
```json
{
    "body": "<div id=\"comment:54\" align=\"right\">comment:54</div>\n\nReplying to [@jdemeyer](#comment:50):\n> Replying to [@simon-king-jena](#comment:49):\n> > Don't we use it being tuple/dict when we use `CallObject`?\n\n> No, Cython declares it as\n> \n> ```\n> object PyObject_CallObject(object callable_object, object args)\n> ```\n\nOK, I have removed the underscore, and I have made it objects rather than tuple/dict. However, in the two methods that set/change the attributes (that's `__init__` and `update_lazy_string`), I am still checking the types. I think now it can be reviewed.",
    "created_at": "2015-07-23T11:09:54Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176852",
    "user": "https://github.com/simon-king-jena"
}
```

<div id="comment:54" align="right">comment:54</div>

Replying to [@jdemeyer](#comment:50):
> Replying to [@simon-king-jena](#comment:49):
> > Don't we use it being tuple/dict when we use `CallObject`?

> No, Cython declares it as
> 
> ```
> object PyObject_CallObject(object callable_object, object args)
> ```

OK, I have removed the underscore, and I have made it objects rather than tuple/dict. However, in the two methods that set/change the attributes (that's `__init__` and `update_lazy_string`), I am still checking the types. I think now it can be reviewed.



---

archive/issue_events_206535.json:
```json
{
    "actor": "https://github.com/jdemeyer",
    "created_at": "2015-07-23T11:26:01Z",
    "event": "unlabeled",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "label": "https://github.com/sagemath/sage/labels/needs%20review",
    "label_color": "7fff00",
    "label_name": "needs review",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/14585#event-206535"
}
```



---

archive/issue_events_206536.json:
```json
{
    "actor": "https://github.com/jdemeyer",
    "created_at": "2015-07-23T11:26:01Z",
    "event": "labeled",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "label": "https://github.com/sagemath/sage/labels/needs%20work",
    "label_color": "ffff00",
    "label_name": "needs work",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/14585#event-206536"
}
```



---

archive/issue_comments_176853.json:
```json
{
    "body": "<div id=\"comment:55\" align=\"right\">comment:55</div>\n\nInstead of\n\n```\ndef __init__(self, f, tuple args, dict kwargs):\n```\nbetter use\n\n```\ndef __init__(self, f, args, kwargs):\n```\n\nIf you really want to check the types, you can do that by\n\n```\nself.args = <tuple?>args\nself.kwds = <dict?>kwds\n```\n(this also disallows `None` for `args` and `kwds`)",
    "created_at": "2015-07-23T11:26:01Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176853",
    "user": "https://github.com/jdemeyer"
}
```

<div id="comment:55" align="right">comment:55</div>

Instead of

```
def __init__(self, f, tuple args, dict kwargs):
```
better use

```
def __init__(self, f, args, kwargs):
```

If you really want to check the types, you can do that by

```
self.args = <tuple?>args
self.kwds = <dict?>kwds
```
(this also disallows `None` for `args` and `kwds`)



---

archive/issue_comments_176854.json:
```json
{
    "body": "<div id=\"comment:56\" align=\"right\">comment:56</div>\n\nThe old-style doctest continuation\n\n```\nsage:\n...\n```\nshould also be fixed.\n\nAlso, in `__richcmp__`, do not use magic values like `v == 2`, but use the symbolic constants `Py_EQ` and so on (but personally I would like to know why `PyObject_RichCompare` is slower).",
    "created_at": "2015-07-23T11:27:22Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176854",
    "user": "https://github.com/jdemeyer"
}
```

<div id="comment:56" align="right">comment:56</div>

The old-style doctest continuation

```
sage:
...
```
should also be fixed.

Also, in `__richcmp__`, do not use magic values like `v == 2`, but use the symbolic constants `Py_EQ` and so on (but personally I would like to know why `PyObject_RichCompare` is slower).



---

archive/issue_comments_176855.json:
```json
{
    "body": "<div id=\"comment:57\" align=\"right\">comment:57</div>\n\nReplying to [@jdemeyer](#comment:56):\n> Also, in `__richcmp__`, do not use magic values like `v == 2`, but use the symbolic constants `Py_EQ` and so on (but personally I would like to know why `PyObject_RichCompare` is slower).\n\nI wonder, too.",
    "created_at": "2015-07-23T11:37:47Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176855",
    "user": "https://github.com/simon-king-jena"
}
```

<div id="comment:57" align="right">comment:57</div>

Replying to [@jdemeyer](#comment:56):
> Also, in `__richcmp__`, do not use magic values like `v == 2`, but use the symbolic constants `Py_EQ` and so on (but personally I would like to know why `PyObject_RichCompare` is slower).

I wonder, too.



---

archive/issue_comments_176856.json:
```json
{
    "body": "<div id=\"comment:58\" align=\"right\">comment:58</div>\n\n[comment:52] gives an important optimization which really should be applied. I can do it if you give me some time.",
    "created_at": "2015-07-23T11:44:11Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176856",
    "user": "https://github.com/jdemeyer"
}
```

<div id="comment:58" align="right">comment:58</div>

[comment:52] gives an important optimization which really should be applied. I can do it if you give me some time.



---

archive/issue_comments_176857.json:
```json
{
    "body": "<div id=\"comment:59\" align=\"right\">comment:59</div>\n\nReplying to [@simon-king-jena](#comment:57):\n> Replying to [@jdemeyer](#comment:56):\n> > Also, in `__richcmp__`, do not use magic values like `v == 2`, but use the symbolic constants `Py_EQ` and so on (but personally I would like to know why `PyObject_RichCompare` is slower).\n\n> \n> I wonder, too.\n\nStrangely, after repeating the test, I get the opposite behaviour: A slight improvement. Perhaps I had confused the two test results?\n\nAnyway, pushing new commits shortly.",
    "created_at": "2015-07-23T11:46:21Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176857",
    "user": "https://github.com/simon-king-jena"
}
```

<div id="comment:59" align="right">comment:59</div>

Replying to [@simon-king-jena](#comment:57):
> Replying to [@jdemeyer](#comment:56):
> > Also, in `__richcmp__`, do not use magic values like `v == 2`, but use the symbolic constants `Py_EQ` and so on (but personally I would like to know why `PyObject_RichCompare` is slower).

> 
> I wonder, too.

Strangely, after repeating the test, I get the opposite behaviour: A slight improvement. Perhaps I had confused the two test results?

Anyway, pushing new commits shortly.



---

archive/issue_comments_176858.json:
```json
{
    "body": "Changed commit from **[aafac26](https://github.com/sagemath/sagetrac-mirror/commit/aafac26f2b18dc81e79496d6eeba03d69f364038)** to **[f1cccb5](https://github.com/sagemath/sagetrac-mirror/commit/f1cccb5557cd49a302a06f8376adfb888f655ea0)**",
    "created_at": "2015-07-23T11:49:14Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176858",
    "user": "https://github.com/sagetrac-git"
}
```

Changed commit from **[aafac26](https://github.com/sagemath/sagetrac-mirror/commit/aafac26f2b18dc81e79496d6eeba03d69f364038)** to **[f1cccb5](https://github.com/sagemath/sagetrac-mirror/commit/f1cccb5557cd49a302a06f8376adfb888f655ea0)**



---

archive/issue_comments_176859.json:
```json
{
    "body": "<div id=\"comment:60\"></div>\n\nBranch pushed to git repo; I updated commit sha1. New commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/c7a52d1d25f9d037e3cd155b4a9449207e81119e\">c7a52d1</a></td><td><code>New style doctest continuation</code></td></tr><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/0b5820fba5f681a257781bc84fb510c8d67938e0\">0b5820f</a></td><td><code>Simplify lazy_string.__richcmp__</code></td></tr><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/f1cccb5557cd49a302a06f8376adfb888f655ea0\">f1cccb5</a></td><td><code>Modified type checking</code></td></tr></table>\n",
    "created_at": "2015-07-23T11:49:14Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176859",
    "user": "https://github.com/sagetrac-git"
}
```

<div id="comment:60"></div>

Branch pushed to git repo; I updated commit sha1. New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/c7a52d1d25f9d037e3cd155b4a9449207e81119e">c7a52d1</a></td><td><code>New style doctest continuation</code></td></tr><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/0b5820fba5f681a257781bc84fb510c8d67938e0">0b5820f</a></td><td><code>Simplify lazy_string.__richcmp__</code></td></tr><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/f1cccb5557cd49a302a06f8376adfb888f655ea0">f1cccb5</a></td><td><code>Modified type checking</code></td></tr></table>




---

archive/issue_comments_176860.json:
```json
{
    "body": "<div id=\"comment:61\" align=\"right\">comment:61</div>\n\n`__rmod__` should be removed and be folded into `__mod__`.",
    "created_at": "2015-07-23T11:57:20Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176860",
    "user": "https://github.com/jdemeyer"
}
```

<div id="comment:61" align="right">comment:61</div>

`__rmod__` should be removed and be folded into `__mod__`.



---

archive/issue_comments_176861.json:
```json
{
    "body": "<div id=\"comment:62\" align=\"right\">comment:62</div>\n\nIf you think that comparing two lazy strings (as opposed to comparing a lazy string and an ordinary string) is common enough, you can do something like\n\n```\n        self = self.value  # Assuming that self is indeed a _LazyString\n        if isinstance(other, _LazyString):\n            other = other.value\n        return PyObject_RichCompare(self, other, op)\n```",
    "created_at": "2015-07-23T12:05:28Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176861",
    "user": "https://github.com/jdemeyer"
}
```

<div id="comment:62" align="right">comment:62</div>

If you think that comparing two lazy strings (as opposed to comparing a lazy string and an ordinary string) is common enough, you can do something like

```
        self = self.value  # Assuming that self is indeed a _LazyString
        if isinstance(other, _LazyString):
            other = other.value
        return PyObject_RichCompare(self, other, op)
```



---

archive/issue_comments_176862.json:
```json
{
    "body": "<div id=\"comment:63\" align=\"right\">comment:63</div>\n\nI have code ready for [comment:52]\n\nIf you want me to push it, just tell me.",
    "created_at": "2015-07-23T12:06:08Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176862",
    "user": "https://github.com/jdemeyer"
}
```

<div id="comment:63" align="right">comment:63</div>

I have code ready for [comment:52]

If you want me to push it, just tell me.



---

archive/issue_comments_176863.json:
```json
{
    "body": "Changed branch from **[u/SimonKing/Make_lazy_format_faster](https://github.com/sagemath/sagetrac-mirror/tree/u/SimonKing/Make_lazy_format_faster)** to **[u/jdemeyer/Make_lazy_format_faster](https://github.com/sagemath/sagetrac-mirror/tree/u/jdemeyer/Make_lazy_format_faster)**",
    "created_at": "2015-07-23T13:42:40Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176863",
    "user": "https://github.com/jdemeyer"
}
```

Changed branch from **[u/SimonKing/Make_lazy_format_faster](https://github.com/sagemath/sagetrac-mirror/tree/u/SimonKing/Make_lazy_format_faster)** to **[u/jdemeyer/Make_lazy_format_faster](https://github.com/sagemath/sagetrac-mirror/tree/u/jdemeyer/Make_lazy_format_faster)**



---

archive/issue_events_206537.json:
```json
{
    "actor": "https://github.com/jdemeyer",
    "created_at": "2015-07-23T13:42:56Z",
    "event": "unlabeled",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "label": "https://github.com/sagemath/sage/labels/needs%20work",
    "label_color": "ffff00",
    "label_name": "needs work",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/14585#event-206537"
}
```



---

archive/issue_events_206538.json:
```json
{
    "actor": "https://github.com/jdemeyer",
    "created_at": "2015-07-23T13:42:56Z",
    "event": "labeled",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "label": "https://github.com/sagemath/sage/labels/needs%20review",
    "label_color": "7fff00",
    "label_name": "needs review",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/14585#event-206538"
}
```



---

archive/issue_comments_176864.json:
```json
{
    "body": "Changed commit from **[f1cccb5](https://github.com/sagemath/sagetrac-mirror/commit/f1cccb5557cd49a302a06f8376adfb888f655ea0)** to **[96b61e3](https://github.com/sagemath/sagetrac-mirror/commit/96b61e39a4669296ea042d6f491997319dd36c5e)**",
    "created_at": "2015-07-23T13:42:56Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176864",
    "user": "https://github.com/jdemeyer"
}
```

Changed commit from **[f1cccb5](https://github.com/sagemath/sagetrac-mirror/commit/f1cccb5557cd49a302a06f8376adfb888f655ea0)** to **[96b61e3](https://github.com/sagemath/sagetrac-mirror/commit/96b61e39a4669296ea042d6f491997319dd36c5e)**



---

archive/issue_comments_176865.json:
```json
{
    "body": "<div id=\"comment:65\"></div>\n\nNew commits:\n<table><tr><td><a href=\"https://github.com/sagemath/sagetrac-mirror/commit/96b61e39a4669296ea042d6f491997319dd36c5e\">96b61e3</a></td><td><code>Use a cdef method for value() + minor fixes</code></td></tr></table>\n",
    "created_at": "2015-07-23T13:42:56Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176865",
    "user": "https://github.com/jdemeyer"
}
```

<div id="comment:65"></div>

New commits:
<table><tr><td><a href="https://github.com/sagemath/sagetrac-mirror/commit/96b61e39a4669296ea042d6f491997319dd36c5e">96b61e3</a></td><td><code>Use a cdef method for value() + minor fixes</code></td></tr></table>




---

archive/issue_comments_176866.json:
```json
{
    "body": "Changed author from **Simon King** to **Simon King, Jeroen Demeyer**",
    "created_at": "2015-07-23T13:42:56Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176866",
    "user": "https://github.com/jdemeyer"
}
```

Changed author from **Simon King** to **Simon King, Jeroen Demeyer**



---

archive/issue_comments_176867.json:
```json
{
    "body": "<div id=\"comment:66\" align=\"right\">comment:66</div>\n\nJeroen, your changes look good to me.\n\nSo, Cython allows to have a cdef function and a property of the same name?\n\nAnyway, the new tests pass, so, I can positively review your changes.",
    "created_at": "2015-07-23T14:05:52Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176867",
    "user": "https://github.com/simon-king-jena"
}
```

<div id="comment:66" align="right">comment:66</div>

Jeroen, your changes look good to me.

So, Cython allows to have a cdef function and a property of the same name?

Anyway, the new tests pass, so, I can positively review your changes.



---

archive/issue_comments_176868.json:
```json
{
    "body": "<div id=\"comment:67\" align=\"right\">comment:67</div>\n\nReplying to [@simon-king-jena](#comment:66):\n> Jeroen, your changes look good to me.\n> \n> So, Cython allows to have a cdef function and a property of the same name?\n> \n> Anyway, the new tests pass, so, I can positively review your changes.",
    "created_at": "2015-07-23T15:47:28Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176868",
    "user": "https://github.com/jdemeyer"
}
```

<div id="comment:67" align="right">comment:67</div>

Replying to [@simon-king-jena](#comment:66):
> Jeroen, your changes look good to me.
> 
> So, Cython allows to have a cdef function and a property of the same name?
> 
> Anyway, the new tests pass, so, I can positively review your changes.



---

archive/issue_events_206539.json:
```json
{
    "actor": "https://github.com/jdemeyer",
    "created_at": "2015-07-23T15:47:41Z",
    "event": "unlabeled",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "label": "https://github.com/sagemath/sage/labels/needs%20review",
    "label_color": "7fff00",
    "label_name": "needs review",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/14585#event-206539"
}
```



---

archive/issue_events_206540.json:
```json
{
    "actor": "https://github.com/jdemeyer",
    "created_at": "2015-07-23T15:47:41Z",
    "event": "labeled",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "label": "https://github.com/sagemath/sage/labels/positive%20review",
    "label_color": "dfffc0",
    "label_name": "positive review",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/14585#event-206540"
}
```



---

archive/issue_comments_176869.json:
```json
{
    "body": "Reviewer: **Simon King, Jeroen Demeyer**",
    "created_at": "2015-07-23T15:47:41Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176869",
    "user": "https://github.com/jdemeyer"
}
```

Reviewer: **Simon King, Jeroen Demeyer**



---

archive/issue_comments_176870.json:
```json
{
    "body": "<div id=\"comment:69\" align=\"right\">comment:69</div>\n\nIdea for a follow-up ticket: make `__mod__` return a `LazyString` itself such that the `%` is lazily evaluated. Then we can completely merge the functionality of `LazyString` and `LazyFormat`.",
    "created_at": "2015-07-23T17:10:41Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176870",
    "user": "https://github.com/jdemeyer"
}
```

<div id="comment:69" align="right">comment:69</div>

Idea for a follow-up ticket: make `__mod__` return a `LazyString` itself such that the `%` is lazily evaluated. Then we can completely merge the functionality of `LazyString` and `LazyFormat`.



---

archive/issue_comments_176871.json:
```json
{
    "body": "Changed branch from **[u/jdemeyer/Make_lazy_format_faster](https://github.com/sagemath/sagetrac-mirror/tree/u/jdemeyer/Make_lazy_format_faster)** to **[96b61e3](https://github.com/sagemath/sagetrac-mirror/commit/96b61e39a4669296ea042d6f491997319dd36c5e)**",
    "created_at": "2015-07-27T15:16:00Z",
    "formatter": "markdown",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_comment",
    "url": "https://github.com/sagemath/sage/issues/14585#issuecomment-176871",
    "user": "https://github.com/vbraun"
}
```

Changed branch from **[u/jdemeyer/Make_lazy_format_faster](https://github.com/sagemath/sagetrac-mirror/tree/u/jdemeyer/Make_lazy_format_faster)** to **[96b61e3](https://github.com/sagemath/sagetrac-mirror/commit/96b61e39a4669296ea042d6f491997319dd36c5e)**



---

archive/issue_events_206541.json:
```json
{
    "actor": "https://github.com/vbraun",
    "created_at": "2015-07-27T15:16:00Z",
    "event": "unlabeled",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "label": "https://github.com/sagemath/sage/labels/positive%20review",
    "label_color": "dfffc0",
    "label_name": "positive review",
    "label_text_color": "ffffff",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/14585#event-206541"
}
```



---

archive/issue_events_206542.json:
```json
{
    "actor": "https://github.com/vbraun",
    "commit_id": "b41c808e8ad2a4ef8439e536c1f7a4d4334d0d67",
    "commit_repository": "https://github.com/sagemath/sage",
    "created_at": "2015-07-27T15:16:00Z",
    "event": "closed",
    "issue": "https://github.com/sagemath/sage/issues/14585",
    "type": "issue_event",
    "url": "https://github.com/sagemath/sage/issues/14585#event-206542"
}
```
